# Socket.io 중계 서버 주소. **하드코딩 폴백이 없다.** 비어 있으면 연결 실패 화면에
# "서버 주소가 설정되지 않았다"가 뜬다. 조용히 로컬로 붙는 것보다 낫다.
#   로컬  http://localhost:3001
#   배포  https://<render-service>.onrender.com
# **폰은 HTTPS로 연다.** iOS는 보안 컨텍스트가 아니면 동작 센서 권한을 안 준다.
# 그 페이지에서 http 서버 주소를 쓰면 혼합 콘텐츠로 막히므로 반드시 https다.
VITE_SERVER_URL=
