# PROGRESS

## 완료
- SETUP: 저장소 구조, 의존성, 설정 파일 생성
  - 폴더 5개(presentation, brand, arena, controller, server) + shared, docs 생성
  - shared/tokens.js, shared/protocol.js 작성. 각 앱 src/tokens.js는 재수출만
  - 프런트 4개 앱 Vite + React 18 + Tailwind 3 + React Router 6 + lucide-react 설치
  - arena, controller에 socket.io-client 추가. 골격 모듈 파일 생성
  - server Express + Socket.io + cors, 방 코드 페어링과 릴레이 최소 구현, 스모크 통과
  - 금지 항목 grep 전부 0건, vercel.json 4개, .env.example 5개
- docs v2: 간합 기준 문서 전면 교체
  - DESIGN.md v2(블랙 + 크롬 + 쨍한 레드, 대비 실측 포함)
  - IA.md, ROUTES.md, COMPONENTS.md, PATTERNS.md 간합 버전으로 교체(이전 프로젝트 내용 폐기)
  - AGENTS.md 간합 병렬 구조로 재작성, BACKEND.md 삭제 후 SERVER.md로 대체
  - shared/tokens.js v2 교체(radius, glow 추가, 네이비 팔레트 폐기)
- PHASE 0 마무리 (P-0 docs+shared 트랙, 종료)
  - MOTION, RESPONSIVE, PITFALLS, CLAUDE 4개에 표준 문서 배너 삽입(본문 무수정)
  - shared/protocol.js MSG에 PEER_LEFT, ERROR 추가. SERVER.md 프로토콜과 1:1
  - server: disconnect 시 peer_left 통지, 같은 방 재접속 허용(옛 소켓 슬롯 인계), 10분 유휴 GC(lastSeenAt 기준, 스윕 60초)
  - 페어링 실패를 room{ok:false}에서 error{reason}으로 분리. 소켓 스모크 6항목 통과
  - arena/.env.example에 VITE_CONTROLLER_URL 추가
  - 4개 앱 index.css 첫 페인트 예외를 #0B0B0E / #F2F6FF로 교체
  - 4개 앱 tailwind extend를 v2 토큰 그룹(bg, red, blue, steel, trail, txt, line)으로 재매핑. accent 폐기
  - build:all 4개 성공, 네이비 잔재 코드 0건, HEX 하드코딩 예외 밖 0건, 이모지 0건
- PHASE 0 마감 수정 (P-0b 트랙, 종료)
  - steel.gradient는 colors 매핑에서 제외, style 경유 전용. tokens.js의 값 자체는 유지
  - 스킬 경로 .claude/skills/coding으로 통일, SESSION_HEADER와 SKILL.md 표기 정정
  - 커밋 3분할: docs+tokens+server / 스킬 배치 / vendor 반입
- P-C C1 arena 키보드 모드 (트랙 종료, 확인 대기)
  - loop.js 고정 타임스텝(1/60 누적기) + 가변 렌더, 로직 시계에만 timeScale, 탭 비활성 정지
  - machine.js phase 8종 순수 전이 함수 + 진입 이탈 훅. 키보드는 PAIRING과 CALIBRATION 건너뜀
  - judge.js 결정적 판정. 거리 35~55, 쿨다운 350ms, 리포스트 600ms 2점, FEINT 무페널티, REAL 가드 패리
  - opponents.js 유파 2종(이탈리아 세이버, 프랑스 에페), rng.js mulberry32 시드 난수
  - renderer/ 4레이어 canvas 2D(배경, 선수 실루엣 + 잔상, 궤적 가산 블렌딩, 파티클 풀링)
  - HUD DOM 6종 + IDLE, MATCH_END, 1024 미만 차단 화면
  - 입력 추상화 input.js(키보드와 C3 소켓 action이 같은 큐로 수렴), F9 전 phase 동작
  - 검증: 셀프테스트 6/6, 결정성 서명 일치, 5점 완주 양방향, 실브라우저 1분 교전 p50 59.9fps
  - 시간 팽창 지속 1200ms와 쿨다운 8000ms 실측, reduced motion 비활성 확인
  - C4 웹캠 지표 주입 인터페이스(shouldDilate의 extra 인자) 선반영
  - 명중 4분기 규칙 승인 완료. COMPONENTS.md judge 항목으로 승격(사양 확정)
  - 패리가 phase를 멈추지 않도록 수정. 리포스트 윈도우가 만료되던 버그 해소
- P-A A1 presentation 스크롤 엔진과 8섹션 골격 (확인 대기)
  - gsap 3.15 + lenis 1.3 도입. @bsmnt/scrollytelling은 채택하지 않음
    (React 18 peer는 맞으나 2024-02 이후 배포 정지, Radix portal과 slot 의존 2개를 끌고 온다.
     PITFALLS 서드파티 절대로 발표 중 죽으면 복구가 없어 ScrollTrigger 직접 제어로 간다)
  - lib/motionMode.js 전역 분기 유틸. 이 트랙의 모든 연출은 이 파일을 거친다
  - lib/scroll.js Lenis + gsap ticker 동기, lagSmoothing 0. reduced면 Lenis 미기동(네이티브 스크롤)
  - Section 8종(IA 2절 id와 1:1), ProgressRail, StageBackground 전역 1회
  - content/sections.js 카피 단일 원천. lorem 없음, 팀 확정분은 TODO 카피 4곳
- tokens 타이포 clamp 상한 상향 (단독 커밋 00dd3ed)
  - title 3rem → 4.25rem, display 6rem → 8.5rem. tracking과 leading은 유지
  - 재검증: 4앱 빌드 성공. 제목 1920 이상에서 48 → 68px, arena display 96 → 136px
  - arena MATCH_END ChromeText를 1024와 3840에서 실제로 띄워 확인. 넘침 0, 가로 스크롤 0
  - DESIGN 4절 타이포 표도 같은 값으로 갱신(사양과 코드 불일치 방지)
- P-A A2 섹션 콘텐츠와 등장 연출 (확인 대기)
  - 확정 카피 4곳 문자 그대로 반영(cover 정의와 부제, background 문제 진술, insight 근거, concept 서사)
  - Reveal 컴포넌트가 등장의 유일한 통로. ScrollTrigger once로 재진입 재실행 차단
  - cover만 SplitText 글자 단위 1회. 다른 섹션 글자 분해 0건 실측
  - interactions pin 스크롤 4스텝. reduced motion과 lg 미만은 세로 나열 폴백
  - 자체 제작 SVG 도식 6종. 원본 영상과 사진 미사용
  - WorkflowRow, 산출물 3종 카드, DemoCta. VITE_ARENA_URL 미설정은 콘솔 경고만
  - 검증: 8섹션 렌더, 재진입 opacity 1.00 유지, 등장 역행 프레임 0, 9개 폭 가로 스크롤 0
- P-A A3 스크롤 궤적 라인 (확인 대기)
  - ScrollTrail.jsx. 문서를 관통하는 검끝 곡선 SVG 하나. 콘텐츠 컬럼 왼쪽 여백에 fixed 세로 띠
  - 띠 폭과 위치를 CSS min과 max로만 계산해 리사이즈에서도 레이아웃을 읽지 않는다
  - 리듬: 마디마다 완만한 준비 곡선 뒤 짧고 급한 찌르기 직선. 마디 수는 섹션 수와 같다
  - 마디 경계를 섹션 경계에 정확히 맞춘다. 경계 진행률과 path 길이 앵커 테이블을 refresh에서 만들고
    스크롤 중에는 산술 보간만 한다. 실측 오차 0.0(viewBox 800 기준)
  - 색: 상단 크롬, 76~100퍼센트 구간에서 red.light로 전이. demo 섹션에서 레드로 완성
  - reduced motion에서 전 구간 dashoffset 0(정적 완성), 스크럽 비활성
  - 검증: 스크럽 단조 감소, 9개 폭 본문 겹침 0과 레일 겹침 0과 화면 밖 0,
    non-scaling-stroke로 3840에서도 stroke 2px 유지, 스크롤 중 60fps에 30미만 프레임 0
- P-A 강화 패스 (라이브러리 전수조사 반영, 확인 대기)
  - docs/LIBRARIES.md에 조사 원문 배치. 상단에 채택 판정표 삽입. 원문 sha 삽입 전후 동일(무수정)
  - 루트 CREDITS.md 신설. 지금까지 쓴 것 소급 기록. Tubes Cursor와 liquidGL은 대기 항목으로 명시
  - Preloader 자체 구현. 검끝 곡선 stroke-dashoffset 그리기, 폰트 준비 시 이탈, 상한 2.5초
  - lib/boot.js 부팅 신호. cover SplitText가 프리로더 이탈 뒤에 시작한다(실측 순서 확인)
  - magnetic-elements(MIT 1.1.0) demo CTA 한 곳. 래퍼로 감싸 press scale(0.97)과 공존
  - PATTERNS 12절에 한글 분해 규칙 추가. cover 워드마크만 char, 자소 분리 0건 실측
  - 검증: A1~A3 무결(8섹션, 레일 8점, ScrollTrail 단조 감소, 등장 1회성),
    9개 폭 가로 스크롤 0과 trail 겹침 0, 라이브러리 로드 차단 시 버튼 정상 동작
- P-C 승격 V0~V3 설계 확정
  - LIBRARIES 판정표 arena 행을 승격 확정으로 갱신, DESIGN 9절 1인칭 재작성, COMPONENTS GameCanvas 인터페이스화
  - V1 경계 보고: 재활용 966줄, 인터페이스화 641줄. 검끝 좌표는 로직이 참조하지 않아 렌더러 소유 확정
  - ARENA_SCENE.md: 카메라, 프리셋 4자세, 가시성 증명(8자세 전부 프러스텀 안), fx 계약, 폴백 3경로
  - d 매핑 정정: judge.js가 d를 근접도로 정의하므로 dist(d) = 6.0 - (d/100)*4.4
  - ARENA_INPUT.md: 이산과 연속 채널 분리, setSwordPose 판별 유니온, 결정성 세 고리 봉쇄
- P-C 승격 V4a 1인칭 씬 골격 (확인 대기)
  - three 0.185.1(MIT), postprocessing 6.39.4(Zlib), ribbon-geometry 0.1.0(MIT) 도입. CREDITS 기록
  - three-screenshake는 npm 404. 셰이크는 trauma 방식 로직 포팅으로 확정
  - render/ 신설. ThreeRenderer 기본, Canvas2dRenderer 폴백. 기존 renderer/를 render/canvas2d/로 이동(git mv)
  - game/pose.js 연속 채널 신설. engine이 import하지 않는다
  - GameCanvas render 콜백 통합. setTimeScale과 drainFx는 GameCanvas 잔류
  - 검증: 셀프테스트 6/6, **기준 서명 완전 일치**, 2D와 three 양 경로 5점 완주,
    폴백 3경로(URL, WebGL 생성 실패, 컨텍스트 손실) 전부 2D 전환과 경기 지속 확인
  - 검끝 화면 좌표 실측이 설계값과 일치(휴식 67%/61% 대 67.9%/65.0%, 가드 57%/19% 대 57.9%/23.0%)
- P-C 승격 V4b 궤적 리본과 블룸 (확인 대기)
  - 궤적 리본 2개 자체 구현. 사전 할당 BufferGeometry 제자리 갱신, 정점 색 itemSize 4로 알파까지 전달
  - ribbon-geometry 미채택. 생성자 전용이라 in-place 갱신이 없고 폭이 단일 상수다.
    매 프레임 지오메트리 재생성은 파티클을 풀링한 것과 같은 이유로 피했다. 의존성 제거, CREDITS 정정
  - 나이별 폭과 알파 감쇠, 명중 시 최근 18구간 흰 코어 고정. 리본은 카메라를 향해 폭 방향을 잡는다
  - postprocessing 컴포저 도입. RenderPass → EffectPass(Bloom). render(delta)로 delta 전달 구조 확정
  - **Bloom 초기값 확정(ARENA_SCENE 15절 미해결 해소): threshold 0.42, smoothing 0.28,
    intensity 1.15, radius 0.62, resolutionScale 0.5, kernelSize MEDIUM**
  - 실측: 하늘 영역 평균 휘도 6.4/255로 배경이 뜨지 않는다. 궤적 최대 176/255로 발광 확인
  - 셀프테스트 6/6, 기준 서명 일치 유지

- P-C 승격 V4c 확장 (승격 종료, 확인 대기)
  - 상대 빌보드: 512x1024 오프스크린 5포즈(대기 전진 텔레그래프 런지 피격) 생성 1회 후 재사용.
    평면 2장이 텍스처 공유, 크로스페이드 120ms, MeshBasicMaterial, renderOrder 빌보드 1 리본 2
  - **림 밝기 실측 정정.** 알파 합성이 sRGB가 아니라 선형 버퍼에서 일어난다. 흰 림은 알파가 곧 선형 휘도라
    0.52는 선형 0.58로 threshold 0.42를 넘어 상대 전체가 블룸에 걸리고 있었다(다리 구간 192/255).
    알파 0.26 + 글로우 0.10으로 내려 159/255(선형 0.347)로 확정. 대신 림 두께를 3 → 5px로 키웠다
  - **정규 좌표 비등방 함정.** 512x1024라 x의 0.1은 51px, y의 0.1은 102px다.
    머리 반지름을 x 0.082 y 0.073으로 주었더니 84x150px 달걀이 나왔다. x 반지름을 y의 두 배로 잡아야 원이다
  - 거리 매핑 상수 재조정: dist(d) = 4.4 - (d/100)*3.1. 유효 범위 d 35~55에서 화면 높이 40.9~50.3퍼센트.
    d 52 실측 48.3퍼센트 대 계산 48.7퍼센트(림 글로우 두께분 0.4퍼센트포인트)
  - AI 검끝을 리본 실소스로 연결. 샘플링은 aiMode가 IDLE이 아니거나 aiLunge가 살아 있는 구간만.
    끊겼다 이어질 때 리본을 비워 화면을 가로지르는 직선을 막았다
  - FEINT와 REAL 구분: 1차는 형태(REAL 검끝이 어깨 위 y 1.76까지 크게, FEINT는 가드 근처 y 1.26에서 짧게),
    2차는 림 색조. 텔레그래프 텍스처는 한 장 공유라 총 5장을 지켰다
  - 피격 틴트: hitOwner가 ME면 득점자가 나이므로 **맞은 쪽이 상대다.** 프롬프트의 (hitOwner가 AI)는 반대라 정정.
    엔진 단독 구동 3회 전부 owner ME, 틴트 120ms(실측 133ms, 스텝 경계 포함), 피격 포즈 633ms
  - **리본 흰 코어 영구 잔존 버그 수정(V4b 회귀).** 해제 조건이 없어 명중 구간이 화면에 박혀 있었다.
    JUDGE 800ms 뒤 보통 구간처럼 사라진다. 코어 폭 1.9 → 1.3배, 구간 18 → 12개(포화 + 블룸으로 과대해 보였다)
  - XR 글라스 팔각형 프레임 GlassFrame.jsx. 뷰포트 실픽셀 SVG + ResizeObserver, 컷 clamp(48px, 6vw, 120px).
    딤(evenodd) / 크롬 스트로크(steel 그라디언트, non-scaling-stroke) / red.light 코너 액센트 3겹.
    CSS filter와 blur 0, 등장 페이드 300ms 1회, reduced에서 생략
  - HUD 5요소를 frameInset = 컷 + 12px 하나로 통합. 게이지를 하단 변 중앙으로 이동.
    JudgeText만 중앙 유지(800ms 전용이라 상주 정보가 아니다). 미터 켜지면 하단 행 62px 상향
  - **잔상을 SavePass + TextureEffect로 세웠다.** postprocessing에 AfterimagePass가 없다(v6.39 전수 확인).
    three examples/jsm의 것은 다른 컴포저의 Pass라 안 꽂힌다. damp 평시 0.82 → **0.70**
    (60fps 18프레임 뒤 0.0016으로 소멸. 0.82는 0.47초를 끌었다). 팽창 0.92는 설계값 유지
  - ?fps=1 미터 신설. dev이거나 파라미터가 있으면 뜨고 **프로덕션 빌드에서도 동작 확인**.
    현재 / 60초 평균 / 최저(1초 버킷 최소, 순간 역수 아님) / cpu / draw / tex / seg. 갱신 초당 2회
  - 검증: 셀프테스트 6종 PASS, **기준 서명 일치**, 5점 완주 2D와 three 양 경로, 다시 재개 OK, 콘솔 에러 0
  - 대리 지표: 드로우콜 28 → **31(+3, 상한 10)**, 텍스처 33 → **38(정확히 +5)**, 업로드 9.9MB 증가
  - 프레임 좌표 대조: 7개 뷰포트에서 컷 침범 0, HUD 겹침 0, 가로 스크롤 0.
    6절 검 8좌표 최소 여유 179px(1024x576) ~ 780px(4K)
  - judge.js machine.js opponents.js 무변경 유지. HEX 0, r3f 0, 이모지 0, localStorage 0, TS 0

- P-C 검 모델 교체 (확인 대기)
  - Antique Rapier Sword(GabrielUCG, CC-BY-4.0) glTF로 박스 검 교체. 메시 6개, 머티리얼 1개 공유
  - **위치는 public이어야 한다.** scene.gltf가 .bin과 텍스처를 상대 경로 문자열로 참조해
    src/assets에 두면 Vite가 .gltf만 해시 URL로 번들하고 나머지가 404가 난다.
    에셋이 미추적 상태였으므로 이동과 경량화를 먼저 끝내고 경량본만 커밋했다(4K 원본은 이력에 없다)
  - 텍스처 3장 4096 → 1024 리사이즈. **36.0MB → 3.8MB(89.5퍼센트 절감).** 파일명 유지라 gltf 무수정.
    PNG 유지(normal을 JPEG로 바꾸면 압축 아티팩트로 법선이 깨진다). KTX2는 보류
  - 정규화는 바깥 컨테이너 그룹에만. 내부 노드를 만지지 않는다(에셋 교체 시 재작업 방지).
    Box3로 장축 판별 후 -z 정렬, 손잡이 중심을 원점으로, 칼끝이 0.68m가 되게 uniform scale 0.758
  - **칼끝 화면 좌표 67.2% / 61.7%.** V4a 박스 검 실측 67% / 61%과 같다. 6절 궤적 좌표가 그대로 산다
  - 칼날(Blade_low)만 머티리얼 clone 후 emissive red.light, intensity 0.35.
    텍스처는 참조 공유라 추가 GPU 업로드 없음. 손잡이와 가드는 원본 금속 텍스처 유지
  - 칼날 휘도 실측 = 0.283 + 0.748 x intensity. 0.283은 금속 반사분.
    0.35에서 0.592로 threshold 0.42를 넘지만 의도한 것이다. 검신과 궤적이 하나의 붉은 선으로 이어져야 하고
    번지면 안 되는 것은 검 전체다. 손잡이는 emissive 0이라 번지지 않는다
  - frustumCulled = false 전 메시(1인칭 무기 표준 처리). 환경맵은 V4a PMREM 재사용
  - 로드는 모듈 캐시로 1회. 실패 시 박스 검 유지 + 콘솔 경고(실측 확인: draw 31 tex 25로 폴백)
  - 검증: 셀프테스트 6종, 기준 서명 일치, 5점 완주 2D와 three 양 경로, dev와 프로덕션 빌드 양쪽 로드 성공
  - 대리 지표: 드로우콜 31 → **33(+2. 레이피어 메시 6개 - 박스 검 4개)**, 텍스처 25 → **28(+3)**
  - CREDITS.md에 license.txt 크레딧 문구 전문 기록

- P-C 게임필 개편 D0 색 체계 개정 (확인 대기)
  - **상시 red emissive 폐지.** 칼날에 레드를 늘 얹으면 레드가 배경색이 되어 득점 순간이 묻힌다.
    DESIGN 2절에 "검과 레드의 관계" 개정을 신설하고 ARENA_SCENE 5절을 다시 썼다
  - finish 두 종 신설: chrome(내 검)과 antique(상대 검, D2에서 사용).
    chrome은 baseColor 맵을 떼고 steel.mid로 덮어 청동 톤을 지운다. metalness 0.97, roughness 0.16.
    **노멀맵과 거칠기 맵은 유지**해 표면 디테일과 마모가 산다. 맵을 유지한 채로는 픽셀별 색을 못 바꾼다
  - 머티리얼을 둘로 갈랐다. 칼날 1장 + 나머지 5메시 공유 1장. 전에는 칼날만 clone이라 힐트가 원본이었다
  - 득점 플레어: fx HIT/RIPOSTE가 owner ME로 들어오면 emissive가 steel.mid → red.light로 옮겨가고
    세기가 0.06 + 1.8t^2로 솟았다 dtRender/0.8로 깎여 JUDGE 800ms 안에 죽는다
  - 박스 검(폴백) 검끝 마커도 red.light → steel.mid. 평시 화면에서 레드를 전부 걷었다
  - 검증: **평시 검 영역 붉은 픽셀 0**(득점 전, 감쇠 후 모두 0). 득점 순간에만 붉은 픽셀 146 실측
    (플레어가 800ms라 헤드리스 캡처로는 못 잡아 감쇠를 8초로 늘려 촬영 후 원복했다)
  - 셀프테스트 6종, **기존 기준 서명 유지**(렌더만 바뀌었다), 5점 완주 2D와 three 양 경로, 에러 0

- P-C 게임필 개편 D1 버그 셋 (확인 대기)
  - **배경 잔상 고스트 = 8비트 양자화.** 원인을 특정했다. `EffectComposer` 기본 프레임 버퍼가 8비트라
    잔상 되먹임 out = current x 0.3 + prev x 0.7에서 prev가 1/255일 때 0.7이 반올림으로 다시 1이 된다.
    **고정점이 0이 아니라 1/255이라 영원히 안 죽는다.** 검을 한 번 휘두르면 그 자리에 유령이 박히고
    25초 뒤에도 밝기가 같았다. damp를 낮춰도 소용없다
  - 수정: `frameBufferType: HalfFloatType`. 실측 잔존 픽셀 **0**(가드 12초 뒤, 리사이즈 왕복 뒤 모두).
    리사이즈 클리어나 phase 전환 클리어 코드는 넣지 않았다. 근본 원인이 없어져 필요가 없다
  - 전환 후 재검증: 하늘 평균 7.08/255로 배경 안 뜸, 상대 림 선형 0.352로 문턱 아래 유지
  - **찌르기 히치는 이 환경에서 계측 불가.** 헤드리스 중앙 프레임이 158ms라 수 ms 히치가 분해되지 않는다.
    대신 렌더 경로에서 히치를 만드는 지점을 코드로 특정해 셋 다 제거했다
    1. 포즈 텍스처 첫 사용 업로드. 512x1024 다섯 장을 `renderer.initTexture`로 개막 전에 올린다.
       **명중 포즈는 하필 찌르기가 꽂히는 순간에 처음 뜬다**
    2. 포즈 교체마다 세우던 `material.needsUpdate` 제거. 두 텍스처 모두 존재해 define이 같은데
       세우면 three가 프로그램을 다시 훑는다
    3. `renderer.compile(scene, camera)`를 init과 검 모델 부착 직후에 호출. 첫 등장 프레임 컴파일 제거
    4. `pose.read()`가 매 프레임 새 객체를 만들던 것을 재사용 객체로 교체(초당 60개 쓰레기 제거)
  - **상대 발 잘림 = 텍스처 경계 클리핑.** 발이 v 0.984라 발 스트로크와 림과 글로우가 밖으로 나갔다.
    발을 0.952로 들이고 머리 v 0.021과 발바닥 v 0.969 사이가 정확히 1.9m가 되게 평면을 2.004m로 역산.
    화면 점유율 d 52에서 49.7퍼센트로 설계값 48.7에 더 가까워졌다(전에는 그림이 1.9m보다 짧았다)
  - 접지 그림자 블롭 추가. 씬에 직접 붙이고 z만 따라간다(빌보드 자식으로 넣으면 카메라 따라 일어선다)
  - 검증: 셀프테스트 6종, **기존 기준 서명 유지**, 5점 완주 2D와 three 양 경로, 에러 0.
    대리 지표 드로우콜 33 → 34(그림자 +1), 텍스처 28 → 30

- P-C 게임필 개편 D2 상대 완성 (확인 대기)
  - **실루엣을 흰 유니폼으로 뒤집었다.** 어두운 채움 + 밝은 림에서 밝은 몸체 + 어두운 부위 테두리로.
    2패스(굵게 어둡게 한 번, 유니폼 톤 한 번)라 겹친 팔다리가 갈린다.
    마스크는 어두운 메시 + 밝은 테두리 + 가로 결, 장갑과 신발도 어둡게 얹었다
  - **유니폼 밝기 상한.** 순백은 선형 1.0이라 상대 전체가 블룸에 번진다.
    steel.mid를 bg.deep으로 0.30 눌러 실측 167/255(선형 0.386)로 문턱 0.42 아래에 앉혔다
  - 텍스처에서 검을 뺐다. 3D 골동 레이피어가 손 앵커에 따로 붙는다(finish 'antique')
  - 손 앵커는 텍스처 좌표에서 역산. 포즈마다 위치와 겨냥이 바뀌고 겨냥 방향으로 검이 돈다
  - **겨냥을 카메라 정면으로 두면 검이 극단적으로 단축돼 점으로 보인다(실측).**
    대기와 예고는 비스듬히 세우고 런지만 카메라 쪽 성분을 키웠다
  - antique 마감을 metalness 0.90 / roughness 0.30으로 올렸다. 원본 값으로는 블랙 무대에서
    빛을 못 받아 검이 통째로 사라진다. 텍스처는 그대로라 청동 결은 유지된다
  - AI 리본 소스를 이 검의 tipAnchor로 옮겼다. 가상 포인트는 로드 실패 폴백으로만 남는다
  - **순간이동 가드 신설(리본 공통).** 겨냥이 크게 돌 때 두 점이 벌어져 화면을 가로지르는
    거대한 띠가 그려졌다(실측 24,286픽셀). max(0.35m, 12 m/s x dt)를 넘으면 이력을 버린다.
    고정 거리로 두면 저프레임 환경에서 정상 큰 걸음까지 잘려 리본이 사라진다(이것도 실측으로 확인)
  - docs/OPPONENT_IMAGE_SPEC.md 산출. 5포즈 자세 서술, 공통 조건, 밝기 상한 근거,
    영문 생성 프롬프트, setPoses 교체 절차와 검수 항목
  - 검증: 셀프테스트 6종, **기존 기준 서명 유지**, 5점 완주 2D와 three 양 경로, 에러 0.
    대리 지표 드로우콜 34 → 40(상대 검 메시 6개), 텍스처 30 → 31
  - **미검증 하나.** 상대 공격 시 파란 리본이 검끝에서 나오는 그림은 헤드리스에서 확인하지 못했다.
    7fps에서 AI 공격 구간이 한 프레임이라 리본이 2점을 못 모은다. 실기 확인이 필요하다

- P-C 게임필 개편 D3 AI 다양화와 런지 (확인 대기)
  - **로직 3파일 중 judge.js와 opponents.js를 처음 수정했다.** machine.js는 무변경 유지
  - 유파 파라미터 넷 신설. 전부 시드 난수 경유라 결정성은 그대로다
    - tempoBands + tempoShiftEvery: 몇 합마다 공격 간격 분포를 갈아탄다. 같은 리듬은 읽힌다
    - comboChance + comboGapMs: 회복 직후 두 번째 공격
    - stepFeintChance: 대치 중 앞으로 훅 들어왔다 빠지는 거리 흔들기
    - ripostePressure: 패리당해 창이 열리면 즉시 되받아치려 든다
  - **유파 차이 실측.** 평균 공격 간격 이탈리아 세이버 1778~2077ms 대 프랑스 에페 3248~3748ms.
    이탈리아 최소 간격 1083ms(콤보가 실제로 붙는다), 프랑스는 거리 흔들기와 되받아치기 성향
  - 런지: 전진 홀드 + 스페이스. **새 키를 만들지 않고 이산 채널을 그대로 쓴다**(ARENA_INPUT 유지).
    유효 범위 35~55 → 32~55, 쿨다운 350 → 500ms. 가까운 쪽 상한은 안 넓혔다.
    **진입 조건만 다르고 4분기 판정은 손대지 않았다**
  - 쿨다운을 state.thrustCooldownMs로 옮겼다. 직전 찌르기가 건 길이로 재야 런지 뒤가 늦어진다
  - 렌더: SWORD_POSES.lungeDeep 신설. 읽기 필드에 meLungeDeep 추가(9개 → **10개**, ARENA_SCENE 표 갱신)
  - 셀프테스트 6종 → **8종**(런지 유효 범위 확장, 런지 쿨다운). 8/8 통과
  - **기준 서명 갱신 절차 수행.** 변경 전 그린 확인 → 변경 → 같은 시드 2회 동일 확인 → 새 서명 채택.
    새 기준: 길이 244, 해시 8c2f910, 꼬리 SCORE:6-2:53.633#6-2#ME. judge.selftest.js의 BASELINE이 원본
  - **이전 기록의 결함을 발견했다.** 기준 서명을 앞 80자만 남겨 두었는데 그 구간은 D3 전후가 똑같았다.
    대본 앞부분은 AI가 갈리기 전이라 변경이 뒤쪽에서만 드러난다. 접두만 보는 가드는 회귀를 놓친다.
    그래서 길이와 해시와 꼬리를 함께 박고 selftest가 매번 대조하게 했다
  - REAL 예고 색조를 0.35 → 0.22로 낮췄다. D2에서 몸체가 밝아지며 색 배수가 몸 전체에 걸려
    상대가 분홍으로 읽혔다. 1차 단서는 칼 각도(형태)이고 색조는 보조다
  - 검증: 셀프테스트 8/8, 같은 시드 2회 동일, 5점 완주 2D와 three 양 경로, 에러 0.
    런지 스모크: d 63에서 "거리 밖"으로 정확히 걸린다(가까운 쪽 상한 미확장 확인)

- P-C 게임필 개편 D4 막기 시각화 (확인 대기)
  - 가드 프리셋 강화. 칼날이 화면 상단을 **가로지르는** 방어선이 됐다.
    이전 값은 세로로 서 있어 "올린 검"이지 "받아내는 선"으로 읽히지 않았다.
    실투영 그립 (79.2%, 52.7%) → 칼끝 (43.6%, 24.1%)
  - 패리 스파크 신설. Points 하나에 particleMax(300)를 미리 잡고 풀링. 드로우콜 +1.
    **흰색에서 steel로 식는다. red를 쓰지 않는다**(레드는 득점 전용).
    알파 대신 색을 어둡게 해 가산 블렌딩에서 사라지므로 커스텀 셰이더가 없다
  - 상대 검 knockBack. 패리당하면 겨냥점이 260ms 밀렸다 되돌아온다
  - **FUI 층 신설(FuiLayer.jsx).** D5 명중 마커와 데미지 표시가 같은 통로를 쓴다.
    렌더러가 setFxObserver로 투영 좌표만 넘기고 그리는 것은 DOM이 한다.
    리포스트 링은 팔각 미니 링이 600ms 동안 줄어드는 형태다(줄어드는 팔각형이 곧 남은 시간).
    transform과 opacity만 애니메이션하고 reduced에서는 축소를 끈다
  - **투영 경로를 두 번 갈아엎었다.** 두 검끝의 중점은 두 검이 2m 넘게 떨어져 있어
    카메라 코앞으로 떨어졌고(실측 y 2082px), 월드 좌표 project 경로도 패리 순간 화면 밖으로 나갔다
    (실측 y 1880px). **카메라 로컬 좌표를 6절 투영식으로 직접 푸는 방식**으로 확정했다.
    검이 카메라의 자식이라 프리셋 좌표가 곧 카메라 공간이고 문서 좌표와 대조가 성립한다
  - 검증: 되찌르기 링 육안 포착, 셀프테스트 8종, 기준 서명 유지, 5점 완주 2D와 three 양 경로, 에러 0.
    대리 지표 드로우콜 40 → 41(스파크 +1)
  - **미확인.** 월드 좌표 project가 패리 순간에만 어긋난 이유는 끝까지 특정하지 못했다.
    같은 값을 쓰는 궤적 리본은 화면에서 정상으로 보인다. 카메라 로컬 경로로 우회했고
    D5에서 명중 지점 투영이 필요하므로 그때 다시 봐야 한다

- P-C D5-pre 월드 투영 갱신 타이밍 (확인 대기)
  - **가설 기각.** matrixWorld 갱신 타이밍이 아니었다. project 직전에 camera.updateMatrixWorld(true)와
    tipAnchor.updateWorldMatrix(true, false)를 강제해도 좌표가 **한 자리도 안 변했다**(before === after)
  - **진짜 원인: 검끝 앵커 자체가 틀려 있었다.** swordModel에서 `blade.worldToLocal(tip)`을
    **모델을 카메라 밑에 붙인 뒤에** 불렀다. worldToLocal은 그 시점 matrixWorld를 쓰는데
    그때는 전혀 다른 공간이라 앵커 로컬이 (-0.104, -2.652, 1.293)로 앉았다(올바른 값 (0, 0, -0.822))
  - 곁가지 발견: **로더가 이 모델에 -90도 X 회전을 얹는다.** glTF JSON 노드에는 회전이 없는데
    로드 후 칼날 장축이 -y로 온다. measure()가 축을 Box3로 판별하게 짜 둔 것이 여기서 값을 했다
  - 수정: measure() 안에서 아직 독립 상태일 때 역행렬(toBladeLocal)을 떠 두고 붙인 뒤에는 그것만 쓴다
  - **1:1 검증.** 앵커 로컬 (-0.0001, 0, -0.8221). 월드 좌표가 해석값과 소수 3자리까지 일치하고
    화면 좌표가 문서값과 같다. 휴식 67.2/61.7, 가드 43.6/24.1
  - D4의 카메라 로컬 우회로를 걷고 **월드 투영 한 경로로 통일**했다.
    D5 명중 지점은 상대 빌보드 위라 카메라 로컬로 안 풀리므로 두 경로를 둘 이유가 없다
  - **이 앵커는 검 모델 도입(9405f0d) 이후 줄곧 틀려 있었다.** 궤적이 칼끝이 아닌 곳에서 나오고 있었고
    검과 함께 움직여 그럴듯해 보였을 뿐이다. 이제 진짜 칼끝에서 나온다
  - 검증: 되찌르기 링 위치 유지, 셀프테스트 8종, D3 기준 서명 유지, 5점 완주 2D와 three 양 경로

- P-C D5 FUI 히트 마커와 명중 연출 (개편 D0~D5 전체 완료, 확인 대기)
  - 명중 마커: 팔각 레티클 + 과녁 링 2겹 + red 꺾쇠 4개 + 부위 라벨 + 점수 팝업.
    D4 되찌르기 링과 같은 시각 문법이고 같은 setFxObserver 통로를 쓴다.
    실측 포착: "+1 투구"가 상대 머리 위에 정확히 섰다(D5-pre 투영 수정 덕이다)
  - 데미지 인디케이터: 팔각 프레임의 피격 방향 변이 red.light로 점등 + "피격 몸통" 라벨.
    **파란 연출 없음.** 경고의 레드라 색 규칙에 맞는다. normal과 reduced 양쪽 포착
  - 부위 근사: y 1.55 초과 투구, 0.95 초과 몸통, 그 아래 팔다리
  - 명중 연출: hitstop 70ms(GameCanvas가 loop.setTimeScale(0)을 걸었다 푼다. 렌더러는 시계를 안 만진다),
    ShockWave(명중 월드 좌표 epicenter, delta 전달), 색수차 250ms sin 반주기, 카메라 셰이크(trauma 포팅)
  - **EffectPass 병합이 안 되는 조합을 실측으로 만났다.**
    "Effects that transform UVs are incompatible with convolution effects (ShockWaveEffect)".
    Bloom은 컨볼루션이고 ShockWave와 색수차는 UV를 옮긴다. 합성 순서는 두고 패스를 셋으로 갈랐다
  - **그 예외가 던져졌을 때 폴백이 정상 동작해 2D로 경기가 이어졌다.** 검은 화면이 아니라
    호환 렌더로 내려앉는 것을 실제 사고로 확인한 셈이다
  - reduced motion: 셰이크와 ShockWave와 색수차를 끄고 hitstop과 흰 코어와 마커(페이드만)는 남긴다
  - 검증: 셀프테스트 8종, **D3 기준 서명 유지**(hitstop이 판정에 영향 없음을 증명한다),
    5점 완주 2D와 three 양 경로, 에러 0. 대리 지표 드로우콜 41 → 43(EffectPass +2), 텍스처 31

- P-C arena 마무리 E1 상대 포즈 전환 과장 (확인 대기)
  - 증상: 상대 검은 날아오는데 몸은 앙가르드로 굳어 "검만 따로 논다". 원인은 포즈 간 실루엣 차이가 작은 것
  - 5포즈 골격을 전부 다시 잡았다. 런지는 대기 대비 **머리가 텍스처 0.205(약 0.41m) 떨어지고
    발 간격이 0.49 → 0.844로 벌어진다.** 앞무릎 깊게 접힘, 뒷다리 펴짐, 검 팔 완전히 뻗음
  - 텔레그래프는 상체를 젖히고 검 어깨를 확실히 올린다. 전진은 앞발이 확실히 뜬다. 피격은 균형이 무너진다
  - **크로스페이드를 방향에 따라 갈랐다.** 공격 진입 55ms, 회복 220ms.
    같은 값이면 찌르기가 미끄러지듯 나가 순간성이 죽는다
  - 3D 검 손 추종도 같은 fadeSec을 쓴다. 몸과 검이 같은 프레임에 뻗는다
  - 손 앵커를 새 포즈의 텍스처 좌표에서 다시 역산. 런지 겨냥을 카메라 쪽으로 더 뺐다
  - 검증: 셀프테스트 8종, D3 기준 서명 유지, 5점 완주 2D와 three 양 경로, 에러 0, 4앱 빌드
  - **미확인.** 런지 프레임을 헤드리스에서 사진으로 못 잡았다. 7fps에서 캡처 한 장이 2초인데
    런지는 수백 ms라 표본 14장에서 한 번도 안 걸렸다. **실기에서 육안 확인이 필요하다**

- P-C arena 마무리 E2 실루엣 부위 구분 (확인 대기)
  - 증상: 단색 회색 채움 + 흰 테두리라 팔 다리 몸통이 뭉쳐 보였다
  - **부위 단위로 쪼개 그린다.** drawFigure 하나를 drawPart로 나누고
    부위마다 어두운 테두리를 두른 뒤 채운다. 그리는 순서가 곧 앞뒤다
    (뒤쪽 팔다리 → 몸통 → 목 → 머리 → 앞쪽 팔다리)
  - **2톤 명암.** 뒤쪽 팔다리를 SHADE_DARKEN 0.52로 더 눌러 sRGB 약 0.42,
    앞쪽과 몸통은 0.30으로 약 0.60. 평면이 아니라 몸으로 읽힌다
  - 검 든 팔 구분: 장갑을 키우고 소매 커프 한 줄을 넣었다. 어느 팔이 검을 쥐었는지가 읽힌다
  - 마스크 메시 결과 비브는 그대로 유지
  - **밝기 상한 유지 확인: 167/255 = 선형 0.386 < 0.42.** 블룸에 안 걸린다
  - OPPONENT_IMAGE_SPEC에 "부위 구분이 명확한 유니폼 음영"을 공통 조건으로 추가하고
    검수 항목에 "실루엣만 잘라 봐도 팔 다리 몸통이 갈리는가"를 넣었다.
    **E2는 임시 실루엣의 개선이지 근본 해결이 아니라는 것을 사양서에 명시했다**
  - 검증: 셀프테스트 8종, D3 기준 서명 유지, 5점 완주 2D와 three 양 경로, 에러 0, 4앱 빌드

- P-C arena 마무리 E4 상대 풋워크 생동감과 착탄 다양화 (확인 대기)
  - **판정 파일 3개 무변경.** 변경은 `render/ThreeRenderer.js`와 `render/three/opponent.js` 둘뿐이다.
    전부 시각 전용이고 착수 전 그린(8/8, 서명 일치)을 먼저 찍어 두고 시작했다
  - **잔움직임.** 좌우 잔발 진폭 0.045m(실루엣 폭 1.002m의 4.5퍼센트) + 호흡 0.6퍼센트 압축
    + 저빈도 큰 숙임 5.5퍼센트를 0.85초 동안, 간격 3.4~6.8초.
    **시간 기반 저주파 노이즈**(주기가 서로 나누어떨어지지 않는 사인 셋)다. 프레임 랜덤 지터를 쓰지 않았다.
    실측 프레임당 최대 이동 **1.02mm로 리본 순간이동 가드 350mm의 1/344**. 궤적을 건드리지 않는다
  - **세로는 위치가 아니라 압축이다.** 그룹을 내리면 발이 바닥면 밑으로 들어가 잘린다
    (D1에서 고친 "잘린 그루터기"가 그대로 재발한다. 지오메트리가 발바닥을 y 0에 맞춰 놓았고
    바닥이 불투명이라 깊이가 발을 먹는다). 실제 펜서도 무게중심을 낮출 때 발은 지면에 붙어 있다
  - **압축을 그룹이 아니라 빌보드 평면에만 걸었다.** 처음에 그룹에 걸었더니 손에 붙은 3D 검까지
    비균등 스케일을 받았다. 게다가 부모 비균등 스케일과 자식 회전이 겹치면 전단이 생겨 칼이 휜다.
    손 앵커와 겨냥점은 압축 비율만큼 y를 곱해 몸을 따라 내려온다. 접지 그림자는 x를 따라간다
  - **예고 진입 순간 시간 자체를 세운다.** 0으로 되돌리지 않고 그 자리에서 얼린다.
    실측 예고 15프레임 변위 폭 **0.0000mm**, 진입 팝 0.0000mm, 재개 팝 0.16mm. 양쪽 다 팝이 없다
  - 화면 점유율은 압축을 먹어도 d 45에서 42.4~44.9퍼센트로 4절 요구(40~55)를 안 벗어난다
  - **내 착탄.** 상대 중심 x 고정을 폐지하고 **그립에서 칼끝으로 뻗은 직선을 상대 평면까지 늘린다.**
    실측 200회에서 x -0.418~+0.438m, 평균 -0.015m, 좌우 끝에 물린 것 0건, 부위 투구 131 / 몸통 69.
    **E4 이전에는 x가 항상 0이고 부위가 항상 투구 하나였다**(찌르기 프리셋이 고정이라 높이도 고정이었다)
  - **이 투영식은 임시가 아니라 미래 코드다.** C3에서 폰 쿼터니언이 검 그룹 회전을 쥐면
    그 방향이 그대로 들어와 **코드 교체 없이 진짜 조준이 된다.**
    임시인 것은 키보드 변주(`AIM_JITTER_X` / `AIM_JITTER_Y`) 하나뿐이고 C3에서 이 상수만 지우면 된다.
    소스 주석에 같은 내용을 박아 두었다
  - 변주 중앙값이 0이 아닌 이유: 찌르기 프리셋 칼끝이 그립보다 왼쪽이라 그대로 투영하면
    착탄이 늘 몸 왼쪽에 몰린다(실측 -0.26m, 몸 반폭 0.50m라 절반이 한쪽 끝에서 잘린다).
    y를 아래로 치우친 것은 투구 문턱 1.55m를 실제로 넘나들게 하기 위해서다
  - **상대 표적 다양화.** 예고 진입 순간 머리/몸통/옆구리에서 하나를 뽑는다.
    예고 자세가 이미 그쪽을 겨누므로 어디로 오는지가 형태로 미리 읽힌다. 검 겨냥과 파란 리본이 따라간다.
    유파 성향 실측 300회: 세이버 머리 150 / 몸통 81 / 옆구리 69, 에페 몸통 150 / 옆구리 81 / 머리 69
  - **`school`을 읽기 필드 표에 정식으로 올렸다(10개 → 11개).** `aiKind`와 같은 시각 스타일 전용 등급이고
    타이밍과 판정 분기 사용을 금지한다고 ARENA_SCENE 1절에 명시했다.
    D3의 `meLungeDeep`과 같은 절차다. 표에 없는 필드를 조용히 읽지 않는다
  - **상대 가드 2종.** 패리 받는 선이 상단과 하단으로 갈리고 검이 그 방향으로 튕겨난다.
    가로도 ±0.08m 안에서 흩는다. 실브라우저 실측에서 되찌르기 링이 화면 y **144px과 305px
    두 군집으로 정확히 갈렸다**(내 가드 프리셋이 고정이라 E4 이전에는 한 점에 박혀 있었다)
  - **시각 시드는 engine의 rng와 완전히 분리했다.** 그 수열을 한 번이라도 당겨 쓰면 판정이 통째로 달라진다.
    sparks.js와 같은 렌더 전용 LCG를 렌더러와 상대가 각자 하나씩 쥔다
  - 검증: 셀프테스트 8/8, **D3 기준 서명 일치**(길이 244 / 해시 8c2f910 / 꼬리 SCORE:6-2:53.633#6-2#ME),
    4앱 빌드, three와 2D 양 경로 5점 완주, 2D에서 "호환 렌더" 표시 확인, 페이지 에러 0,
    금지 항목 grep 0건(잔여 hit는 전부 주석이거나 2D 파티클의 기존 시각 산포다)
  - 대리 지표 **드로우콜 43 유지, 텍스처 31 유지.** 새 오브젝트를 만들지 않았다
  - **헤드리스로 재지 않은 것 하나.** 5fps에서는 fx가 뜨는 시점의 검 자세가 뭉개져
    착탄 분포 비율이 실기와 다르게 나온다. 실브라우저는 "흩어진다"까지만 증명하고
    비율은 200회 해석값이 기준이다(13절 헤드리스 원칙과 같다)

- P-C 룰 심화 R0 문서 배치 (확인 대기)
  - 사용자 제공 원문을 `docs/FENCING_RULES.md`에 **무수정으로** 배치했다.
    sha256 `e352bdf9b74e...5ec9`, 61행 6068바이트. 커밋 전후 동일을 해시로 확인했다
  - ARENA_SCENE.md 머리에 한 줄: 룰 심화의 사실 근거는 FENCING_RULES.md이고
    **채택 판정표가 본문보다 우선**한다
  - 판정표에서 이 트랙을 묶는 두 줄을 규율로 승격했다.
    **부위별 점수 차등 금지**(실룰에 없고 착탄을 판정에 넣으면 결정성이 무너진다),
    **초록 램프 미도입**(좌우 식별이 본질이고 초록은 디자인 시스템을 깬다. 나 red / AI blue로 이식)
  - 코드 무변경. 셀프테스트와 서명은 손대지 않았다

- P-C 룰 심화 R1 가드 홀드 전용화, 찌르기 우선 (확인 대기)
  - **원인은 가드 홀드가 아니라 분기 순서였다.** `poseSword`가 `if (guard)`를 먼저 봐서
    `else if (lunge > 0)`이 통째로 죽어 있었다. 가드를 문 채 되찌르면 찌르기 자세가 **아예** 안 나온다.
    `judge.js`는 guard on/off를 순간 이벤트로 이미 옳게 처리하므로 고친 곳은 자세 채널뿐이다
  - **우선순위 thrust > guard(홀드 중) > 디폴트를 두 곳에 똑같이 넣었다.**
    `GameCanvas`의 프리셋 결정부와 `ThreeRenderer.poseSword`. 한쪽만 고치면 프리셋과 실제 자세가 어긋난다
  - 가드 자세는 홀드 구간에만. 진입 55ms(반응이라 지연이 보이면 안 된다, MOTION 1절),
    이탈 220ms(E1 회복값 재사용. 공격은 빠르고 회복은 느리다는 규칙이 검에도 걸린다).
    전에는 트윈이 아예 없어 양쪽 다 팝이었다
  - 찌르기 와인드업의 출발을 **지금 있는 자리**로 바꿨다. 가드 중이면 방어선에서 당겨지므로
    되찌르기가 막기에서 이어져 보인다. 뻗기 구간은 와인드업에서 시작하므로 출발과 무관하다
  - **C3 정합**: 쿼터니언은 회전만 덮어쓰고 그립 위치는 이 블렌드가 계속 쥐므로
    폰으로 찌를 때도 같은 우선순위가 유지된다. 주석에 명시
  - 실측(60fps 환산): 가드 홀드 중 찌르기 칼끝이 가드 없이 찌를 때와 **차이 0.0000m**.
    가드만일 때 대비 칼끝 1.125m 이동. 진입 4프레임(67ms) 이탈 14프레임(233ms)으로
    프레임 양자화 안에서 설계값 일치. 이탈 완료 자세가 디폴트와 정확히 일치.
    블렌드 전 구간 6절 안전 부등식 최대 |ndcY| 0.823 / |ndcX| 0.700 (한계 0.92)
  - 실브라우저 육안: 대기(검 우하단) / 가드(칼날이 화면 상단을 가로지름, 칼끝 실측 (545,195) 대
    설계 (558,193)) / **가드 홀드 유지한 채 찌르기(흰 코어 리본이 화면을 가로지르고 "명중 +1 투구")** /
    뗀 뒤 대기 복귀, 네 장면 전부 포착
  - 검증: 셀프테스트 8/8, **D3 기준 서명 유지**(244 / 8c2f910 / 꼬리), 4앱 빌드,
    three와 2D 양 경로 5점 완주, 에러 0, 드로우콜 43과 텍스처 31 유지
  - (R1 시점 잔여였던 2D 폴백 분기 순서는 아래 항목에서 정합시켰다)

- P-C 룰 심화 2D 폴백 가드/찌르기 우선순위 정합 (확인 대기)
  - `canvas2d/fighters.js` 포즈 선택과 `canvas2d/geometry.js` `swordTip`이 가드를 먼저 보고
    `lungeT`를 통째로 무시하고 있었다. three와 같은 **thrust > guard > 디폴트**로 맞췄다
  - **three와 구현 방식이 다르다.** three는 홀드 블렌드를 상태로 들고 있지만 2D는 프레임마다
    계산하는 stateless 경로다. 그래서 찌르기 진행도 `e`로 가드 자세와 찌르기 자세를 섞는다.
    `e`가 0이면 방어선 그대로, 1이면 찌르기 그대로라 **경계값에서 결과 순서가 three와 같다**
  - 검증: 셀프테스트 8/8, D3 기준 서명 유지, 2D 경로 5점 완주, 에러 0.
    육안으로 가드(검 들림) → 가드 홀드 유지한 채 찌르기(전방 뻗음 + 레드 궤적) 전환 포착

- P-C 룰 심화 R2 히트 이펙트 게이밍 증폭 (E3, 확인 대기)
  - D5 팔각 레티클은 그대로 두고 사건성만 얹었다. 문법은 프레임과 같다(팔각, steel, red.light)
  - 득점: 흰 임팩트 플래시(150ms) + red.light 확산 링 2겹(r 62 / r 96, 280ms, 바깥이 70ms 늦게)
    + 점수 팝업 pop(scale 0.9 → 1.0). 부위 라벨 유지
  - 피격: 가장자리 비네트 펄스(진입 90ms 이탈 380ms) + 맞은 자리 red 팔각 링과 확산 링 1겹
    + D5 프레임 세그먼트. **파란 연출 없음.** 경고의 레드다
  - **확산 링은 DOM으로 갔다.** 캔버스로 옮기면 패스와 드로우콜이 늘고 후처리 순서까지 얽힌다.
    DOM은 transform과 opacity만 만져 합성만 돈다. 실측 **드로우콜 43, 텍스처 31로 증가 0**
  - **r을 애니메이션하지 않는다.** SVG 속성을 흔들면 매 프레임 도형을 다시 그린다.
    최종 크기로 한 번 그려 두고 래퍼 scale만 키운다
  - **비네트 시작점을 42 → 58퍼센트로 죘다.** 42에서는 붉은 기가 화면 중앙까지 올라와
    상대와 판정 문구를 덮었다(실측 후 정정). 58에서 가장자리 띠로만 읽힌다
  - reduced: 링 플래시 펄스 전부 끄고 마커와 부위 링과 라벨과 프레임 세그먼트는 유지. 점수는 pop 없이 고정
  - 검증(DOM 계산값 샘플링. 헤드리스 5fps로는 280ms 연출을 사진으로 못 잡아 CSS 값을 직접 읽었다):
    정상 모드 명중 4회에 플래시 4개와 링 8개, 피격 6회에 펄스 6회와 링 6개로 **개수가 정확히 맞는다.**
    링 궤적 scale 0.32 → 1.0, opacity 0.95 → 0. reduced에서 링 0 / 플래시 0 / 펄스 0, 마커는 그대로
  - 셀프테스트 8/8, **D3 기준 서명 유지**, 4앱 빌드, 에러 0

- P-C 룰 심화 R3 피스트 경계, 유파 락아웃, 램프 문법 (서명 갱신, 확인 대기)
  - **새 기준 서명: 길이 205 / 해시 ef4b27d5 / 꼬리 `JUDGE:6-1:51.833#6-1#ME`.**
    직전 D3 서명은 244 / 8c2f910 / `SCORE:6-2:53.633#6-2#ME`였다. 셀프테스트 8종 → **10종**
  - 갱신 절차 그대로 밟았다. 변경 전 그린 확인 → 구현 → 10/10 통과 → **같은 시드 2회 동일**
    → 교차 프로세스 3회까지 동일 확인 → 새 서명 채택
  - **R3-1 피스트.** 위치가 진실이고 d는 두 위치의 간격에서 유도한다.
    시작 나 5.0m / AI 9.0m(간격 4m), 길이 14m, 경고선 각 끝 2m
  - **결정성 보존 제약을 실측으로 증명했다.** `METERS_PER_D = 0.05`가 그 정합을 쥔다.
    d를 먼저 정하고 위치를 유도하는 것이 아니라 **기존 d 변화량을 그대로 받아 미터로 환산해 위치에 싣는다.**
    경계 셋(피스트 양 끝, 교차 최소 간격, d 0/100 포화)에 안 닿는 구간에서
    기준 대본 리듬 6000스텝 최대 오차 **5.3e-13**, 무작위 보행 20만 스텝 최대 오차 **2.6e-12**.
    배정도 한계이므로 항등이다
  - 유효 범위 d 35~55가 간격 3.50~4.50m(폭 1m), 내 이동 속도 **1.3 m/s**로 실제 어드밴스와 같은 자리.
    계속 후퇴하면 3.85초에 후방 경계에 닿는다
  - **R3-2 락아웃.** 세이버 170ms 시뮬타네(무득점), 에페 40ms 더블 투셰(양쪽 +1),
    동점 매치포인트(4-4) 더블은 무효. 창 길이와 더블 인정 여부를 호출자가 넘겨 **judge는 유파를 모른다**
  - **구조적 결함 하나를 잡았다.** 맞불(상대 공격)이 phase를 JUDGE로 세우고 있어
    상대 공격이 JUDGE 800ms + SCORE 420ms 뒤로 밀렸다. 그러면 락아웃 창이 **구조적으로 닫혀**
    동시타가 영원히 안 난다(실측: 맞불 5회 전부 상대 명중까지 333ms). 패리와 같이 phase를 안 멈추게 고쳤다.
    4분기 판정의 의미(맞불은 헛침)는 그대로다
  - 재현 실측. 후퇴 지속 → 피스트 밖 실점(시드 20260802 1회, 11에서 4회).
    세이버 시드 7 반응 190ms → **시뮬타네 10회**, 110ms → 0회(창 경계 확인).
    에페 시드 1 반응 290~310ms → **더블 투셰 3회**, 280ms 이하와 320ms 이상은 0회.
    매치포인트 무효는 시드 69에서 더블 4회로 4-4 도달 후 **더블 무효 3회** 연속 발생
  - HUD 둘 신설. **PisteStrip**(14m 가로 바, 두 위치 점, 경고선 눈금, 경고 문구.
    transform만 움직이고 경고 점등은 상태 변화에만 만진다), **LampPanel**(좌 red 나 / 우 blue 상대.
    초록 미도입. 더블과 시뮬타네는 양쪽 점등이고 구분은 JudgeText 문구가 진다)
  - 위치는 **`engine.getPiste()` 폴링 창구**로 뺐다. 렌더러가 읽는 view에는 안 넣는다.
    읽기 필드 11개를 그대로 유지한다(DistanceGauge의 getD와 같은 방식)
  - AI 라인 몰기: `lineDrive` 파라미터 신설(세이버 0.55 / 에페 0.22). 판단은 전부 시드 난수 경유
  - 검증: 셀프테스트 10/10, 새 서명 일치, 4앱 빌드, 에러 0, **드로우콜 43과 텍스처 31 유지**(DOM 전용)
  - 실브라우저 육안: 피스트 스트립 눈금 위치가 계산값과 일치(2m 지점 실측 318px 대 계산 322px),
    두 점 간격에서 역산한 d가 게이지 표시와 일치(51.6 대 52), 상대 득점 시 blue 램프만 점등

- P-C 룰 심화 R4 유파 기술 시각 다양화 (확인 대기)
  - **전부 시각 계층이다.** 공격 타이밍과 판정은 opponents와 judge가 그대로 쥐고
    "어떤 기술로 보이느냐"만 갈린다. **R3 새 서명 유지가 시각 전용임을 증명한다**
  - 기술 8종 신설. 사브르 베기 4종(머리 수직 / 옆구리 수평 / 뺨 대각 / 손목 스톱컷)과
    장거리 플런지, 에페 찌르기와 플레슈와 손목 스톱히트
  - **베기 호는 겨냥점이 스윙 중간에 부푸는 것으로 만든다.** 시작과 끝에서 0이라
    착지점은 표적 그대로이고 가는 길만 휘어진다. 3D 검이 호를 그리고 파란 리본이 따라온다.
    실측 직선 대비 최대 편차 베기 0.20~0.36m 대 찌르기 0.07~0.17m로 확실히 갈린다
  - **사브르는 전진 발 교차가 반칙이라 플레슈를 못 쓴다**(FENCING_RULES 4절).
    그래서 장거리가 플런지다. 전진 포즈에 교차를 넣지 않았다
  - 발레스트라(홉 스텝) 신설. 전진 중에만 뛰고 **위로만 뜬다**(내려가면 D1 발 잘림이 재발한다).
    E4 정지 규칙 그대로 예고 진입에 홉도 함께 언다
  - 몸은 텍스처 5장 그대로다. 기존 포즈에 압축(dip)과 카메라쪽 전진(reach)을 조합해 기술을 만든다.
    플레슈 0.42m로 가장 깊고 플런지 0.36m, 스톱히트 0.05m로 가장 얕다
  - **부위 라벨 불일치를 실측으로 잡았다.** 겨냥 보정은 방향이지 위치가 아니라 보정값과
    착지 높이가 선형이 아니다. 머리 보정 +0.42로는 검끝이 y 1.497에 앉아 **라벨이 몸통으로 나왔다.**
    FUI가 "투구"라 쓰는데 칼이 가슴에 꽂히면 R4 요구가 깨진다. 문턱 1.55를 넘도록
    역산해 +0.78로 올렸고 착지 y 1.66이다. **기술 8종 전부 표적과 라벨이 일치한다**
  - 유파 분포 실측 300회. 세이버 근거리 머리 119 / 옆구리 67 / 뺨 64 / 스톱컷 50,
    장거리 플런지 209 / 머리 91. 에페 근거리 찌르기 231 / 스톱히트 69, 장거리 플레슈 209 / 찌르기 91
  - 검증: 셀프테스트 10/10, **R3 새 서명 유지**, 4앱 빌드, three와 2D 양 경로 5점 완주,
    에러 0, 드로우콜 43과 텍스처 31 유지

- P-C 룰 심화 R5 웹캠 시간 팽창 트리거와 머리 패럴랙스 (arena 룰 심화 완결, 확인 대기)
  - `game/faceTracker.js` 신설. MediaPipe tasks-vision FaceLandmarker를 **CDN에서 받는다.**
    번들에 넣지 않았다(vision_bundle이 수 MB고 실패해도 게임은 돌아야 한다).
    Vite가 URL을 해석하지 않게 `@vite-ignore`를 달았고 **프로덕션 빌드에서 실제 로드를 확인했다**
  - 추론은 **메인 루프 밖 15Hz 스로틀**이다. 렌더 콜백 안에서 추론하면 프레임이 통째로 밀린다.
    렌더는 마지막 결과를 읽기만 한다
  - 지표 4개(머리 x, 요, 얼굴 크기, 안정도) 전부 EMA. 468 랜드마크 중 코끝과 양 눈꼬리 셋만 쓴다.
    요는 각도 추정 대신 두 눈 중점 대비 코의 치우침으로 근사했다. 더 싸고 충분히 안정적이다
  - **판정에 들어가는 것은 `{ focused: boolean }` 하나뿐이다.** engine은 faceTracker를 import하지 않는다.
    캠이 안 서면 트리거가 null을 돌려 기존 성공률 근사로 내려간다(우선순위 캠 > 근사).
    **R3 새 서명이 그대로 나오는 것이 이 분리의 증명이다**
  - 패럴랙스 카메라 x ±0.05m, z ±0.03m, 요 ±0.02rad + 스무딩 한 겹.
    검이 카메라 자식이라 함께 움직인다. 크게 걸면 조준이 흔들려 게임이 안 되므로 절제가 전부다
  - **셰이크와의 충돌을 계약으로 풀었다.** 둘 다 camera.position에 절대값을 쓰면
    프레임마다 서로를 덮어써 떨림이 된다. **패럴랙스가 기준을 정하고 셰이크가 그 위에 얹는다**
    (`shake.setBase()` 신설)
  - 우아한 저하: 거부와 실패와 소실이 전부 한 경로로 수렴한다. 지표 목표가 0이 되고
    패럴랙스가 스무딩을 타고 기준 자세로 돌아온다. reduced에서도 같은 경로로 0이다
  - 실측: 카메라 없음 → 칩 "거부됨", 5점 완주, 에러 0. 가짜 카메라 + 권한 허용 →
    **`요청 중 → 얼굴 없음` 전이 확인**(CDN 로드와 추론 루프가 끝까지 돌았다는 뜻), 5점 완주
  - 검증: 셀프테스트 10/10, **R3 새 서명 유지**, 4앱 빌드, 에러 0, 드로우콜 43과 텍스처 31 유지
  - **회피 판정은 스코프 아웃.** 머리를 젖혀 피하는 것을 판정에 넣으면 아날로그 값이
    판정 경로로 들어와 결정성이 깨진다. 후보로만 남긴다

- **arena 룰 심화 R0~R5 전체 완결.** 발표 명시 사항(정직 노선):
  판정은 거리와 타이밍의 **결정적 근사**, 부위는 **표시 전용**(실룰에도 부위 점수 차등이 없다),
  우선권은 **단순화 근사**(완전한 우선권 심판은 주관 판정이다)

- controller C2 센서 파이프라인과 5phase 화면 (코드 완료, 실기 검증 대기)
  - **범위는 폰 단독 센서 동작까지다.** 소켓 연동은 C3이고 `pipeline.js`의 action/pose 콜백이 접점이다
  - `sensors/motion.js` 이산 채널, `sensors/orientation.js` 연속 채널(ahrs Madgwick),
    `sensors/permission.js`, `sensors/wakelock.js`, `pipeline.js` 오케스트레이터
  - **전방 축을 하드코딩하지 않았다.** 폰을 검처럼 쥘 때 전방이 +y인지 -z인지는 파지법과 기기마다 다르다.
    대신 **중력 기준 수평 성분이 지배적인가**로만 판정한다. 중력은 캘리브레이션에서 재고 느린 EMA로 따라간다.
    걷기는 수직이 지배적이라 여기서 잘린다
  - THRUST 네 관문: 크기(임계 18) / 수평 지배(수직의 1.25배) / 최소 지속(40ms) / 쿨다운(350ms).
    하나라도 빼면 걷기와 고쳐잡기가 샌다. 임계는 캘리브레이션에서 관측 노이즈의 2.4배로 개인 보정
  - **deg → rad 변환과 dt 실측 둘 다 코드에 있다.** ahrs Madgwick은 rad/s를 받는데
    DeviceMotion rotationRate는 deg/s다. 샘플 주기도 상수 대신 이벤트 간 실측값을
    `update`의 `deltaTimeSec` 인자로 넘긴다
  - 중력 분리 3단: `acceleration` 우선 → 없으면 `includingGravity` 하이패스 →
    둘 다 없으면 탭 버튼 모드. power 정규화는 [임계, 임계x2.5] → 0~1
  - **iOS 권한은 버튼 핸들러 첫 줄에서 부른다.** 앞에 await를 하나라도 두면 제스처 컨텍스트를 잃는다.
    `requestMotionPermission()`이 await 없이 Promise를 그대로 돌려주는 이유다.
    안드로이드는 `requestPermission` 부재를 존재 검사로 분기해 바로 통과
  - 연속 채널 30Hz 스로틀, 이산은 스로틀 없이 즉시. Wake Lock은 visibilitychange 재획득, 실패 무시
  - 화면 5종(JOIN / PERMISSION / CALIBRATION / PLAY / END) + 탭 버튼 모드 + 가로 안내 + HapticFlash.
    코드 입력은 자체 키패드다(네이티브 UI 금지). 혼동 문자 0 O 1 I를 알파벳에서 뺐다
  - `?debug=1` 디버그 뷰: 수평/수직 그래프 + 임계선, THRUST 로그(power), 가드, 자세 오일러, 실측 주기
  - 데스크톱 검증 9항목 전부 OK. 코드 자동 주입, 4자리 미만 차단(aria-disabled),
    5 phase 전환, 센서 미수신 시 탭 모드 폴백, 탭 찌르기 로그, 가로 안내, 디버그 패널, 에러 0(favicon 404 제외)
  - grep: localStorage / 100vh / HEX / 이모지 / 네이티브 select / TS **전부 0건**
    (히트 3건은 전부 규칙을 적은 주석이다). 4앱 빌드 성공
  - CREDITS에 ahrs 기록. **동봉 LICENSE는 Apache-2.0인데 package.json 필드가 APSL-2.0으로 오기돼 있다.**
    동봉 파일이 우선하고 LIBRARIES 판정표와 일치한다

- presentation-v2 브랜드 확정과 발표 덱 골격 이식 (확인 대기)
  - **브랜드 VORTEX 확정.** 펜싱 칼이 소용돌이처럼 빨아들이는 몰입. 역동과 몰입을 한 단어에
  - `presentation-v2/src/tokens.js` 한 파일만 수정. **`shared/tokens.js`는 무변경**(앱 간 색 통일은 별개 작업).
    black `#0A0A0B` → **`#000000`**(순수 블랙 확정. 깊이는 배경 그라디언트가 아니라 레드 radial 글로우가 만든다),
    red `#B3122C` → **`#E60D15`**, redGlow `rgba(230,13,21,0.45)`. **trail 색은 무변경**
  - **`typography.displayFamily` 신설.** 제목 폰트가 미정이라 본문 `family`와 분리했다.
    S1 워드마크와 S3 컨셉명만 이 키를 참조하므로 **한 줄 교체로 제목 폰트가 전부 바뀐다**.
    지금 값은 본문과 같은 Pretendard다. `motion`도 이 파일에 신설(shared를 안 건드리려면 자급해야 한다)
  - **`src/copy.js` 신설. 모든 화면 문구의 단일 원천이고 컴포넌트 하드코딩 0건**(grep 실측).
    `TITLE`과 `CONCEPT_NAME`은 지금 같은 값이지만 나중에 갈라질 수 있어 별도 상수다
  - **카피 규칙 충돌 하나를 처리했다.** DESIGN.md 260행이 화면 카피에 가운데점을 금지하는데
    지시 라벨은 `WHY · 문제` 형태였다. 라벨을 `{ en, ko }` 두 필드로 나누고
    **가운데점 대신 red 점과 얇은 선을 그려** 두 규칙을 다 지켰다. copy.js 가운데점 0건, em대시 0건, 이모지 0건
  - SECTIONS 8장 재편: COVER / WHY / CONCEPT / EXPERIENCE / AI DUELIST / AI 워크플로우 / 산출물 / 데모.
    S6~S8은 기존 플레이스홀더 유지. **`S2Background` `S3Trajectory` `S4Concept` 파일은 보존**하고 import만 뺐다
  - 셸 위임을 인덱스 키 맵으로 일반화했다. 전에는 s2/s5 전용 ref 4개였는데 위임 섹션이 셋(1 문제 / 3 인터랙션 / 4 유파)이 됐다.
    `go()`의 동작은 그대로다. **register 함수는 useMemo로 한 번만 만든다**(렌더마다 새로 만들면 자식 useEffect가 매번 다시 돈다)
  - **이미지 계약을 `components/AssetImage.jsx` 한 곳으로.** 404나 디코드 실패면 tokens 기반 다크 그라디언트 div로 내려앉는다.
    **이미지를 전부 지운 상태에서 전 8섹션 순회 완주, 콘솔 에러 0 실측**
  - S1 표지: 사진 하단 정렬 contain(인물 안 잘림) + 상단 빈 블랙에 메탈릭 워드마크와 포스터 코너 라벨.
    사진 배경이 순수 블랙이라 섹션과 이음매가 없어 **그라디언트 오버레이를 쓰지 않았다.** Tubes Cursor 제거(파일은 보존)
  - **초광폭 이음매 버그를 실측으로 잡았다.** 레드 글로우를 사진 **아래**에 깔았더니 사진 영역에서만 가려져
    3840에서 좌우에 하드한 세로 경계가 생겼다. 글로우를 위로 올리고 **`mix-blend-mode: screen`**으로 바꿨다.
    screen은 흰 유니폼(선형 1.0)을 그대로 두고 검은 영역에만 값을 더하므로 사진 안팎이 한 덩어리로 이어진다.
    **글로우 중심도 58% → 82%로 내렸다.** 머리 높이에 두면 마스크가 붉게 물들어 레드가 배경색이 된다
  - S2 문제(2단계): 단계 1에서 `scale(0.49) + translateX(∓24.5vw)`로 좌우 패널 전환. **width 애니메이션 0건.**
    uniform scale이라 사진 왜곡이 없다. TO-BE는 자리에 미리 앉아 opacity와 brightness만 오른다(예외 1곳, reduced면 opacity만).
    **배지와 캡션은 패널에 안 넣었다.** 넣으면 0.49배로 축소돼 못 읽는다. 별도 요소로 두고 translateX만 준다
  - S3 컨셉: duel.png 우측 하단 앵커 부상(opacity 0→0.9, y 40→0, 1.4s) + radial 마스크로 사각 경계 제거.
    **리퀴드글래스 라이브러리 미사용**(진입 블랙아웃 미해결). CSS 글래스 + 좌측만 큰 라운드의 비대칭 박스.
    좌측 블록의 중앙 정렬은 flex가 잡는다. **인라인 `translateY(-50%)`와 GSAP `y`를 겹치면 GSAP이 덮어써 정렬이 깨진다**
  - S4 인터랙션(4단계): 중앙 카드 1장 활성, lucide-react 원형 아이콘(red 원). 하단 진행 도트 4개
  - S5 유파(3단계): 좌측 1/3 인물 컷아웃 바닥 정렬, 전환 시 opacity + x(-30px). 우측 배지와 서술
  - lucide-react 도입. **CREDITS에 이미 "전 앱 아이콘"으로 기재돼 있어 추가 기록 없음**
  - 검증(실브라우저 헤드리스, 방향키 실제 순회 + 스크린샷 육안):
    1512에서 정방향 13장 역방향 9장 전부 포착. **서브 진행 경계 정확**(S2가 2번, S4가 4번, S5가 3번을 소비하고
    그 다음 입력에서만 섹션이 바뀐다). 역방향 대칭 확인. **콘솔 에러 0**
  - 색 실측: 섹션 배경과 html 배경 **`rgb(0,0,0)`**, `rgb(230,13,21)` 사용 요소 15개
  - 폭 실측 3840 / 2560 / 1024: **가로 오버플로 전부 0**, 페이지 에러 0
  - 4앱 아님 주의: 이 트랙은 presentation-v2 단일 앱만 손댔다. presentation-v2 빌드 성공(268.6KB, gzip 91.0KB)

- presentation-v2 색 보정과 S1 OGL Polyline 커서 궤적 (확인 대기)
  - **색 3종 확정. 생블랙과 생화이트를 쓰지 않는다.** black `#000000` → **`#101010`**,
    white **`#FDFDFD`** 신설, red `#E60D15` 유지, redGlow `rgba(230,13,21,0.45)`.
    `presentation-v2/src/tokens.js` 한 파일만. **`shared/tokens.js` 무변경**
  - `whiteA(alpha)` 헬퍼 신설. 흰 계열 값(text 4종, line 4종, surface 2종)이 전부 이 하나에서 파생한다.
    전에 흩어져 있던 `rgba(242,246,255,...)` 하드코딩을 전부 걷었다. **잔여 0건**(grep 실측).
    예외는 메탈릭 그라디언트의 `#FFFFFF` 하이라이트 스톱 하나뿐이고 주석에 예외라고 박아 뒀다

  - **코드 출처(추론 아님).** 두 곳을 실제로 읽고 가져왔다. `VortexLine.jsx` 머리 주석에 같은 내용이 있다
    - `node_modules/ogl/src/extras/Polyline.js` (설치본 1.0.11): 생성자 인자 `{ points, vertex, fragment, uniforms, attributes }`,
      **points는 Vec3 배열이고 점당 하나만 넘긴다**(정점 이중화는 클래스가 한다), 자동 유니폼
      `uResolution/uDPR/uThickness/uColor/uMiter`, `updateGeometry()`와 `resize()`의 실제 동작
    - `raw.githubusercontent.com/oframe/ogl/master/examples/polylines.html` (HTTP 200, 7889바이트):
      **vertex 셰이더 전문**(aspect 보정, prev/next 탄젠트, 법선 90도 회전, 점 겹침 smoothstep 축소, pixelWidthRatio 폭 환산),
      **스프링/프릭션 추적 루프**(i=0은 스프링, 나머지는 앞 점으로 lerp), 마우스 정규화식, resize 처리
    - 예제에서 바꾼 것: 라인 5개 → 1개, 색 tokens.red, 두께 20~50 → 4.2, 스프링 0.02~0.1 → 0.42,
      프릭션 0.7~0.95 → 0.56, 테이퍼를 양끝 → 꼬리 쪽, fragment 셰이더 신설(예제는 기본 불투명 단색),
      idle 나선 추가, body 대신 섹션 안 마운트, React 생명주기
  - ogl 1.0.11 **버전 고정 설치**(`--save-exact`). CDN 아님. 가산 블렌딩(SRC_ALPHA, ONE), depth 미사용, 드로우콜 1

  - **버그 둘을 실측으로 잡았다.**
    1. **캔버스가 300x150에 고착.** `Renderer.setSize`가 인라인 `style.width/height`를 px로 덮어써
       CSS `width:100%`가 죽고, resize가 `canvas.clientWidth`를 읽어 자기 값을 되읽는 교착이었다.
       **크기는 host div에서 재고 캔버스는 이펙트가 직접 만들어 붙인다**로 바꿨다
    2. **StrictMode 이중 마운트에서 컨텍스트 사망.** cleanup의 `loseContext()` 뒤 같은 캔버스에 다시 붙어
       잃은 컨텍스트로 렌더했다. 실측 `gl.getError() 0x9242 CONTEXT_LOST_WEBGL`, 콘솔 에러 **483건**.
       캔버스를 cleanup에서 버리고 다음 마운트가 새로 만들게 해 **에러 0건**이 됐다
  - **사진 이음매.** 사진 배경은 정확히 `rgb(0,0,0)`인데 섹션이 `#101010`이라 그냥 얹으면
    더 어두운 사각형이 된다. **사진 레이어를 screen으로 합성**했다. screen은 검정에서 배경을 그대로 통과시킨다.
    실측: 경계 x=369와 x=1143을 가로질러 R값 23→24 / 25→25 / 26→27, **바깥 평균 27.72 대 안쪽 28.17로 차이 1 미만**

  - 검증(실브라우저 헤드리스, 실제 픽셀):
    1. **idle 나선이 산다.** 커서 무입력 20초 14회 표본 레드픽셀 982~304, **0인 표본 0개**. 빈 화면 없음
    2. **커서 추종.** 스윕 중 레드픽셀 2017, 머리가 커서에 붙고 꼬리가 따라온다
    3. **라인이 워드마크를 안 가린다.** 같은 프레임 A/B 차분으로 못 박았다.
       워드마크 박스 안 실버 글자 픽셀 **26005개 중 라인 때문에 바뀐 것 0개**,
       글자 사이 여백에서만 153개 변화. host zIndex 2 < 워드마크 3
    4. **색.** 최채도 픽셀 **`rgb(230,13,21)` = `#E60D15` 정확 일치.** 배경 `rgb(16,16,16)` = `#101010`
    5. 섹션 이탈 후 프레임버퍼 1.5초/2.7초 시점 동일 → **렌더 정지 확인.** 콘솔 에러 0, GL 에러 0
  - presentation-v2 빌드 성공(324.4KB, gzip 107.9KB. ogl 도입분 +55.7KB)

- presentation-v2 이미지 6장 규약 경로 반입 (확인 대기)
  - **이 트랙은 파일 배치만 했다.** 섹션 컴포넌트 로직과 레이아웃과 애니메이션 코드는 한 줄도 안 건드렸다
    (영역 소유 규칙. 다른 세션이 동시 작업 중). 만진 것은 `public/images/` 아래와 이 문서뿐이다
  - 매핑(고정): `image 1208091651` → `why/asis.png`, `1208091652` → `why/tobe.png`,
    `1208091649` → `concept/duel.png`, `Mask group` → `duelist/style-1.png`,
    `1208091654` → `duelist/style-2.png`, `1208091653` → `duelist/style-3.png`
  - 각 원본은 리포에서 **정확히 1건씩만 매치**됐다. 원본은 리포 루트에 남겼다
  - **복사가 매핑대로 됐는지 sha256으로 대조했다. 6/6 일치, 서로 다른 그림 6장(중복 배치 0건)**
  - 참조 경로 대조: `S2Why` `/images/why/asis.png` `/images/why/tobe.png`,
    `S3Concept` `/images/concept/duel.png`, `S5Duelist` `` `/images/duelist/style-${i+1}.png` ``.
    `copy.js`의 `DUELIST_STYLES` 순서가 `[0] Ver.1 공격형 / [1] Ver.2 카운터형 / [2] Ver.3 심리전형`이라
    `style-N` 인덱스와 지시 매핑이 일치한다
  - 검증(실브라우저 헤드리스, 방향키 실제 순회):
    **7장 전부 로드 성공**(naturalWidth 0 없음), `/images/` 요청 실패 **0건**, 콘솔 에러 **0건**
  - 육안 확인: WHY 단계 0 AS-IS(버려진 장비)와 단계 1 좌우 분할 TO-BE(대련 컷) 정상,
    컨셉 duel 배경이 마스크 타고 블랙에 녹는 것 정상,
    **유파 3종 배지와 인물 짝 정상**(공격형=런지 / 카운터형=낮은 준비 자세 / 심리전형=직립 대기)
  - 배치만으로 드러난 문제 둘은 코드 소유 영역이라 손대지 않고 미해결에 수치로 남겼다
    (유파 소스 비율 불일치, S2 단계 0 라벨 대비)

- presentation-v2 브랜드 4장 삽입 (대상 / 디자인 키워드 / 네이밍 / 컬러, 확인 대기)
  - **레이아웃은 추론이 아니라 원본 SVG를 렌더해서 잡았다.** 리포의 `Slide 16_9 - 5 / 29 / 30 / 31.svg`
    (전부 1920x1080)를 **Chromium으로 PNG 렌더**해 기준으로 삼았다.
    rsvg-convert와 cairosvg와 inkscape가 이 환경에 없어 Playwright의 Chromium을 썼다
  - **SVG를 img로 박지 않고 React로 재구성했다.** 색과 문구가 코드로 교체 가능해야 하기 때문이다
  - 좌표는 SVG에서 직접 뽑아 각 컴포넌트 머리 주석에 남겼다. 대표값:
    - 대상: **카드가 아니라 큰 원 3개다.** cx 440.5 / 978.0 / 1487.0, cy 655.8, r 231.3, 채움 white 0.07
    - 디자인 키워드: 카드 x 60 / 673.3 / 1286.7, y 293, w 573.3, h 624.3, rx 20. 원 cy 605.2, r 117
    - 네이밍: 하단 사진 2컷이 각 960x492로 y 588부터 바닥까지 풀블리드. 워드마크는 **굵지 않고 얇다**
    - 컬러: 좌 1064x764 / 우상 696x369 / 우하 696x355, 전부 rx 20
  - 문구는 전부 `copy.js` 상수. 컴포넌트 하드코딩 0건
  - **원본 SVG의 오타 둘을 지시 문구로 바로잡았다.** `Desigh Keyword` → `Design Keyword`,
    `2026 KDM+ AI Workshap` → `Workshop`(표지 카피는 이전 세션에서 이미 바른 값)
  - 원본 캡션의 `거리 · 자세 · 타이밍`은 DESIGN 카피 규칙이 가운데점을 금지하므로
    `{ terms, suffix }`로 쪼개 두고 컴포넌트가 얇은 선으로 가른다. **조사는 마지막 낱말에 붙인다**
    (떼면 '타이밍  을'로 읽힌다. 실측 후 수정)
  - 토큰: `redDeep #80070C` 신설, `brandGradient`와 `brandGradientStops`와 `hexToRgbText` 신설.
    **컬러 시스템 섹션은 화면에 뜨는 HEX와 RGB와 스톱 4개를 전부 tokens에서 읽는다**(색 리터럴 0건)
  - **인덱스 밀림을 구조로 막았다.** 브랜드 4장이 문제와 컨셉 사이에 들어가면서
    인터랙션과 유파의 인덱스가 3,4 → **7,8**로 밀린다. 손으로 적으면 다음 삽입에서 또 깨지므로
    위임 섹션을 **id로 선언하고 인덱스를 findIndex로 유도**하게 바꿨다(`DELEGATE_IDS = ['s2','s8','s9']`).
    렌더 분기도 `i === n`에서 **`s.id === 's_'`로 교체**했다. `go()`의 동작은 무변경
  - 검증(실브라우저 헤드리스, 방향키 완주): 12섹션 정방향 완주, **섹션별 입력 소비 횟수
    s2 2회 / s8 4회 / s9 3회 / 나머지 1회씩으로 서브 진행 무결**, 역방향 s1 복귀, 콘솔 에러 0
  - 폭 실측 3840 / 1512 / 1024: 브랜드 4장 전부 **가로 오버플로 0, 페이지 에러 0**
  - **그라디언트 카드 라벨 대비는 원본에서 물려받은 문제다.** 흰 글자가 그라디언트의 밝은 쪽에 얹힌다.
    실측 원본 SVG의 Color1 라벨 뒤 190.2/255, 재구성본 164.1/255.
    지정 토큰이라 그라디언트는 그대로 두고 **레이아웃을 안 바꾸는 텍스트 그림자로만** 읽히게 했다
  - presentation-v2 빌드 성공(340.2KB, gzip 111.3KB)

- presentation-v2 컨셉 섹션 사진 블렌딩과 인용 구분선 (확인 대기)
  - **이 트랙은 `S3Concept.jsx`와 `copy.js`의 CONCEPT 상수만 만졌다.** 다른 섹션 무변경
  - 레이아웃은 `Slide 16_9 - 4.svg`(1920x1080)를 Chromium으로 렌더해 기준으로 삼고
    좌표와 그라디언트 스톱을 SVG에서 직접 뽑았다. 컴포넌트 머리 주석에 전부 남겼다
  - **증상(실측): 사진 좌측에 하드한 사각 경계.** 이전 판은 radial 마스크 하나로 덮으려 했다.
    **원본은 마스크가 아니라 좌→우 블랙 오버레이로 덮는다.** 원본 rect x 724 w 1058 h 1080,
    그라디언트 x1 1782 → x2 651.674에 알파 0.1 / 0.822596(offset 0.6875) / 1.
    즉 오른쪽은 거의 투명하고 왼쪽으로 갈수록 새까매진다. 이 구조를 1920 기준 퍼센트로 환산해 이식했고
    (33.94% 알파 1 / 52.33% 알파 0.823 / 92.81% 알파 0.1),
    사진 자체에도 좌측 55% 페더링과 상하 페더링을 `mask-composite: intersect`로 곱해 두 겹으로 녹였다
  - **경계 소멸 실측.** 사진 좌측 시작(29% = x 438) 주변 가로 스캔에서 휘도가 y 200 / 400 / 700 전부
    **16/255 근처로 평탄**하고 계단이 없다. 상단 경계(7.1% = y 64)도 15~17로 평탄.
    남아 있는 큰 계단(23~131)은 전부 x 905~1409의 **선수 흰 유니폼이지 경계가 아니다**
  - 텍스트 순서를 원본대로 세웠다. CONCEPT(red) → VORTEX(메탈릭) → 인용 → 가로 구분선 → 글래스 필
    - 인용: 원본 x 102.4 y 446.098 size 27
    - 구분선: 원본 rect x 102.4 y 496.6 w 498.6 **h 0에 그라디언트 스트로크**(알파 1 → 0.1).
      원본은 알파만 빼지만 검끝 모티프를 살리려 `clip-path`로 **두께도 오른쪽으로 0에 수렴**시켰다
    - 글래스 박스: 원본 rect w 1039.79 h 175.7 **rx 87.85 = h/2 이므로 완전한 필**이다.
      이전 판의 좌측만 굴린 비대칭 형태를 원본대로 필로 교체했다.
      원본 fill 그라디언트(알파 1 → 0.2)에 **fill-opacity 0.3** → 유효 0.30 → 0.06을 그대로 썼다
  - **보더 그라디언트 트릭의 함정을 실측으로 잡았다.** 두 겹 `background-clip: padding-box, border-box`는
    채움이 불투명일 때만 성립한다. 반투명 채움이면 아래 깔린 솔리드 레드가 박스 전체로 비친다.
    실측 필 좌측이 **(208,13,20)**으로 원본 (54,11,18)보다 훨씬 뜨거웠다.
    링을 별도 레이어로 빼고 `mask-composite: exclude`로 테두리만 남겨 **(75,15,17)**로 잡았다.
    원본보다 조금 밝은 것은 브랜드 레드가 #B3122C에서 #E60D15로 바뀐 몫이다(알파는 원본과 동일)
  - 좌측 텍스트 뒤 배경 휘도 실측: CONCEPT 16 / VORTEX 27 / 인용 33 / 글래스 51 (전부 흰 텍스트 가독 확보)
  - 검증: 1512 / 3840 / 1024에서 **가로 오버플로 0, 콘솔 에러 0**, 빌드 성공(341.4KB, gzip 111.6KB)
  - **미확인 하나.** 링의 `mask-composite`는 Chromium에서만 확인했다. 발표는 크롬이라 문제없지만
    Safari는 `-webkit-mask-composite: xor` 경로를 타므로 실기 확인이 필요하다

- presentation-v2 유파 섹션 개편, 카드 안무 6단계 (확인 대기)
  - **이 트랙은 `S5Duelist.jsx`와 `copy.js`의 DUELIST 상수만 만졌다.** 다른 섹션 무변경
  - 레이아웃 출처: `Slide 16_9 - 7.svg`(1920x1080)를 Chromium으로 렌더하고 좌표를 SVG에서 직접 뽑았다
    - 인물 3장 x 581.1 w 609.4 / x 1153.1 w 290.1 / x 1504.6 w 196.7.
      **원본은 인물 폭이 제각각인 것이 설계다.** 런지가 넓고 서 있는 둘은 좁다.
      이전 세션 미해결이던 "유파 인물 3종 소스 비율 불일치"는 **에셋 결함이 아니라 원본 의도로 확인됐다.**
      높이를 맞추고 폭은 자연 비율대로 두는 것으로 정리
    - 배지 y 936.575 h 60.35 **rx 30.17 = h/2 필**. 레드 글로우 ellipse cx 1165.5 cy 718 rx 997.1 ry 730
    - **원본 글로우는 알파 0.9에서 시작하고 주황(#D93E16 #EA520C #FF6A00)으로 번진다.**
      브랜드 팔레트에 주황이 없고 지시가 저알파 radial이라 tokens.red 한 색으로 낮은 알파만 썼다
  - **카드 인터랙션 출처: 포폴 저장소 `client/src/components/work/StackCarousel.jsx`를 실제로 열고 이식했다.**
    `~/Downloads/26-portfolio-hyunho-main.zip`에서 해당 파일만 추출했다(zip에 파일명 인코딩 오류가 있어
    전체 해제는 중간에 끊긴다). 가져온 것과 원본 행 번호:
    - `circDist(i, activePos, N)` 원형 최단거리 43~47행
    - `isActive = absD < 0.5` 214행 / `z = Math.round(100 - absD)` 217행
    - dim 계산 `absD < 0.02 ? 0 : Math.min(0.35, absD * 0.12)` 225~227행
    - 락 기반 한 칸 스텝 `go(dir)` + `lockRef` 83~88행
    - transition 곡선 `cubic-bezier(0.22,1,0.36,1)` 230~232행
    - **안무는 새로 짰다.** StackCarousel은 아치형 팬 캐러셀이고 여기는 가로 일렬에서 한 장이 앞으로 나온다
    - 조정 둘: 카드가 3장뿐이라 absD 상한이 1이고 원식이면 dim이 0.12까지만 간다.
      포커스 상태에서만 배수를 걸되 **원본 상한 0.35는 지켰다.**
      락 시간도 원본 90ms(260ms 전환)에서 이 안무의 620/920ms에 맞춰 늘렸다
  - 안무 6단계: 0 인트로 / 1 카드1 전진 / 2 복귀 / 3 카드2 / 4 복귀 / 5 카드3.
    커지는 전환 600ms, 복귀 900ms. transform과 opacity와 filter blur만 쓴다
  - **버그 둘을 실측으로 잡았다.**
    1. **흐린 카드가 상세 패널을 덮었다.** 카드 z가 `100 - absD`라 99~100인데 패널이 7이었다.
       패널을 110으로 올려 카드 위에 뒀다
    2. **카드 하단 유파명이 진행 도트와 겹쳤다.** 텍스트 블록을 `bottom: clamp(38px, 5.4vh, 62px)`로 띄웠다
  - 검증(실브라우저 헤드리스, 방향키 순회. DOM에서 실제 transform과 filter를 읽었다):
    - 상태1: 카드1 **cx 378(좌측 이동) w 526 h 950(확대) blur 0 z 100**,
      카드2/3 **blur(6px) w 390 h 704 z 99**, 상세 opacity 1
    - 상태2/4: 세 장 전부 **cx 697 / 1022 / 1263, w 454 h 819로 완전 복귀**, blur 0, 상세 opacity 0
    - 인트로 cx 697 / 1022 / 1263 = **46.1% / 67.6% / 83.5%로 원본 인물 중심 비율과 일치**
    - 배지와 인물과 문구 정합: 카드1 이탈리아 세이버/Ver.1 공격형 + "긴장감을 조성해서",
      카드2 프랑스 에페/Ver.2 카운터형 + "먼저 움직이면 불리", 카드3 헝가리안/Ver.3 심리전형 + "침착한 관찰력"
    - 경계: 상태5에서 아래 → s10 이탈. 역방향 7회로 대칭 복귀. **콘솔 에러 0**
  - 폭 실측 3840 / 1512 / 1024: 상태0과 상태1 전부 **가로 오버플로 0**. 빌드 성공(344.0KB, gzip 112.5KB)

- presentation-v2 데모 CTA 섹션 신설 + 브랜드 3장 이미지 반입과 원 디자인 (확인 대기)
  - **데모 CTA(`S7Demo.jsx` 신설, s12).** 레이아웃은 `Slide 16_9 - 9.svg`를 Chromium으로 렌더하고
    좌표를 SVG에서 직접 뽑았다. 레드 원 `circle cx 1743 cy 881.3 r 68 fill #E60D15` → **90.78% / 81.60%**,
    화살표 `path M1704.07 874.3 H1781.93 L1758.11 899.948` stroke white 8px → 우상향.
    lucide `ArrowRight`를 -45도 돌려 같은 방향으로 맞췄다
    - **arena URL을 하드코딩하지 않았다.** `import.meta.env.VITE_ARENA_URL`을 읽고
      **미설정이면 이동하지 않고 콘솔 경고만 남긴다**(발표 중 빈 탭이 열리는 것을 막는다)
    - 진입: 라벨 → 헤드라인 3줄 y 페이드업(stagger 0.12) → 버튼 0.72s 지연 등장. transform과 opacity만.
      글로우는 정적 box-shadow이고 호버에서 한 단계 세진다
  - **이미지 5장 반입.** 매핑 그대로 복사하고 sha256으로 대조해 **5/5 일치, 중복 배치 0건** 확인.
    `Frame 2147230493` → `keyword/dynamic.png`, `494` → `precision.png`, `495` → `immersion.png`,
    `500` → `naming/photo-1.png`, `501` → `naming/photo-2.png`. 각 원본은 리포에서 1건씩만 매치됐다
    - `copy.js`의 네이밍 경로를 `shot-1/2` → `photo-1/2`로 맞췄다
  - **TARGET 원 재설계.** 그라디언트 채움을 걷고 테두리만 남겼다
    - **주의: `Slide 16_9 - 5.svg`는 수정돼 있지 않다.** 4345바이트로 이전 세션과 동일하고
      여전히 `fill="white" fill-opacity="0.07"` 채움이 들어 있다. 렌더해서 확인했다.
      그래서 원 디자인은 **지시서의 명시 스펙**을 기준으로 삼았고 크기와 배치만 SVG 좌표를 유지했다
      (cx 440.5 / 978.0 / 1487.0, cy 655.8 → 세로 60.7%로 중앙보다 살짝 아래)
    - 채움 `transparent`. **backdrop-filter를 쓰지 않았다**(뒤가 단색이라 효과가 없고 비용만 든다)
    - 유리 림은 `linear-gradient(160deg, ...)`를 mask로 뚫어 1.2px 링만 남긴다.
      **두 겹 background-clip 트릭을 쓰지 않았다.** 링이 반투명이라 그 방식이면 아래 레이어가
      원 전체로 비친다(컨셉 섹션에서 실측으로 확인한 함정). `components/Bits.jsx`의 `GlassRim`으로 공용화
    - 번호 01/02/03을 **tokens.red**로. 본문은 기본 400이고 강조 조각만 700(볼드 위치는 copy.js가 쥔다)
  - **디자인 키워드 원형 라벨**도 같은 `GlassRim` 문법. 여기는 뒤가 사진이라 **`backdrop-filter: blur(3px)`를
    원 안쪽에만 걸었다**(TARGET과 달리 효과가 실제로 보인다). 사진 3장 연결 완료
  - **네이밍 하단 2컷** 연결. 위쪽 블랙과 사진 사이 경계를 상단 16% 구간의 `#101010` 페이드로 죽였다
  - 검증(실브라우저 헤드리스, 실제 픽셀):
    - TARGET 원 3개 `background: rgba(0,0,0,0)`, **원 안쪽 휘도 17.3 대 바깥 배경 16.9로 차이 0.5**
      → 채움이 완전히 사라졌다. 상단 링 95.6으로 배경 대비 **+78.8**. 번호 실측 **rgb(230,13,21)**
      원 중심 x 339 / 756 / 1173(등간격 417), y 546 = **60.7%**
    - 키워드 사진 좌→우 **dynamic / precision / immersion 순서 정확**, 셋 다 로드 성공
    - 네이밍 좌 `photo-1.png` / 우 `photo-2.png` 정확
    - 데모: 헤드라인 `["ENTER","THE","VORTEX."]`, 버튼 중심 **90.8% / 81.6%로 SVG와 일치**,
      배경 `rgb(230,13,21)`, 호버에서 글로우 강화 확인,
      **클릭 시 `[demo] VITE_ARENA_URL 미설정. 이동을 건너뛴다.` 경고 확인**
    - `/images/` 요청 실패 **0건**, 콘솔 에러 **0건**
    - 1512 / 3840 / 1024 전부 **20회로 s12 완주, 최대 가로 오버플로 0**. 빌드 성공(347.8KB, gzip 113.4KB)

- presentation-v2 인터랙션 섹션 개편, 서브 진행 제거와 4열 동시 노출 (확인 대기)
  - **이 트랙은 `S4Experience.jsx`와 `copy.js`의 EXPERIENCE/INTERACTIONS, `App.jsx`의 위임 등록만 만졌다**
  - **레이아웃 원본이 SVG가 아니었다.** 리포의 Slide SVG 8장을 전부 렌더해 확인했고
    이 섹션에 해당하는 것이 없다(8번은 유파 카드 페이지다).
    원본은 `바인더1.pdf` **6페이지**의 4카드 그리드다
    - PDF 렌더 도구가 없어(`pdftoppm` `cairosvg` `pdf2image` `Quartz` 전부 부재,
      `qlmanage`는 1페이지만, Chromium 헤드리스는 PDF를 다운로드로 처리) **PyMuPDF를 설치해** 7페이지를 렌더했다
    - 실측(1600x900 기준): 카드 4장 x 85~418 / 449~781 / 813~1145 / 1176~1509
      → 폭 **20.875%**, 간격 **1.88%**, 좌우 여백 5.31%로 대칭.
      카드 세로 상단 **43.1%**, 아이콘 원 지름 = **카드 폭의 22.2%**,
      카드 채움 rgb(26,26,29) ≈ tokens.raised, 원본 레드 rgb(179,17,43)은 tokens.red로 교체
  - **서브 진행 전면 제거.** 이전 판은 카드 1장씩 방향키로 넘기는 4단계 위임이었다.
    `App.jsx`의 `DELEGATE_IDS`에서 **`s8`을 뺐고**(`['s2','s8','s9']` → `['s2','s9']`)
    렌더 분기도 `registerHandler/registerEnter` 대신 `active`를 넘기게 바꿨다.
    인덱스는 여전히 id로 유도하므로 손으로 고칠 곳이 없었다
  - 진입: `active`가 true가 될 때마다 4장이 좌에서 우로 밀려 들어온다(GSAP stagger 0.14, 카드당 0.55s).
    **1회성으로 고정하지 않아 왕복하면 다시 재생된다.** transform과 opacity만. reduced면 이동 없이 페이드만
  - **셸 이동과 겹치는 문제를 실측으로 잡았다.** 셸의 섹션 이동이 1초인데 `active`는 키를 누른 즉시
    true가 되어 지연이 없으면 **스테거가 화면이 도착하기 전에 끝난다.**
    타임라인에 0.5s 지연을 줘 카드가 밀려 들어오는 구간이 착지 이후에 오게 했다
  - 카피: PDF의 문구를 그대로 쓰되 둘을 고쳤다. 헤드라인의 **'포르테를'은 구 제품명(PORTÉE)이라
    확정명 VORTEX로 교체**했고 **'인터렉션' 오타를 '인터랙션'으로** 바로잡았다.
    원본의 가운데점(`페인트 · 리포스트 판독`, `속도·각도`)은 DESIGN 규칙이 금지하므로
    낱말을 terms로 쪼개 얇은 선으로 가른다
  - 아이콘은 원본 그림에 맞춰 lucide `Footprints` / `PersonStanding` / `Swords` / `Brain`. 존재 확인함
  - 검증(실브라우저 헤드리스, DOM에서 실제 opacity와 transform을 읽었다):
    - **스테거 실측.** 재진입 +960ms에서 `op 1.00 / 0.47 / 0.00 / 0.00`,
      +1213ms에서 `1.00 / 0.97 / 0.81 / 0.29` → 좌에서 우로 순차 진입
    - **왕복 재생 확인.** 재진입 시 네 장 전부 `op 0.00 x -34`로 리셋된 뒤 다시 재생된다
    - 4장 동시 노출: cx 239 / 584 / 928 / 1273, 같은 top, 317x240 동일 크기
    - **서브 진행 잔존 0.** 전체 순회에서 소비 횟수가 s2 2회 / s9 6회뿐이고 **s8은 1회**로 통과.
      총 17회로 s12 도달
    - 위치 실측: 라벨 **19.6%**(원본 20.4%), 카드 상단 **42.7%**(원본 43.1%)
    - 폭 1512 / 3840 / 1024 / 430: 앞 셋은 4열 1행, **430에서 2x2로 접힌다.**
      전부 가로 오버플로 0, 콘솔 에러 0. 빌드 성공(348.7KB, gzip 113.6KB)

- presentation-v2 배경 PROLOGUE와 인사이트 PAIN POINT 신설, 위임 id 기준 리팩터 (확인 대기)
  - **선행 리팩터: 서브 진행 위임을 인덱스에서 섹션 id로 옮겼다.**
    - `SECTION_LABELS`의 id를 `s1~s12` 번호에서 **의미 이름**(`cover` `prologue` `painpoint` `why` ...)으로 바꿨다.
      번호 id는 앞에 섹션을 끼울 때마다 전부 밀려 위임과 렌더 분기가 같이 깨진다
    - `subHandlers`/`subEnters`를 **id로 키잉**하고 `go()`가 `SECTIONS[currentRef.current].id`로 조회한다.
      진입 통지도 `SECTIONS[next].id`로 한다. **App.jsx에 인덱스 하드코딩이 0건**이 됐다
    - `DELEGATE_IDS = ['why', 'duelist']`. 렌더 분기도 전부 의미 id로 교체
  - 섹션 2장을 표지 바로 다음에 삽입해 **14섹션**이 됐다.
    표지 → 배경 → 인사이트 → 문제 → 대상 → 디자인 키워드 → 네이밍 → 컬러 → 컨셉 → 인터랙션 → 유파 → 워크플로우 → 산출물 → 데모
  - **배경 PROLOGUE**(`SPrologue.jsx`). 원본 `Slide 16_9 - 2.svg`. 이 SVG는 텍스트가 살아 있어
    좌표와 강조 구간을 직접 뽑았다
    - 라벨 `#E60D15` size 24 weight 600, x 60 y 229.9 → **3.13% / 21.3%**
    - 본문 2줄 size 38, y 535 / 615 → 49.5% / 56.9%. **가운데 정렬**(두 줄 모두 중심 960)
    - 강조 3구는 밝고 굵게, 나머지는 `whiteA(0.7)`. 이미지 없음. 줄 단위 페이드업
  - **인사이트 PAIN POINT**(`SPainPoint.jsx`). 원본 `1.svg`
    - **파싱 주의: 750KB라 base64를 먼저 걷어내지 않으면 정규식이 백트래킹으로 멈춘다**(실제로 겪었다)
    - 라벨 둘 `#E60D15` x 57.6, y 256.1 / 708.2 → 3.0% / 23.7% / 65.6%
    - 상단 글래스 카드 x 381.883 / 817.883 / 1253.883, y 235.441, w 389.5, h 261.5, rx 24.75, 채움 black 0.01
    - 점선 화살표 x 577.1 / 1013.1 / 1449.1, y 537 → 649
    - 하단 레드 카드 x 392.633 / 828.633 / 1264.633, y 685.948, w 368, h 223.743, rx 25, `#B3122C` 0.35
      → 브랜드 확정색 `tokens.red` 0.35로 교체
    - **열 정렬이 이 페이지의 핵심이다.** 하단 카드가 21.5 좁고 10.75 안쪽으로 들어와
      상하 중심이 576.6 / 1012.6 / 1448.6으로 정확히 같다. 이 비율(94.5%)을 그대로 옮겼다
    - **원본 오타 `Fuctional`을 `Functional`로 교정**했다
    - 배경 사진은 라이선스 미확인 시안이라 임베드하지 않고 `/images/painpoint/bg.png` 경로로 뺐다.
      파일이 없어 지금은 다크 플레이스홀더다
    - 진입: 상단 카드 좌→우 stagger → 화살표가 위에서 아래로 자람 → 하단 카드. transform과 opacity만
  - 두 섹션 모두 셸 이동 1초를 감안해 타임라인에 **0.45s 지연**을 줬다(없으면 착지 전에 연출이 끝난다)
  - 검증(실브라우저 헤드리스, 방향키 완주):
    - **순서 정확.** Home → cover, 이어서 prologue / painpoint / why
    - **위임 무결.** 섹션별 입력 소비가 `why 2회` / `duelist 6회`뿐이고 나머지는 1회씩.
      총 19회로 demo 도달, 역방향 19회로 cover 복귀
    - **열 정렬 실측.** 페인·화살표·인사이트 중심 x가 3열 전부 일치(454 / 797 / 1141),
      하단·상단 폭 비 **94.5%로 원본과 동일**
    - 오타: `Functional` 있음 / `Fuctional` 0건
    - 1512 / 3840 / 1024 전부 가로 오버플로 0, 열 정렬 유지, 콘솔 에러 0.
      빌드 성공(355.4KB, gzip 115.1KB)

- presentation-v2 전역 텍스트 정리와 아이브로우 통일 (확인 대기)
  - **레이아웃과 인터랙션 로직은 안 건드렸다.** copy.js와 공용 조각, 각 섹션의 아이브로우 호출부만 손봤다
  - **공용 `Eyebrow` 신설(`components/Bits.jsx`).** 전 섹션이 이것만 쓴다. 개별 하드코딩 아이브로우 0건
    - 왼쪽에 레드 원 7px(`marginTop: 0.44em`으로 영문 baseline 정렬), weight **600 통일**
    - 영문 위 작게 / 국문 아래 크게인 **스택 라벨**. 국문이 없으면 단일 라벨
    - 예전 `SectionLabel`은 영문과 국문을 얇은 가로선으로 갈라 한 줄에 늘어놨다.
      **스택으로 바꾸면 구분자 자체가 필요 없다**(가운데점을 피하려던 장치였다). `SectionLabel` 제거
    - 교체 대상 12곳: PROLOGUE / PAIN POINT / INSIGHT / WHY / TARGET / Design Keyword /
      Brand Naming / Color System / CONCEPT / EXPERIENCE / AI DUELIST / IMMERSIVE FENCING XR
    - 스택 3곳(키워드, 네이밍, 컬러)은 별도로 붙어 있던 국문 div를 Eyebrow가 흡수했다
  - **금지 문자 제거.** UI 문자열에서 가운데점과 em대시와 박스라인을 걷었다
    - `{ terms }` 구조를 평문으로 되돌렸다. `거리, 자세, 타이밍을` / `검끝 속도, 각도 추적, 명중 순간` /
      `페인트, 리포스트 판독`. 그 구조를 그리던 `Segmented`와 `CaptionLine` 헬퍼도 함께 제거
    - **원래 UI 문자열에는 이미 금지 문자가 없었다**(앞 세션들이 필드 분리로 피해 뒀다).
      실제 잔존은 전부 주석이었고, 이번에 만진 파일과 사용 중 파일의 주석까지 정리했다
    - **잔존은 미사용 보존 파일 5개뿐이다**: `S2Background` `S3Trajectory` `S4Concept`
      `S5Interactions` `panels/shaderPanel`. 이 트랙 범위 밖이라 손대지 않았다(전부 주석)
  - **첫 화면 조작 힌트 삭제.** `App.jsx`의 힌트 블록과 `copy.js`의 `COVER_HINT`를 지웠다
  - 검증(실브라우저 헤드리스, 섹션 순회):
    - 아이브로우 12개 전부 **레드 원 7px + weight 600**, 스택 라벨 국문도 600
    - **각 섹션 화면 텍스트의 금지 문자 0건**(innerText에서 `·` `—` `─` `•` `・` 검사)
    - 표지에서 `SPACE`/`SCROLL` 힌트 **없음** 확인
    - 콘솔 에러 0, 빌드 성공(353.2KB, gzip 114.7KB)

- presentation-v2 디자인 키워드 서브 진행 포커스 (확인 대기)
  - **이 트랙은 `S4Keyword.jsx`와 `App.jsx`의 위임 등록 두 줄만 만졌다.** 다른 섹션과 다른 위임 무변경
  - **레이아웃 무변경이라 SVG를 다시 렌더하지 않았다.** 원본 `Slide 16_9 - 29.svg` 좌표는
    파일 머리 주석에 이미 있고 카드 배치와 크기를 그대로 둔 채 포커스 상태만 얹었다
  - 정적 동시 표시 → **서브 진행 3단계 포커스**로 바꿨다. S2Why의 registerHandler/registerEnter 패턴 재사용
    - `App.jsx`는 이미 id 기준 위임이라 `DELEGATE_IDS`에 `'keyword'`를 넣는 것으로 끝났다.
      **인덱스 하드코딩이 없어 다른 위임(why, duelist)은 손댈 일이 없었다**
    - 위임 섹션이지만 진입 연출 트리거로 `active`도 함께 받는다
  - 포커스 시각: 포커스 카드 `scale 1.08` + blur 0 + brightness 1, 나머지는 `scale 0.94` + `blur 5px` +
    `brightness 0.6`. **`transform-origin: center`라 확대해도 좌우 중심이 안 움직인다**
  - 진입 방향으로 시작 단계를 정한다. 아래로 진입 → 0(Dynamic), 위로 진입 → 2(Immersion).
    경계(0에서 위 / 2에서 아래)에서만 섹션이 바뀐다
  - reduced motion: 확대와 blur 없이 밝기만으로 포커스를 표시한다
  - **부양감을 box-shadow 트랜지션으로 내지 않았다.** 별도 글로우 레이어의 opacity만 움직여
    transform과 opacity와 filter만 쓴다는 규칙을 지켰다
  - **글로우가 헤더를 덮던 문제를 실측으로 잡았다.** 처음엔 `inset: -6% -4% 4% -4%`에
    `rgba(16,16,16,0.85)` 그림자였는데 위로 번져 1번 카드에서 헤드라인 위에 어두운 판이 얹혔다.
    위쪽 번짐을 없애고(`inset: 2% -3% 2% -3%`) 그림자 알파를 0.5로 낮췄다
  - 진입 연출과 포커스가 서로 안 부딪히게 **속성을 갈랐다.** 진입은 `opacity`와 `y`,
    포커스는 `scale`과 `filter`. GSAP `overwrite: 'auto'`는 같은 속성만 죽이므로 안전하다
  - 검증(실브라우저 헤드리스, DOM에서 실제 transform과 filter를 읽었다):
    - 포커스 이동 1→2→3에서 포커스 카드 `scale 1.080 / blur 0px / bright 1`,
      나머지 `scale 0.940 / blur 5px / bright 0.6`
    - **좌우 위치 고정.** cx가 세 상태 내내 273 / 756 / 1239로 동일
    - **경계 정확.** 3번 포커스에서 아래로 눌러야 다음 섹션(naming)으로 넘어가고 중간에서는 안 넘어간다
    - **역방향 대칭.** 위로 진입하면 Immersion부터 시작해 3회 만에 앞 섹션(target)으로 이탈
    - 3840 / 1512 / 1024 전부 좌우 고정, 가로 오버플로 0, 콘솔 에러 0. 빌드 성공(354.4KB, gzip 115.2KB)

- controller C3 소켓 (S0~S3 전체 완료, 실기 확인 대기)
  - **데모 코어 루프가 성립했다.** 폰을 움직이면 arena 1인칭 검이 따라오고, 폰 찌르기가 판정에
    들어가고, 명중과 패리가 폰으로 되돌아간다
  - **S0.** `server/src`는 손댈 것이 없었다. SERVER.md 방 생명주기 1~5를 스모크 17항목으로 전수
    확인했고 전부 통과했다(코드 4자 혼동 문자 제외, not_found, 양쪽 paired, 릴레이 5종 왕복,
    자기 에코 없음, controller 재접속 재합류, arena 이탈 시 peer_left와 방 소멸, bad_role)
    - **이산 채널이 두 이름으로 말하고 있었다.** ARENA_INPUT 스키마는 다섯(thrust guard_on
      guard_off advance retreat)이고 SERVER.md `action.kind`는 넷에 on/off를 power로 접는다.
      `toWireAction`과 `fromWireAction` 둘만 변환 통로로 두고 왕복이 항등임을 확인했다
    - `INPUT_EVENT` `SOURCE` `STEP` 상수를 신설해 controller의 문자열 리터럴 8곳을 걷었다
    - `fromWireAction`이 모르는 kind에 null을 돌린다. **여기서 THRUST를 만들어 내지 않는 것이
      이중 판정 금지의 경계다**
  - **S1.** `controller/net/socket.js` 구현. pipeline의 action과 pose 콜백이 그대로 emit으로 간다.
    이산은 즉시, 쿼터니언은 orientation.js가 이미 건 30Hz 스로틀을 탄다(실측 27Hz, 단위 크기 1.0000)
    - 서버 주소는 `VITE_SERVER_URL`에서만 읽고 폴백을 안 뒀다. 비면 화면에 그 사실을 띄운다.
      배포에서 틀린 주소로 붙는 것보다 안 붙는 것이 낫고 안 붙는 이유가 보여야 고칠 수 있다
    - **무한 스피너를 만들지 않았다.** socket.io는 기본값으로 영원히 재시도하는데 화면에서는
      멈춘 스피너로 보인다. 8초 상한 뒤 사유 넷과 재시도 버튼을 낸다
    - 끊긴 동안의 입력은 버린다. 큐에 쌓았다 재연결에 쏟으면 낡은 찌르기가 몇 초 뒤에 꽂힌다
    - **`MSG.CALIB`이 싣던 값을 고쳤다.** `getPose()`는 rel을 돌려주는데 rel은 다음 센서
      이벤트에서야 재계산되므로 `setBaseline` 직후에 읽으면 보정 전 값이 나갔다(실측 92도)
  - **S2.** arena가 방을 만들고 PAIRING에서 코드와 QR을 낸다. paired에 CALIBRATION,
    폰의 calib 통지에 EN_GARDE. **machine.js 무변경이라 기존 전이표를 그대로 탄다**
    - `input.js`를 ARENA_INPUT 2절대로 확장했다. `emit()`이 단일 입구가 되고 홀드가 소스별로
      갈린다. `push`와 `setHeld`는 시그니처와 의미를 보존했다. 셀프테스트 대본이 그 둘을 쓰고
      기준 서명이 거기서 나오기 때문이다. `isHeld`는 소스 OR이고 소스가 하나면 항등이다
    - **`pose.js`의 모드 소유권 버그를 잡았다.** `setPreset`이 모드를 되돌리는데 GameCanvas가
      매 프레임 그것을 부르므로 쿼터니언이 흘러도 다음 프레임에 프리셋이 덮어썼다.
      **검이 폰을 전혀 안 따라갔다**(실측: 다섯 자세가 전부 같은 그림). 모드는 소스만 바꾼다
    - **회전은 폰이, 그립 위치는 프리셋 트윈이 쥔다.** 6절 우선순위를 회전에 그대로 걸면
      찌르기 순간 착탄이 겨눈 곳이 아니라 프리셋이 정한 곳에 꽂혀 "폰으로 조준한다"가 깨진다.
      트윈이 뻗는 호를 계속 쥐므로 찌르기 연출 자체는 그대로 산다. 소스 전환은 0.35초 슬러프
    - **AIM_JITTER는 제거가 아니라 분기다.** 폰이 쥐는 동안 0이고 키보드 폴백에서는 그대로 산다
    - QR은 `qrcode-generator`(MIT, 의존성 0)가 모듈만 내고 **SVG 사각형으로 직접 그린다.**
      canvas도 이미지도 안 만들어 4K에서 선명하다. CREDITS와 COMPONENTS에 기록
    - 컨트롤러가 붙어도 키보드는 계속 산다. 끊기면 검이 프리셋으로 돌아가고 컨트롤러 소스의
      홀드가 풀린다(안 풀면 마지막 가드가 영원히 켜진 채로 남는다)
  - **S3 검증(실브라우저 두 탭 + 서버).** 11/11
    - 방 코드 표시와 QR 렌더, arena 서버와 컨트롤러 칩 연결됨, calib 수신 후 EN_GARDE 진입
    - **폰 탭 찌르기가 판정에 도달해 득점**(0 → 2), 햅틱 왕복
    - 컨트롤러 연결 중 키보드 병행, 강제 종료 후 "폰 대기" 표시와 키보드로 경기 지속, 재접속 재합류
    - **폰 쿼터니언 5자세에서 검이 실제로 회전.** 칼끝 세로 스팬 342px, 검 픽셀 수 -2193~+837
      (정면을 향하면 단축돼 픽셀이 줄고 옆으로 누우면 는다). 고치기 전 프레임은 다섯 장이 동일했다
    - **서버 프로세스를 실제로 내린 상태에서 arena 단독 키보드 5점 완주**(6 대 0).
      PAIRING에 갇히지 않고 [키보드로 시작]이 늘 열려 있다
    - grep: 서버 주소 하드코딩 0, 소켓 emit과 on이 전부 MSG 상수, judge machine opponents engine에
      `source`와 `pose` 0건, localStorage 0
    - 셀프테스트 10/10, **R3 서명 205 / ef4b27d5 / JUDGE:6-1:51.833#6-1#ME 유지**, 4앱 빌드, 앱 콘솔 에러 0
  - 배포 절차를 `docs/SERVER.md`에 한 절로 적었다. Render 배포 순서, Vercel 환경 변수,
    **HTTPS 페이지는 wss만 붙는다**(가장 흔한 실패), Render 콜드 스타트 대응
  - **사용자 실기 절차.** 노트북에서 arena를 열고 경기 시작 → 폰(Safari)으로 QR 스캔 →
    센서 권한 → 검처럼 쥐고 3초 정지 → 폰을 움직여 검이 따라오는지, 찌르면 판정이 되는지,
    명중 때 폰이 번쩍이는지 확인. **지연 체감과 검 자세의 자연스러움을 보고해 달라.**
    검이 엉뚱한 방향을 보면 폰을 쥐는 기준 자세 문제이므로 캘리브레이션을 다시 한다

- P-C VORTEX 리스킨 V0 토큰 값 교체와 문서 (확인 대기)
  - `shared/tokens.js` 색 값을 VORTEX 확정값으로 교체했다. **키 구조와 소유 의미는 그대로 두고 값만 갈았다**
    - bg.base `#0B0B0E` → `#101010`, bg.deep `#050506` → `#0A0A0A`, bg.raised `#15151A` → `#181818`
    - red.light `#FF2442` → `#E60D15`, red.fill `#D90429` → `#80070C`, red.press `#A6031F` → `#610509`
    - red.glow `rgba(230,13,21,0.45)`. 흰 계열 `#F2F6FF`와 `#FFFFFF`는 전부 `#FDFDFD` 파생
    - blue와 steel.mid와 steel.shadow는 유지. steel의 흰 끝단만 `#FDFDFD`로 정합
  - deep과 raised는 VORTEX가 값을 안 정한 층이라 재산출했고 **presentation-v2 로컬 팔레트와 같은 값**으로 맞췄다
  - red.press는 이전 팔레트의 fill 대비 press 비율(0.76)을 그대로 옮겨 산출했다. 감으로 정하지 않았다
  - **`withAlpha`를 두고 rgba와 그라디언트와 glow를 전부 팔레트에서 계산한다.**
    색 값이 파일 안에서 정확히 한 번씩만 나온다(`DEEP` `RAISED` `BLUE` `STEEL_MID` `STEEL_SHADOW`로 상수화).
    브랜드 색이 또 바뀔 때 따로 고쳐야 하는 자리를 남기지 않는 것이 목적이다
  - **대비 재실측(값이 바뀌었으니 문서의 근거 숫자도 같이 갈았다).**
    text.primary 18.7:1, text.dim 합성 6.1:1, blue.light 9.8:1, red.fill 위 흰 텍스트 10.6:1
  - **red.light가 4.0:1로 내려갔다.** 이전 `#FF2442`는 5.2:1이었다. 대형과 UI 기준 3:1은 넘지만
    본문 4.5:1에는 못 미치므로 DESIGN 2절에 "본문 크기 텍스트 금지"를 명문화했다.
    브랜드 확정에 따른 의도적 하락이고 arena에서 red.light가 지는 자리는 전부 대형 숫자와 선과 아이콘이다
  - 갈래 B 결정과 사유를 DESIGN.md 2절 "소유 관계 결정"에 기록했다. 팀 표기(VORTEX, 김다영 주현호 윤소희)도 반영
  - 첫 페인트 예외 HEX는 arena controller brand 세 곳만 새 값으로 옮겼다.
    **presentation은 이 트랙이 안 건드리기로 한 폴더라 `#0B0B0E`로 남아 있다**(미해결에 적었다)
  - 검증: 셀프테스트 10/10, **R3 서명 205 / ef4b27d5 / JUDGE:6-1:51.833#6-1#ME 유지**, 4앱 빌드 성공

- P-C VORTEX 리스킨 V1 arena 검증과 하드코딩 정리 (확인 대기)
  - **판정 3파일(judge machine opponents) 무변경.** 색은 렌더 계층이고 서명이 그것을 증명한다
  - **하드코딩 파생값 7곳을 토큰 파생으로 고쳤다.** 전부 구 팔레트 값을 손으로 적어 둔 자리였다
    - `canvas2d/background.js` 안개 2줄(`rgba(216,226,240,...)` = 구 steel.mid), 비네트 2줄(`rgba(5,5,6,...)` = 구 bg.deep)
    - `canvas2d/fighters.js` 실루엣 채움과 다리 2줄(`rgba(21,21,26,0.96)` = 구 bg.raised)
    - `VignetteOverlay.jsx` 안쪽 스톱 1줄(`rgba(5,5,6,0)`)
    - 넷 다 토큰이 바뀌어도 안 따라오던 값이다. 이번에 안 고쳤으면 구 팔레트가 화면에 남았다
  - **고치지 않고 주석으로 남긴 것 둘. 색이 아니라 합성 항등원이다**
    - `canvas2d/index.js`의 `rgba(0,0,0,α)`는 `destination-out` 지우개라 rgb가 버려진다
    - `three/opponent.js` 그림자 블롭의 흰색은 알파 마스크다. 머티리얼 color(bg.deep)가 색을 지므로
      rgb가 1.0이어야 틴트가 안 섞인다. 토큰 값으로 바꾸면 그림자가 물든다
  - **유니폼 밝기 재확인(계산과 화면 양쪽).** 유니폼은 steel.mid를 bg.deep으로 누른 값이라
    bg.deep이 밝아지면 함께 밝아진다. sRGB (152.7,159.7,169.8) → (154.2,161.2,171.0),
    **선형 0.3468 → 0.3539로 문턱 0.42까지 여유 0.066.** 화면 A/B에서도 중앙값 121 → 117로 사실상 무변화다.
    ARENA_SCENE 7절에 "이 값을 만질 때 bg.deep도 함께 본다"를 적어 뒀다
  - **블룸 문턱 관계 실측.** 새 레드가 어두우므로 플레어가 죽는지를 A/B로 봤다.
    플레어 감쇠를 임시로 8초로 늘려 촬영하고 되돌렸다(D0에서 쓴 방법 그대로. 800ms는 헤드리스 표본에 안 걸린다)
    - 피크 프레임 붉은 픽셀 **724(구) → 1091(신)**, 최대 붉은 RGB (194,86,99) → (200,89,97)
    - 검 영역 **문턱 초과 픽셀 123 → 100.** 계산과도 맞는다. 플레어 피크 emissive 상대휘도가
      0.4262 → 0.3193(-25퍼센트)이므로 **붉게는 그대로 읽히고 번짐만 줄었다.**
      요구가 "붉게 읽히되 블룸이 과하지 않게"였으므로 emissiveIntensity는 건드리지 않았다
  - **평시 검 영역 붉은 픽셀 0.** D0의 "레드는 사건의 색" 규칙이 그대로 산다(HUD를 숨기고 캔버스만 실측)
  - 배경: `scene.background`와 바닥과 fog와 PMREM 환경이 전부 토큰을 읽는다. 하드코딩 0.
    PMREM은 init에서 토큰으로 굽기 때문에 별도 재생성이 필요 없다.
    하늘 평균 **7.08 → 12.0/255(선형 0.0037)**. bg.base가 11 → 16으로 올랐으니 정합이고 여전히 안 뜬다
  - **순검정 픽셀 0**(캔버스 전 프레임). 순백도 페인트 0
  - HUD 레드 전 요소 확인: 팔각 코너, 램프 좌, FUI 레티클과 링, 데미지 세그먼트, 피스트 점, 점수 숫자,
    게이지 유효 구간, 상태 칩. **DOM 계산값 전수에서 레드는 `rgb(230,13,21)` 하나뿐**이고 구 팔레트 잔재 0
  - **램프 패널 실측: 좌 `rgb(230,13,21)` "나" / 우 `rgb(53,200,255)` "이탈리아 세이버".** 좌우와 라벨이 함께 맞다
  - **DOM 감사의 `rgb(0,0,0)`은 전부 페인트가 아니다.** html head div span 같은 비SVG 요소에
    `fill`과 `stop-color`의 CSS 초기값이 계산되어 잡힌 것이고 `hasAttr` 0이다.
    실제로 칠하는 SVG는 전부 토큰을 쓴다. 속성 스타일과 폭을 함께 보게 감사 스크립트를 정밀화했다
  - 2D 폴백: `?renderer=2d`로 **5점 완주(나 5 대 0)**, "호환 렌더" 칩 표시, 같은 토큰이라 색 자동 반영, 에러 0
  - three 경로: **5점 완주(나 6 대 1)**, 에러 0
  - controller: 5화면(방 코드, 권한, 캘리브레이션 2단, 플레이) 전부 도달.
    CTA `rgb(128,7,12)`, 액센트 `rgb(230,13,21)`, 배경 `rgb(16,16,16)`, 표면 `rgb(24,24,24)`, 텍스트 `rgb(253,253,253)`.
    **깊은 레드 CTA 위 흰 글자가 10.6:1이라 이전보다 오히려 읽힌다**
  - 대리 지표 **draw 43 / tex 31 / seg 6으로 D5 기록과 동일.** 색은 비용 0이라는 예상과 맞는다
  - grep: HEX는 `shared/tokens.js`와 첫 페인트 예외 3줄 밖에 0, 이모지 0, localStorage 0
  - 검증: 셀프테스트 10/10, **R3 서명 유지**, 4앱 빌드 성공, 콘솔 에러 0

- P-C VORTEX 리스킨 V2 웹캠 디버그 뷰와 패럴랙스 체감 상향 (확인 대기)
  - 증상은 "웹캠 패럴랙스가 있는데 체감이 안 된다"였다. **원인 후보 셋 중 어느 것도 아니었다**
  - **진짜 원인. `render/index.js`의 렌더러 핸들이 `setHeadSource`를 안 넘겨 주고 있었다.**
    App이 `r.setHeadSource?.(() => face.read())`로 부르는데 그 핸들에 그 이름이 없었고
    옵셔널 체이닝이 없는 이름을 조용히 삼켰다. **R5 이후 캠 지표가 렌더러에 닿은 적이 없다.**
    진폭이 작아서 안 보인 것이 아니라 배선이 끊겨 있었다. `getParallax`와 함께 두 줄을 넘긴다
  - **두 번째 결함. `faceTracker`가 dispose 뒤 영구히 죽는다.** `disposed`를 되돌리지 않아
    StrictMode의 dev 재마운트 한 번에 추적기가 못 살아난다. 두 번째 `start()`가 getUserMedia까지
    갔다가 `disposed` 가드에서 조용히 돌아서고 **상태가 `요청 중`에 영원히 멎었다.**
    고친 뒤 가짜 카메라에서 `요청 중 → 얼굴 없음` 전이가 살아났다(MediaPipe 모델까지 실제로 로드된다)
  - **둘 다 새로 만든 `?cam=1` 패널이 잡아냈다.** 패널이 "지표는 도는데 걸린 오프셋이 0"과
    "상태가 요청 중에서 안 넘어감"을 각각 화면에 내놓아서 원인이 특정됐다. 만들려던 진단 도구가
    만들자마자 제 일을 한 셈이다
  - **`CamDebug.jsx` 신설.** `?cam=1`일 때만 좌하단 두 줄
    - 추적 상태 5종 라벨, 머리 x, 요와 문턱(0.30), 안정도와 문턱(0.72)
    - **실제로 카메라에 걸린 패럴랙스 오프셋(mm와 도)**, 팽창 조건 충족 여부, 모션 감소 표시
    - 지표가 아니라 걸린 양을 내는 것이 핵심이다. 캠이 돌아도 0이면 배선이 끊긴 것이다
    - 팽창 조건은 `isFocused()` 한 곳에서만 계산한다. 패널과 트리거가 갈리면 패널이 진단을 그만둔다
    - **처음엔 세 줄로 짰다가 두 줄로 줄였다.** 71px이라 62px 클리어런스를 넘어 간합 게이지에
      8px 겹쳤다(실측). 문턱을 지표 옆에 붙여 줄을 합쳤다. 지금 51px이고 게이지 아래로 11px 여유다
    - `?fps=1`과 같은 규율(프로덕션 포함, 무애니메이션, 초당 2회 갱신)이되 **dev 자동 노출은 없다.**
      상주하면 화면을 계속 가리고 이것은 의도가 있을 때만 필요한 도구다
  - **진폭 상향.** x 0.05 → **0.09m**, z 0.03 → **0.05m**, 요는 x와 같은 비율로 0.02 → **0.036rad**.
    스무딩 시정수(6.0)는 그대로다. 멀미는 진폭이 아니라 급한 추종이 만든다
  - **화면 이동량 실측**(지표 주입, 간합 52, 상대 2.788m, 1440x900. 상대 잔발 흔들림을 표본 6장 평균으로 지웠다)

    | 조건 | 실측 | 이론 |
    |---|---|---|
    | 평행이동만 | **42.1px** | 41.5px |
    | 회전만 | **21.4px** | 26.9px |
    | 둘 다(실제 고개 기울임) | **11.4px** | 14.6px |

  - **평행이동과 요가 서로를 상쇄한다.** 고개를 돌리면 머리 x와 요가 함께 가는데 카메라가 왼쪽으로
    가면 세상이 오른쪽으로 밀리고 같은 방향 요가 그것을 되돌린다. 순수 옆이동 42px이 실제
    고개 기울임에서는 11px이 된다. 상향 전 값으로 같은 계산이 7.7px이므로 V2가 약 1.5배를 벌었다.
    **실기에서 여전히 약하면 만질 것은 `PARALLAX.yaw`다.** 셋을 같이 올리면 상쇄비가 그대로다
  - **판정 유입 금지 불변.** `engine`은 `faceTracker`를 import하지 않고 트리거는 `{ focused }`
    하나만 넘긴다. `debug()`는 패널만 읽는다. R3 서명이 그것을 증명한다
  - 1차 측정에서 **표본이 오염된 것을 잡았다.** 조건별로 오래 기다리는 사이 경기가 5점에 끝나
    렌더가 멈춘 프레임이 뒤 조건에 섞였다(표준편차 0.01이 단서였다). 조건마다 페이지를 새로 열어
    같은 시드의 같은 구간에서 다시 쟀다
  - 검증: `?cam=1` 없으면 패널 없음, 있으면 표시, fps 미터와 좌우로 공존(겹침 0),
    2D 폴백에서 "패럴랙스 해당 없음", 카메라 거부 시 "거부됨"과 경기 지속,
    **reduced motion에서 패럴랙스 전 조건 0mm 유지**, 프로덕션 프리뷰에서 `?cam=1` 동작,
    셀프테스트 10/10, **R3 서명 유지**, 4앱 빌드, 콘솔 에러 0
    (가짜 카메라 실행의 콘솔 1건은 MediaPipe 자신의 `INFO: Created TensorFlow Lite XNNPACK delegate`이고
     모델이 실제로 올라갔다는 증거다. 우리 에러가 아니다)

## 진행중
- **presentation-v2 표지 사진 제거와 로고 중앙 완료(확인 대기). 트랙 선점 해제.** presentation-v2 표지(S1Cover)만 만졌다.
  fencer 사진 패널 제거, logo_main.svg를 정중앙(가로/세로, clamp 240~520px display 스케일, PV1 평면 잉크).
  서브카피는 로고 바로 아래 중앙(absolute+keep-all 한 줄), 팀/행사 표기는 상단 모서리 유지. 색/토큰 무변경.
  - **VortexLine 제거(선언 수정):** 처음엔 유지 선언했으나 실측에서 idle 나선이 중앙 로고를 관통해 아티팩트로
    보여 걷었다. OGL 의존 빠져 번들 347→291KB.
  - **검증:** 로고 centerX/centerY = 뷰포트 중앙 실측 일치(1440/3840). 서브카피 한 줄(keep-all). 페이지 가로
    오버플로 320/1440/3840=0. 빌드 통과. (상단 모서리 라벨은 320에서 겹침 — 기존 반응형 한계, 이번 변경과 무관.)
  - 커밋 예정: [presentation] 표지 사진 제거와 로고 중앙. 푸시 안 함.
- **brand BV2-4 파트 A(남은 하드코딩 제거) 완료 + 파트 B 재검증.** 트랙 선점 해제. 푸시 안 함.

  - **파트 B는 이미 끝나 있었다**(커밋 `9895e56`). 딥다이브 구조, Apple 실측, 제품명과 slug
    변경이 그 커밋에 다 있다. 다시 만들지 않고 이 세션 체크리스트로 재검증만 했다.

  - **파트 A 착수 시 실측.** 색과 fontSize는 이미 0이었고 남아 있던 것은 굵기와 여백이었다.

    | 대상 | 착수 시 | 완료 시 |
    |---|---|---|
    | 색 리터럴(실행 코드) | 0 | 0 |
    | `fontSize` 리터럴 | 0 | 0 |
    | **`fontWeight` 리터럴** | **12건** | **0** |
    | **여백 리터럴** | `gap 10 / 12 / 2 / 24`, `paddingLeft 20`, `padding 12` 등 | **0** |
    | 옛 레드 `#E60D15` | 0 | 0 |

  - **굵기 스케일을 tokens에 세웠다**(`weight.regular ~ black`). 강릉페이 tokens의 weight와
    같은 이름과 같은 숫자다. **역할 토큰에 안 얹히는 자리를 위한 것이다** — 버튼 라벨,
    활성 내비, 탭처럼 크기는 다른 역할을 쓰면서 굵기만 따로 정하는 자리에 숫자를 손으로 적으면
    500과 600이 파일마다 흩어진다.

  - **여백은 8pt 격자로 올렸다.** 격자 밖이던 값(10, 2, 20)을 허용값으로 맞췄다
    (`spacing.unit * 1.5 / 0.5 / 3`). `margin: 0`은 리셋이라 그대로 뒀다.

  - **파트 B 재검증(브라우저 직접).**

    | 항목 | 결과 |
    |---|---|
    | 고정 방식 | **CSS sticky. GSAP pin이 아니다** — `.pin-spacer` **0개** |
    | 워프 충돌 | 워프 한복판에도 가로 초과 **-15px**(초과 없음), 헤더 top 0 유지 |
    | 고정 유지 | 워프가 멎은 상태에서 **top 92에 정확히 머물다** 섹션 끝에 해제 |
    | 제품 rename | `xr-glass` 잔존 **0**, `demo-app`은 라우트가 사라졌다는 **주석 1건**뿐 |
    | Apple 문구 복제 | **0.** 딥다이브 카피는 전부 새로 쓴 국문이다 |
    | 대비 | 7개 페이지 **209건 미달 0건**, 중간 회색 0건 |
    | 가로 오버플로 | 320 / 768 / 1440 / 3840 전부 0 |
    | clean 빌드 | 4앱 통과 |

  - **워프 중 sticky가 안 붙는 것처럼 보이는 것은 정상이다.** 워프가 도는 동안 미디어 top이
    92가 아니라 72로 읽히는데, `skewY`가 x에 비례해 세로를 밀기 때문이다(미디어 중심 x 700에서
    tan(1.7도) x 700 = 약 21px). 워프가 가라앉으면 정확히 92로 돌아온다.

  - **커밋 분리 지시를 지키지 못한 부분.** 프롬프트는 `[brand] 하드코딩 제거와 제품 rename`과
    `[brand] Apple 딥다이브 구조` 둘로 나누라고 했는데, **rename과 딥다이브는 `9895e56`에
    이미 함께 들어가 있다.** 이번 세션의 새 변경은 하드코딩 제거뿐이라 커밋도 하나다.

- **brand BV2-4 제품 딥다이브 Apple Vision Pro 문법 완료.** 트랙 선점 해제. 푸시 안 함.

  - **Apple Vision Pro를 직접 열어 쟀고 프롬프트 값 셋 중 둘이 실제와 달랐다.**

    | 프롬프트 | 실측 |
    |---|---|
    | 히어로 34px/600 | **34px가 없다.** 가장 가까운 것이 32px/700/+0.128 |
    | 딥다이브 헤드라인 56px/600/-0.28 | 있지만 **2회뿐**. 최대는 **80px/700/-1.2**(7회) |
    | 섹션 소제목 24px/700/+0.216 | **11회, 정확히 일치** |
    | "타이포 3단계뿐" | **아니다.** 헤드라인 계열만 80/56/48/32/28/24 여섯 단계 |

    **빈도를 지배하는 것은 12px 312회, 17px 94회, 21px 58회로 전부 작은 지원 텍스트다.**
    BV2-1에서 Satisfy로 확인한 것과 같은 패턴이다(큰 헤드라인 몇 개 + 압도적으로 많은 작은 글자).

  - **구조 실측은 프롬프트가 맞았다.** 문서 32,734px = 36화면. 섹션 12개 중 긴 것(1.5화면+)
    7개, 짧은 것 5개, 미디어 보유 75퍼센트. **`sticky, top 52px`, 1425x848 고정 미디어가
    긴 딥다이브에만 있고 짧은 섹션에는 없다.** 그 대비가 리듬을 만든다.

  - **우리 적용(사용자 확정: `/products` 한 페이지).** 카드 인덱스를 걷고 그 자리에
    마스크 긴 딥다이브 → 짧은 브리지 → 컨트롤러 긴 딥다이브를 넣었다. 상세 라우트는 살아 있고
    **`data-flip-id`를 고정 미디어가 이어받아 상세와의 Flip 짝이 그대로 산다.**

  - **타이포는 px를 안 가져왔다.** 우리 스케일이 이미 같은 레퍼런스에서 나왔으므로 자리의
    역할만 옮겼다. 딥다이브가 쓰는 것은 **셋뿐**이다(실측으로 확인).

    | 자리 | 우리 토큰 | 1440 실측 | Apple 대응 |
    |---|---|---|---|
    | 제품명 | display | 52.5px w800 | 80 / 56 자리 |
    | 비트 제목 | heading | 21.8px w700 | 24/700 자리 |
    | 비트 본문 | body | 16.5px w400 | 17 자리 |

  - **제품명 변경(PLAN 4절이 BV2-4에 묶어 둔 것).** XR 글라스 → **마스크**,
    모의 검 컨트롤러 → **컨트롤러**. slug도 `/product/mask`로 옮겼다.
    이름이 바뀌어 낡은 문장이 된 랜딩 관문 한 줄도 PLAN 3절 확정 문구로 같이 고쳤다.
    **구 `/product/xr-glass`는 NotFound로 떨어진다**(리다이렉트를 두지 않았다).

  - **밟은 함정 하나.** 고정 미디어가 안 붙었다. `align-items: start`인 격자에서 미디어 칸의
    높이가 제 콘텐츠 높이와 같아져 **sticky가 붙을 구간 자체가 없었다**(실측: top이 92까지
    갔다가 곧바로 밀려 올라갔다). 칸을 행 높이로 늘리고(`stretch`) **안쪽에 한 겹을 넣어
    그것이 붙게** 고쳤다. 지금은 top 92에 머문다.

  - **실측.**

    | 항목 | 결과 |
    |---|---|
    | 문서 규모 | 5,604px = **6.2화면** |
    | 긴 딥다이브 | **2.4화면 x 2**(Apple design 섹션과 같은 값), 고정 미디어 있음 |
    | 짧은 섹션 | 0.4~0.5화면 x 3, 고정 미디어 없음 |
    | 고정 | 스크롤 25~50퍼센트 구간에서 **top 92 고정 유지**, 섹션 끝에서 해제 |
    | 비트 등장 | 화면 아래 비트 opacity 0.00 → 지나가면 1.00, `once`라 되감아도 안 사라진다 |
    | 타이포 단계 | display 52.5 / heading 21.8 / body 16.5 + 아이브로우 12 |
    | 좁은 화면 | 900 미만에서 한 열로 쌓이고 고정 해제(`relative`) |
    | 대비 | 7개 페이지 **209건 미달 0건**, 중간 회색 0건 |
    | 가로 오버플로 | 320 / 768 / 1440 / 3840 전부 0 |
    | clean 빌드 | 4앱 통과 |

  - **남은 것.** 3D 자리는 여전히 크롬 플레이스홀더 프리미티브다(`MODEL_URL`이 둘 다 null).
    실제 GLB가 오면 그 한 줄만 채우면 고정 미디어가 그대로 제품을 돈다.

- **presentation-v2 PV3 레이아웃 2단 통일과 키워드 인터랙션 완료(확인 대기). 트랙 선점 해제.** presentation-v2만 만졌다.
  색/크기 무변경(PV1/PV2). 배치만. 상세는 docs/PRESENTATION_V2_FOUNDATION.md("2단 헤더 틀", "디자인 키워드 인터랙션").
  - **파트 A(커밋 `6685441`):** 컬러 시스템 2단 헤더 구조를 공용 SlideHeader(Bits.jsx) 하나로 뽑아
    STACKED였던 WHY/타겟/인터랙션/유파 + 가운데정렬 프롤로그 + 키워드/네이밍 + 워크플로우/산출물 placeholder에
    적용. 실측(1440): 전 대상 아이브로우 좌상단 (45,65) 고정, 헤드라인 우측 열, layout 전부 2COL.
    제외: 컨셉(명시, 무변경 확인), 컬러 시스템(기준), 표지/ENTER(포스터), 페인포인트(2밴드 비교 매트릭스,
    단일 헤드라인 없음, 라벨은 이미 marginX 좌열). 페이지 가로 오버플로 320/3840=0.
  - **파트 B(커밋 예정):** 디자인 키워드 인트로 둘째 줄(copy.js) 삭제 → 카드 상향(cardTop 200→130).
    방향키 포커스 확장은 기존 유지(셸 DELEGATE_IDS, handleStep 경계 false로 셸과 무충돌). 확장이 마진을 넘던
    문제(가장자리 카드/캡션 1.08배에서 marginX 17px 초과)를 **가장자리 카드 transformOrigin 안쪽 고정**으로 해결.
    세 포커스 상태 모두 카드/캡션 마진 안(45/1395, 오버플로 0). 320/1440/3840 페이지 오버플로 0. 빌드 통과.
  - 푸시 안 함(일괄 예정).
- **brand BV2-2 히어로 Satisfy 문법 재구성 완료.** 트랙 선점 해제. 푸시 안 함.

  - **Satisfy 히어로를 직접 열어 쟀다(1440x900).** 비주얼이 `video`/`img` 1440x617로
    **첫 화면의 69퍼센트**를 먹고, y 32에서 649까지라 **100vh를 안 채운다.** 카피는 비주얼
    안쪽 y 285, 즉 **비주얼의 46퍼센트 지점**에 헤드라인 60/900 → 서브 14/400 → 알약 CTA로
    **총 150px** 덩어리다. 비주얼이 끝나고 다음 섹션까지가 비어 있다.
    **셋이 같이 있어야 성립한다**(큰 비주얼 + 작은 카피 덩어리 + 안 채운 아래 여백).

  - **우리 히어로는 첫 화면의 45퍼센트만 쓰고 아래가 통째로 비어 있었다.** 이제 전폭 슬롯이
    헤더 아래 612px(뷰포트의 **76퍼센트**)를 먹고, 카피가 슬롯의 **45퍼센트 지점**에 앉고,
    슬롯 아래 띠 바닥에 팀 표기와 스크롤 단서가 눕는다. **첫 화면 채움 45퍼센트 → 100퍼센트.**

  - **슬롯은 빈 박스가 아니다.** 카드와 같은 표면(`--card-bg`, 잉크 3.5퍼센트)이고 전폭이라
    (shell 거터를 뚫는다) 판으로 읽힌다. 우하단에 이미지 자리 표기를 눕혀 카피와 안 겹친다.
    이미지가 오면 이 슬롯을 그대로 덮으면 된다.

  - **크롬 셰이더 판정: 걷는다.** 실제 GPU에서 셰이더가 정상 가동한 상태로 픽셀을 쟀다.

    | 측정 | 값 |
    |---|---|
    | 배경 밝기 | 207 |
    | 워드마크 평균 | 193 → **1.16:1** |
    | 가장 밝은 획 | 252 → 1.52:1. **배경보다 밝아 글자에 구멍이 난다** |
    | 가장 어두운 획 | 102 → 3.69:1. 본문 4.5 미달 |
    | 평면 잉크 교체 시 | **10.57:1** |

    크롬은 어두운 무대에서 밝은 면이 도는 재질이라 밝은 무대에서는 획이 끊긴다.
    헤더 워드마크와 스플래시가 이미 평면 잉크라(BV2-1) 셋이 같은 얼굴이 됐다.

  - **리본은 색이 아니라 합성이 문제였다.** `trail.js` 재질이 `AdditiveBlending`이고
    가산은 배경을 밝게만 만든다. **밝은 무대에서는 색을 잉크로 바꿔도 안 보인다**(더할 여지가 없다).
    실측: 스틸 코어 #D8E2F0가 1.38:1. 브랜드 쪽에서 일반 합성으로 바꾸고 색을 잉크로 내려
    **11.49:1**이 됐다. **arena 파일은 안 건드렸다**(돌려받은 mesh의 재질만 덮어썼다).

  - **실측.**

    | 항목 | 결과 |
    |---|---|
    | 슬롯 비율 | 320 / 768 / 1440 / 3840 전부 **76퍼센트** 일정 |
    | 워드마크 | 40 → 52.5 → 64px. BV2-1 display 스케일 그대로, 평면 잉크 |
    | 리본 | **11.49:1**. 라이트에서 보인다 |
    | 커서 | 잉크 유지 |
    | R3 워프 | 워프 한복판에도 가로 초과 **0px**, 헤더 top 0 유지, 정지 후 `transform: none` |
    | 대비 | 6개 페이지 **149건 미달 0건**, 중간 회색 0건(BV2-1 보증 유지) |
    | 가로 오버플로 | 320 / 768 / 1440 / 3840 전부 0 |
    | clean 빌드 | 4앱 통과 |

  - **사용자 판단으로 남긴 것.** 셰이더를 걷어 `@paper-design/shaders`가 brand에서
    안 쓰이게 됐다. **패키지와 `public/licenses/paper-design-shaders/`는 그대로 뒀다.**
    번들에서 코드가 빠지는 것과 패키지를 지우는 것은 다른 결정이고 배포물 라이선스에 걸린다.

- **presentation-v2 PV2 헤드라인 아이브로우 텍스트 크기 통일 완료(확인 대기). 트랙 선점 해제.** presentation-v2만 만졌다.
  색은 안 건드렸다(PV1). tokens.typography에 역할별 단일 스케일 신설(eyebrow/headline/body/caption/display),
  전 슬라이드의 개별 fontSize clamp 리터럴을 전량 제거하고 role 토큰 하나씩만 참조하게 했다.
  기준은 컬러 시스템 슬라이드. 상세 확정값은 docs/PRESENTATION_V2_FOUNDATION.md 타이포 스케일 표.
  - **실측(1440, computed):** 통일 전 헤드라인 19.4~38.4 / 본문 13.5~22.5 / display 105~137로 제각각이었다.
    통일 후 **헤드라인 전부 19.4/700, 아이브로우 21/700, 본문 15, 캡션 13.5, display 전부 135.4**로 동일.
    가장 긴 헤드라인(TARGET)이 1440에서 1350px = 콘텐츠 폭에 한 줄로 들어간다. 위계는 굵기(700 vs 400)와 여백.
  - **검증:** 페이지 가로 오버플로 320:0 / 3840:0(섹션 overflow hidden, 텍스트는 PV1보다 작아져 새 오버플로 없음).
    각 슬라이드 스크린샷으로 절제된 크기와 일관성 확인. presentation-v2 빌드 통과.
  - 커밋 예정: [presentation] PV2 헤드라인 아이브로우 본문 크기 전 슬라이드 통일. **푸시 안 함(일괄 예정).**
- **brand BV2-1 디자인 시스템 파운데이션 완료.** 트랙 선점 해제. 푸시 안 함(일괄 예정).
  `shared/tokens.js`는 안 건드렸다. `brand/src/tokens.js`가 v2 값만 재정의한다.

  - **레퍼런스를 직접 브라우저로 열어 computed style로 쟀다. 문서 요약과 달랐다.**

    | | PLAN 0절 요약 | 실측(1440) |
    |---|---|---|
    | Satisfy 히어로 제목 | 24px/900/-0.48px | 그건 **워드마크**다. 섹션 헤드라인은 **60 / 48 / 43.2px, 전부 w900** |
    | Apple 본문 | 14px | **17px/400/-0.374px**. 14px는 국가 배너 링크였다 |
    | Apple 내비 | 12px | **12px/400/-0.12px** 맞다 |
    | Apple 디스플레이 | (없음) | **56px/600/-0.28px** |

    **두 곳 다 "전부 작다"가 아니다.** 헤드라인이 48~60px로 크고 나머지 전부가 12~17px다.
    위계는 그 간극과 굵기가 만든다. **그래서 축소 대상은 헤드라인이 아니라 136px까지 가던 display였다.**

  - **레드를 바꾼 것은 대비 실측 때문이다.** 어두운 글자는 배경이 어두울수록 불리해서
    최악은 그라디언트 **최상단 #C1C1C1**이다.

    | 색 | #C1C1C1 위 | #F6F6F6 위 | 판정 |
    |---|---|---|---|
    | `#E60D15` | **2.63:1** | 4.38:1 | 대형 3.0도 미달. 채움도 버튼 경계 2.63이라 UI 3:1 미달 |
    | `#80070C` | **5.97:1** | 9.95:1 | 본문 4.5 통과. 흰 글자 10.57 |

    dim 알파도 0.55는 3.5:1로 미달이라 **0.66(4.8:1)**으로 올렸다. 다크 알파를 그대로 옮기면 깨진다.

  - **타이포 v2.** display 136 → **64px**, title 68 → **36px**, heading 28 → **22px**,
    body 18 → **17px**, hud와 eyebrow → **12px**. 아이브로우가 21px에서 12px로 내려온 것은
    다크 시절 red가 4.02:1이라 대형 예외로만 통과했기 때문인데 그 제약이 풀렸다.
    손으로 적힌 `clamp(1.5rem, 1rem + 1.9vw, 2.6rem)` 하나가 1440에서 41.6px를 내며 다른 섹션
    제목(32.8px)과 갈리고 있어서 걷었다. **헤드라인 계열은 display와 title 둘뿐이다.**

  - **실측 결과.**

    | 항목 | 결과 |
    |---|---|
    | 전 페이지 배경 | 6개 페이지 전부 `linear-gradient(180deg, #C1C1C1, #F6F6F6)` |
    | 텍스트 색 종류 | 잉크 1.0 / 0.78 / 0.66, `#FDFDFD`, `#80070C` **다섯뿐. 중간 회색 0건** |
    | 대비 | **147건 측정, 미달 0건**, 최저 4.76 |
    | 헤더 불릿 | **0건**. 현재 항목은 굵기 700 대 400과 잉크 대 dim 둘로 갈린다 |
    | 커서 | 링과 점 모두 `rgb(16,16,16)`. 라이트에서 보인다 |
    | 줄바꿈 | 전 페이지 **0건**. 가장 긴 줄 1000px(한글 약 59자) |
    | 가로 오버플로 | 320 / 768 / 1440 / 3840 전부 0 |
    | clean 빌드 | 4앱 전부 통과 |

  - **덤으로 잡은 것 둘.**
    - **`scrollbar-gutter: stable both-edges`가 좌측 15px 흰 띠를 만들고 있었다.** 브라우저가
      그 자리를 캔버스 배경으로 안 칠하고 **fixed 레이어로도 못 덮는다**(실측). 다크 시절에도
      검은 화면 왼쪽의 흰 선으로 있던 기존 결함이다. `stable` 하나로 바꿔 0px가 됐다.
      대가는 콘텐츠 중심이 7.5px 왼쪽인데 전 페이지 상수라 움직임이 없다. PITFALLS에 남겼다.
    - **스플래시 워드마크 에셋이 흰색이라 라이트에서 안 보였다.** `vortex-wordmark.svg`의 `fill`을
      `#101010`으로 맞췄다(소비처는 brand 프리로더 하나).

  - **읽기 한도를 `--measure` 하나로 모았다.** 여섯 파일이 `maxWidth: 720`을 손으로 적고 있었다.

  - **다음 세션에 넘긴다.** 히어로 재구성(BV2-2)은 범위 밖이라 히어로가 첫 화면 위쪽만 쓰고
    아래가 비어 있다. `HeroWordmark`의 liquid-metal 셰이더는 라이트에서 크롬이 배경에 녹을
    수 있는데, 헤드리스에서는 폴백(평면 잉크)이 떠서 셰이더 경로를 눈으로 못 봤다.
    **BV2-2에서 실기 GPU로 확인하고 필요하면 걷는다.**

- **presentation-v2 PV1 파운데이션 완료(확인 대기). 트랙 선점 해제.** presentation-v2만 만졌다.
  다크→라이트 반전(배경 #F6F6F6 + 잉크 #101010 전역), 브랜드 레드/블랙과 네이비 #263E5F는 컬러 시스템
  슬라이드에만 격리, 본문 SUIT + 아이브로우 Pretendard(CDN 웹폰트 로딩 확인 + 시스템 fallback),
  컬러 시스템 실측(1920에서 좌우 60 상단 78 하단 59)을 grid 토큰으로 뽑아 전역 공통 마진(아이브로우 좌상단 고정),
  표지 VORTEX를 logo_main.svg(#101010 플랫, CSS mask)로 교체(S3/S5 워드마크도 잉크 평면), 01/14 카운터 제거 +
  새로고침 첫 섹션(scrollRestoration manual). 상세 규칙은 docs/PRESENTATION_V2_FOUNDATION.md. tokens 단일 원천.
  - **검증(실측 픽셀):** 12개 콘텐츠 섹션 전부 라이트 배경 + 잉크 텍스트 확인. 네이비/레드/whiteA는 grep으로
    S6ColorSystem에만 잔존(견본 내용). SUIT/Pretendard document.fonts 로드 및 computed 적용 확인.
    가로 오버플로 320:0 / 3840:-15(스크롤바 거터). build:all 4앱 통과 + presentation-v2 단독 빌드 통과.
  - **프리뷰 페인 한계:** Lenis 셸 네비게이션이 숨겨진 프리뷰 페인에서 안 돌아(rAF 스로틀) 스크롤 이동 검증 불가.
    섹션 격리(형제 display:none) + gsap opacity 강제 해제로 각 섹션을 개별 스크린샷했다. 셸 로직 자체는 무변경.
    **주의:** 그 디버그 헬퍼가 aria-hidden 리시드 막까지 opacity 1로 올려 유파 사진을 가리는 착시가 있었다(실코드 정상).
  - **미보수(트랙 밖):** 루트 build:all이 구 presentation을 빌드(v2 미포함). 발표 경로 확정 시 한 줄 교체.
  - 커밋 예정: [presentation] PV1 파운데이션 색 폰트 그리드 시그니처. **푸시 안 함(일괄 예정).**
- **RECORDS_DB(파트 D) 완료.** 트랙 선점 해제. 커밋 `a16e4f6`

  - **진단에서 또 전제가 틀렸다.** 프롬프트는 "DB 연동은 됐는데 저장을 안 한다"의 지점을
    찾으라고 했는데 **연동 자체가 없었다.** `docs/SERVER.md` 3줄이 "Postgres, 인증,
    마이그레이션 전부 없음"이라고 선언하고 있었고 의존성도 `cors`, `express`, `socket.io`
    셋뿐이었다. **저장이 빠진 자리는 `index.js`의 범용 릴레이 루프다** — RESULT를
    열어보지도 않고 그대로 통과시키고 있었다.
    "RECORDS가 하나만 보인다"도 파트 B 이전의 증상이라 이미 없었다.

  - **결과가 흐르는 경로 전체.**

    ```
    engine.getMatchStats() + judge  →  arena App.jsx:236 link.sendResult(...)
      →  MSG.RESULT  →  server 릴레이 루프  →  controller  →  RECORDS 탭
                              ↑ 여기에 append가 붙었다
    ```

  - **쓰기는 서버가 한다.** 폰이 점수를 써넣는 경로를 열지 않았다. 조건이 "보낸 소켓의
    역할이 arena"라 폰이 result를 흉내 내도 저장되지 않는다. 서버는 값을 다시 계산하지 않는다.

  - **저장 지표는 arena가 이미 내던 것뿐이다**(사용자 확정). 폰 통계(파워, 손떨림, 가드 수)는
    폰 안에서만 계산되고 서버를 안 거친다. 저장하려면 폰이 쓰는 경로를 열어야 하고 그게
    신뢰 경계를 깨서 안 열었다. 그 값들은 결과 화면에서만 본다.

  - **신원은 httpOnly 쿠키 `vx_id`의 익명 UUID 하나다**(로그인이 없어서 종류를 안 나눈다).
    소켓 핸드셰이크가 그 쿠키를 실어 오므로 **앱이 소켓을 붙기 전에 `/api/me`를 먼저 부른다.**
    순서가 어긋나면 그 판의 기록이 주인을 못 찾는다.

  - **`DATABASE_URL`이 없으면 기록만 꺼지고 중계는 그대로 돈다.** 릴레이가 본체이고 기록은
    그 위에 얹힌 것이라 DB 때문에 데모가 멈추면 안 된다. RECORDS 탭은 그 상태를 빈 목록으로
    위장하지 않고 사유를 적는다(`/health`의 `records`가 그 값이다).

  - **실측.** 로컬에 Postgres도 Docker도 없어서 스크래치패드에 Postgres 와이어 서버를 세우고
    **진짜 `pg` 드라이버로** 붙였다(리포에는 테스트 의존성 0건). 폰 UI를 실제로 눌러 한 판을 끝냈다.

    | 항목 | 결과 |
    |---|---|
    | 1판 후 | 내 신원 기록 **1행**, RECORDS 카드 1장 |
    | **서버 재시작 후** | **1행 유지**(부팅마다 도는 스키마 재적용이 안 지운다) |
    | 2판 후 | **2행**, 최근 판이 위. 덮어쓰기 아님 |
    | 다른 쿠키로 조회 | **0행**. 쿠키 없이 조회도 0행 |
    | 구 테이블 위 마이그레이션 | 옛 행 보존, 새 컬럼은 기본값으로 채워짐 |
    | 비파괴 grep | `db.js` 실행 줄에 DROP/TRUNCATE/DELETE **0건**, 스키마 9문장 전부 IF NOT EXISTS |
    | 서명 | **13/13, 결정성 통과, 1097/c820d0f2 유지** |
    | clean 빌드 | 4앱 + server `npm ci` 기동까지 전부 통과 |

  - **Neon 실물 확인(커밋 `d5b4772`).** 사용자가 `DATABASE_URL`을 넣어 준 뒤 같은 검증을
    Neon으로 다시 돌렸다. 부팅 시 스키마 적용, 한 판 append, **서버 재시작 후 유지**,
    두 번째 판 **누적**, 다른 쿠키 조회 **0행**. 전부 통과.
    **그 김에 TLS를 조였다.** `rejectUnauthorized`를 끄고 있었는데 Neon은 엄격 검증이
    그대로 통과해서, 중간자에게 문을 열어 둘 이유가 없었다.
    **`.env`는 `server/.env`에 둔다.** 로더가 그 자리를 읽는다(`.env.example` 옆).
    `server/src/.env`에 두면 안 읽히고 기록이 조용히 안 쌓인다.

  - **배포에 필요한 것 셋.** Render server에 `DATABASE_URL`(Neon 등), `NODE_ENV=production`,
    그리고 기존 `CORS_ORIGINS`. **`NODE_ENV`를 빼면 신원 쿠키가 안 붙는다** — 폰(Vercel)과
    서버(Render)가 다른 도메인이라 SameSite=None과 Secure가 둘 다 필요하다.

  - **남은 것.** 로그인 계정이 아직 없어서 신원 승계 규칙도 없다. 계정이 생기면 그때
    `identity.js`가 그 규칙을 갖는다.

- **CONTROLLER_APP A B C 완료(실기 확인 대기), D는 이 세션에서 착수.** 트랙 선점 해제.
  커밋 `58f090d` `4209c97` `78aa779`

  - **Phase 0에서 나온 사실 하나가 이 세션의 전제를 바꿨다.** 프롬프트는 모든 UI를
    강릉페이 레퍼런스에서 가져오라고 했는데, `docs/reference/gnpay/`를 전수 확인하니
    **스플래시와 로그인 화면과 제품 히어로는 코드도 명세도 없다**(금융 앱이라 제품이 없다).
    가져올 것이 있는 것은 바텀탭 하나뿐이고 그것도 명세(DESIGN.md 229줄)만 있다.
    시스템 자체는 이미 이식돼 있었다(ScreenContainer, StatusBar, Button, BottomSheet,
    CoachMarkOverlay, usePlatform, `ig` 타이포, `radius` 별칭).
    **사용자 확정: 토큰만 강릉페이, 형태는 VORTEX 기존 화면 재사용.** 새로 정한 디자인 0건.

  - **A 스플래시.** 세션 첫 진입 1회. 워드마크 처리는 HomeScreen 것을 그대로 쓴다.
    `onDone`을 useCallback으로 넘긴 것이 요점이다. 인라인 화살표면 App이 다시 그려질
    때마다 효과가 재실행되어 대기 타이머가 처음부터 다시 시작한다(연결 로그 하나에 안 끝난다).
    reduced motion이면 페이드를 걷고 1100ms를 400ms로 줄인다.

  - **B 메인 셸.** 게스트로 시작하면 곧장 코드 입력이던 것을 바꿨다. 첫 얼굴이 페어링
    폼이면 앱이 무엇인지 말할 기회가 없다. MAIN이 홈이고 페어링은 상단 연결 액션 뒤다.
    페어링 자체와 `?room=` 자동 주입은 그대로 산다.

    | 항목 | 결과 |
    |---|---|
    | 탭바 실측 | 바 **49**, 아이콘 **24**, 레이블 **11px** (강릉페이 명세와 일치) |
    | 세 탭 텍스트 대비 | **40건 측정, 미달 0건**, 전역 최저 4.02 |
    | 320 폭 가로 스크롤 | 0 |

    대비에서 셋을 고쳤다. **red.light는 bg.base 위 4.02:1이라 본문 기준 4.5에 미달한다.**
    제품 한 줄은 20px/700으로 올려 대형 기준 3.0으로 넘겼고(아이브로우 v2와 같은 근거),
    **탭 활성 레이블은 11px라 어떤 굵기로도 대형이 못 되어** 레이블을 흰색으로 두고
    액센트를 아이콘이 지게 했다(비텍스트라 3.0 기준). 유파 배지 12px도 같은 이유로 text.dim.

    제품 히어로 이미지 자리는 비어 있다. `MainScreen.jsx`의 `PRODUCT_IMAGE` 한 줄이고
    brand `ProductViewer`가 `MODEL_URL`을 비워 두는 것과 같은 규약이다. **사용자가 줄 것.**

  - **C 다시대전 훑기.** `MSG.FOCUS`를 새로 뒀다. **SELECT와 엄격히 다른 채널이다.**
    SELECT는 확정이라 `engine.setSchool`을 부르고 FOCUS는 미리보기라 하이라이트만 바꾼다.
    한 채널로 합치면 훑는 동안 확정 없이 상대가 바뀐다.

    **하이라이트를 켤 자리가 없던 것이 진짜 걸림돌이었다.** 폰이 훑는 동안 arena는
    MATCH_END라 승패와 스코어만 떠 있고 유파 카드는 IdleScreen에만 있었다. 카드를
    `arena/src/components/ui/SchoolCards.jsx`로 뽑아 두 화면이 같은 것을 쓰게 했다.

    **다시 대전이 PERMISSION을 건너뛴다.** 캘리브레이션 자체는 이미 매 판 돌고 있었고
    (`90d8521`), 남아 있던 군더더기는 세션당 한 번이면 되는 권한 설명 화면이었다.

    3자 실측(진짜 서버 + 진짜 arena 빌드 + 진짜 controller 빌드).

    | 검증 | 결과 |
    |---|---|
    | focus sabre/epee/hungarian/mixed | 해당 카드만 점등, null이면 소등 |
    | select 확정 후 미리보기 | 0건 (확정이 미리보기를 거둔다) |
    | 폰 카드 훑기 → arena 수신 순서 | sabre, epee, hungarian, mixed, sabre 일치 |
    | `?room=` 자동 페어링 | MAIN 상단 "연결됨", 대전 시작 → SELECT 직행 |
    | CALIB 재캘리브 | MATCH_END 탈출 확인 |

  - **통합 검증.** 셀프테스트 **13/13**, 결정성 통과, **서명 1097/c820d0f2 유지.**
    judge, machine, opponents, engine, sensors, pipeline, pose **diff 0건**.
    4앱 clean 빌드(격리 트리 + `npm ci`) 전부 통과. localStorage 0건, HEX 하드코딩 0건,
    이모지 0건, 네이티브 select/date 0건.

  - **실기 확인이 필요한 것 하나.** 다시 대전의 PERMISSION 건너뛰기는 `sensorReadyRef`가
    권한 승인에서만 서므로 **헤드리스에서는 그 분기를 탈 수 없다.** 실기 폰에서
    둘째 판이 유파 확정 직후 곧장 3, 2, 1 카운트다운으로 가는지 확인이 필요하다.

  - **D(기록 DB 누적)는 다음 세션.** 프롬프트는 "저장 안 되는 경로를 찾아"라고 했는데
    **찾을 경로가 없다. 서버에 DB가 아예 없다.** `server/`는 `index.js`와 `rooms.js`뿐이고
    방 상태는 메모리 Map, HTTP 라우트는 `/health` 하나, 인증 개념이 없다. 즉 D는 수정이
    아니라 신규 백엔드다(DB 선정, 스키마, 마이그레이션, 인증 쿠키, API, 배포 변수).
    **착수 조건: DB 종류와 호스팅을 먼저 정할 것.**
    그때까지 RECORDS 탭은 이번 세션 경기를 메모리에 append하고, 앱을 닫으면 사라진다는
    것을 탭과 결과 화면 양쪽에 적어 두었다(localStorage는 금지라 대안이 아니다).

- **상대 자세 다듬기 완료(실기 확인 대기).** 트랙 선점 해제. 커밋 `7f5be78`
  - 구조는 그대로 두고 **관절 좌표와 두께 값만** 조정했다. 만진 파일은 `three/opponent.js` 하나다
  - **다리 두께.** 허벅지 78 대 종아리 52는 1.5배 차이라 굵기가 다른 막대 둘을 이어 붙인
    것처럼 보였다. **66과 60**으로 맞춰 발목 쪽으로 1.1배만 가늘어지게 했다.
    팔(46/38)보다 확실히 굵다는 위계는 유지한다
  - **오다리의 원인은 무릎이 발보다 바깥에 있었던 것이다**(무릎 x 0.210 대 발 0.255).
    정강이가 안쪽으로 꺾여 O자가 됐다. 무릎을 발 안쪽으로 들이면 골반에서 무릎까지가
    바깥으로 벌어지고 무릎에서 발까지는 거의 수직이 되어, 같은 좌표로 **앙가르드의 무릎 벌림이
    오히려 살아난다.** 5포즈 전부 같은 규칙으로 정리했다
  - **앙가르드는 무게중심 하강과 발 벌림으로 만든다.** 정면 빌보드라 옆으로 트는 데 한계가 있다.
    골반 0.520 → **0.548**, 발 0.255/0.745 → **0.230/0.770**. 허벅지와 정강이 길이를 같게
    맞춰(각 0.202) 사람 다리 비례가 되게 했다. 런지는 뒷무릎을 골반과 발을 잇는 직선에 얹어
    뒷다리가 곧게 펴지고, 앞무릎은 안쪽 위로 접혀 깊은 굽힘이 읽힌다
  - **마스크.** `GEAR_BLUR` 2 → **1**, 메시 결을 굵고(3 → 4) 진하게(0.5 → 0.8) 간격을 좁혔다.
    윤곽은 **어둡고 얇게** 둔다. 예전의 steel.mid 6px 밝은 링은 블룸 문턱을 건드리고 부위
    윤곽선처럼 읽혔는데, 어두운 쪽으로 그으면 두상만 서고 밝기는 안 오른다
  - **검이 뜰 위험을 구조로 막았다.** `hand`, `shoulder`, `elbow`, `head`, `neck`은 **diff 0건**이다.
    착수 전에 5포즈 전부에서 `HAND` 상수가 `hand[0]`을 미터로 옮긴 값과 일치하는지 재서
    최대 오차 **0.5mm**를 확인했고(계약 성립), 그 좌표를 안 건드렸으므로 계약이 그대로 산다
  - **실측.**

    | 항목 | 결과 |
    |---|---|
    | 실루엣 상위 1퍼센트(선형) | 조용한 프레임 0.32~**0.398** (문턱 0.42 미만) |
    | 문턱 초과 픽셀 비율 | **0.16~0.20퍼센트**. 초과분은 실루엣이 아니라 명중 FUI와 궤적 |
    | 셀프테스트 | **13/13, 결정성 통과** |
    | clean 빌드 | 4앱 전부 통과 |
    | fps | 헤드리스 6(소프트웨어 렌더링 값). 자세 변경은 굽는 좌표라 런타임 비용 0 |
  - **사용자 실기 확인 필요.** 상대가 목각이 아니라 겨눈 펜서로 보이는지, 다리 균형과 마스크 선명도
- **비주얼 실루엣 후처리 완료(실기 확인 대기).** 트랙 선점 해제. 커밋 `31cba3a`
  - **목각 티의 원인은 부위마다 두르던 어두운 테두리였다.** `drawPart(..., bg.deep, EDGE=11)`이
    관절마다 경계를 그어 몸이 조각난 종이 인형으로 읽혔다. 전 스크린샷에 팔, 다리, 몸통, 머리가
    각각 테두리를 두른 채 겹쳐 있는 것이 그대로 찍혔다
  - **부위 구조와 그리는 순서는 그대로 두고 합성만 바꿨다.** 판정도 부위 판정 표시도 무변경이다
    1. 부위를 **합집합 마스크**로 합쳐 어깨, 팔꿈치, 무릎, 골반이 애초에 이어진 한 영역이 되게
    2. 앞뒤 톤 차이는 선이 아니라 **크게 흐린 그늘**(SEAM_BLUR 16)로
    3. 실루엣 가장자리를 **페더링**(FEATHER 3)해 하드 컷 제거
    4. 바깥은 **알파가 퍼지는 헤일로**(HALO_BLUR 16, alpha 0.12)로 흡수
  - **헤일로는 밝기가 아니라 알파다.** `destination-over`로 몸 뒤에 깔아 몸 위에 더해지지 않는다.
    그래서 블룸 천장이 유지되는 정도가 아니라 **내려갔다**. 마스크에 두르던 steel.mid 6px 링을
    걷어낸 것이 컸다. 그 링이 실루엣에서 가장 밝은 점이면서 동시에 목각 티의 윤곽선이었다
  - **블러는 로드 시 포즈 5장을 구울 때만 돈다. 런타임 비용 0이다**(성능 제약 준수)
  - **실측(같은 시드, 같은 클립, 같은 프로브로 전후 비교).**

    | 항목 | 후처리 전 | 후처리 후 |
    |---|---|---|
    | 실루엣 상위 1퍼센트(선형) | 0.32~**0.585** | **0.398 고정** |
    | 문턱 0.42 초과 픽셀 비율 | 0.24~**1.65퍼센트** | **0.18퍼센트** |
    | fps(헤드리스 동일 조건) | 5 | **5** (차이 0) |
    | 셀프테스트 | 13/13 | **13/13, 결정성 통과** |
    - 문턱을 넘는 프레임이 남아 있으나 **그것은 실루엣이 아니라 명중 FUI 글자와 궤적이다**
      (프레임 21 스크린샷에서 파란 `명중` 텍스트로 확인). 궤적은 원래 블룸의 전유물이다
  - **fps 5는 헤드리스 소프트웨어 렌더링이 블룸 체인을 도는 값이지 이 변경의 비용이 아니다.**
    stash로 후처리 전 코드를 같은 조건에서 재서 5로 동일했다. 2D 폴백은 54다.
    실기 게이트는 발표 노트북에 남는다
  - **찌를 때와 예고에 칼날을 세운다.** 상대 검은 `flareColor` 없이 붙였으므로 `setFlare`가
    색은 안 건드리고 밝기만 올린다(idle과 flare 둘 다 steel.mid). red는 사건 전용 그대로다
  - **2D 폴백에서 한 번 헛디뎠다가 고쳤다.** 테두리만 걷었더니 `bg.raised` 채움이 배경에 잠겨
    몸이 통째로 안 보였다(스크린샷). 테두리가 형태를 세우고 있었던 것이라, three와 같은 계산으로
    채움을 `darken(steel.mid, 0.3)`으로 밝혀 선 없이 실루엣이 서게 했다
  - **모션 감소는 같은 굽기를 0.45배 강도로 한다.** 글로우와 페더가 최소이고 목각 티는 여전히 없다
  - **실루엣 스타일 확정.** 발표에서 "의도적 추상화된 검객 실루엣"으로 설명할 수 있다.
    사실적 3D 캐릭터가 아니라 하나의 발광체이고, 그 선택이 크롬 스틸 정체성과 블룸 예산 양쪽에
    맞물려 있다는 것이 근거다
  - **사용자 실기 확인 필요.** 상대가 하나의 실루엣으로 보이는지, 찌를 때 검이 명확한지
- **brand R4 이후 Vercel 배포 미반영 진단 완료(조치 대기).** 트랙 선점 해제. 커밋 `a09ccc2`
  - **빌드 실패가 아니다. 코드에 고칠 것이 없다.** 세션 전제였던 "R4 커밋이 빌드를 깼다"는
    성립하지 않는다. 원인은 **Vercel 배포 쿼터 소진**이고 커밋 상태에 그대로 적혀 있다.

    ```
    failure  Vercel – brand  Deployment rate limited — retry in 24 hours.
    ```
  - **조회 한 줄이 답을 냈다.** `gh api repos/<owner>/<repo>/commits/<sha>/status`
  - **레이트리밋은 R4보다 4시간 반 먼저 시작됐다.** 첫 발생이 `68658b7` **13:28**이고
    R4 첫 커밋은 18:05다. 그 뒤로 brand 배포는 대부분 막혔고 슬롯이 간헐적으로 열려
    14:17, 15:00, 16:18, 16:47, **17:49(R3)** 다섯 번만 통과했다.
    **R3가 성공한 것이 예외였고 R4가 막힌 것이 평상 상태였다.** "R3까지 성공, R4부터 실패"는
    가짜 상관관계다
  - **배수가 진짜 원인이다.** 리포 하나에 Vercel 프로젝트가 **4개**(brand, arena, ppt, 루트
    `kdm-gen-ai-team-1`) 붙어 있어 푸시 한 번이 배포 4개를 태운다.
    오늘 커밋 61개 x 4 = **244 시도**가 Hobby 하루 한도 100을 넘겼다

    | 프로젝트 | 성공 | 레이트리밋 | 빌드실패 |
    |---|---|---|---|
    | brand | 11 | 20 | 24 |
    | arena | 37 | 18 | 0 |
    | ppt | 35 | 20 | 0 |
    | kdm-gen-ai-team-1 | 33 | 22 | 0 |
  - **brand의 빌드실패 24건은 전부 10:02~13:21 구간이다.** three dedupe 시절의 옛 실패이고
    이미 고쳐졌다. **13:21 이후 빌드 실패는 0건이다.** R4와 아이브로우 커밋은 빌드를
    시도조차 못 했다
  - **코드에 원인이 없다는 것도 실측으로 닫았다.**

    | 검사 | 결과 |
    |---|---|
    | R3 이후 import 변경 | **0건** (dedupe도 대소문자도 원인이 될 재료가 없다) |
    | R3 이후 설정 파일 변경 | **0건** (vercel.json, package.json, vite.config, lockfile 전부) |
    | 격리 트리 `npm ci` + 빌드 | 통과, exit 0 |
    | **대소문자 구분 볼륨** 빌드 | 통과, 산출물 해시까지 동일(`index-SGL-m87L.js`) |
    | import 문자열 대 `git ls-files` 바이트 대조 | 불일치 0건 |
    | `resolve.dedupe: ['three']` | 이미 들어 있다 |
  - **남은 조치는 우리 손 밖이거나 검증이 불가능하다.** 24시간 대기가 기본이고,
    되풀이를 막으려면 안 쓰는 Vercel 프로젝트 연결을 끊어 배수를 줄여야 한다(대시보드 작업).
    각 앱 `vercel.json`에 `ignoreCommand`를 거는 방법도 있으나 **brand는 `shared/`와
    `arena/`를 import하므로 자기 디렉터리만 보면 오늘 아이브로우 커밋 같은 변경을 놓친다.**
    레이트리밋이 풀리기 전에는 검증이 안 되므로 이 세션에서 넣지 않았다
- **아이브로우 굵기 700 대비 수정 완료(확인 대기).** 트랙 선점 해제. 커밋 `c14e411`
  - R4에서 보고한 미달 1건을 닫았다. `shared/tokens.js`의 `typography.eyebrow.weight`
    한 값이 전부다. 컴포넌트는 그 값을 읽기만 하므로 코드 변경 없이 주석만 현행화했다
  - **근거.** 아이브로우 영문은 red.light이고 bg.base 위 4.02:1이다(DESIGN 2절 기록값).
    WCAG 대형 기준은 24px 이상이거나 **18.66px 이상이면서 굵기 700 이상**이라 21px라도
    600은 대형으로 안 쳐서 본문 기준 4.5:1을 요구받고 0.48 모자랐다.
    700이면 21px가 대형에 들어 기준 3.0으로 통과한다
  - **닿는 범위를 먼저 확인했다.** `typography.eyebrow`를 실제로 읽는 곳은 brand의
    `Eyebrow.jsx`와 `Experience.jsx`(size만, weight는 이미 700 하드코딩) 둘뿐이다.
    arena와 controller와 presentation은 shared tokens를 재수출하지만 이 키를 안 읽고,
    presentation-v2는 자체 `tokens.js`라 안 닿는다(P1 트랙 그대로)
  - **실측.**

    | 항목 | 결과 |
    |---|---|
    | 굵기 | 6개 페이지 아이브로우 **18개 전부 700**, 600 잔여 0 |
    | 대비 | 검사 18개 **미달 0**. red.light 13개가 4.02 대 기준 3.0 통과, text.dim 5개가 6.14 대 4.5 통과 |
    | 뭉개짐 | dpr3에 4배 확대해 600과 700을 같은 자리에서 비교. E/P/R/C와 ㅎ/ㅁ의 속공간이 다 열려 있고 획이 안 붙는다 |
    | 폭 | 320/768/1440/3840 가로 overflow 0 이하 |
    | clean 빌드 | 4앱 전부 격리 트리 `npm ci` 후 통과 |
  - **DESIGN 4절 표와 REBOOT_PLAN 2.2를 700으로 개정하고 사유를 함께 적었다.**
    굵기를 낮추면 그 순간 대비가 미달로 돌아가므로 값과 이유를 붙여 뒀다
- **brand R4 제품 재편과 Kew 상세 완료(확인 대기).** 트랙 선점 해제. 커밋 `06720ca` `510d66c`
  - **파트 A. 제품 2종.** `copy.js`와 `ProductViewer.jsx`의 `MODEL_URL` 둘만 고쳤다.
    라우트는 App이 `PRODUCTS.cards.map`으로 만들므로 카드에서 빼면 자동으로 사라진다
    (실측: `/product/demo-app` → 404 화면)
  - **흡수는 한 문장이었다.** 데모 앱 문구와 기존 EXPERIENCE를 대조하니 셋이 이미 겹쳤고
    (방 코드 연결은 `body` 뒷문장, 3단계는 `steps`, 노트북과 폰도 `body`)
    없던 것은 **"설치 없이 브라우저에서"** 하나뿐이라 그 한 가지만 `EXPERIENCE.body` 앞에 붙였다.
    새 자리는 짓지 않았다. `PRODUCTS.index.line`은 이미 `PRODUCTS_INDEX_LINE_TODO`라 그대로 뒀고,
    관문 한 줄은 데모 앱을 부르고 있어 남은 둘의 이름으로 다시 썼다
  - **파트 B. Kew 무배경.** 뷰어 판(`bg.raised`)과 라운드를 걷어내 3D가 페이지 배경 위에 직접 뜬다.
    상세 전체에서 판과 보더가 남은 것은 `제품군으로` 버튼과 CTA 둘뿐이고 그건 장식이 아니라 형태다
  - **대비를 오프셋이 아니라 배치로 지켰다.** 3D가 상시 autoRotate라 밝은 크롬 면이 글자 뒤를
    지나가는 순간에만 대비가 무너지는데, 격자 열은 서로 겹칠 수 없으므로 3D를 왼쪽 열,
    정보를 오른쪽 열에 두는 것 자체가 회전 각도와 무관한 보증이 된다
  - **실측이 두 번 설계를 고쳤다.**
    1. 위계 블록을 왼쪽 열 안에 두니 세로를 먹어 3D에 **240px만** 남았다. 격자 밖으로 빼서
       남는 세로를 3D 몫으로 돌렸다(378px). 무대 바닥 893으로 900 화면 안에서 안 잘린다
    2. Flip이 무대를 absolute로 띄우자 격자에 남는 항목이 정보 열뿐이라 **전환 내내 첫 열로
       튀었다**(실측 스크린샷). `grid-column: 2`로 자리를 못박아 잡았다
  - **Flip은 무대가 그대로 받는다. 크로스페이드 폴백은 필요 없었다.** `data-flip-id`를
    `.vx-kew-stage`로 옮기니 정방향 22~25개, 역방향 20개 중간 사각형이 나온다
  - **실측.**

    | 항목 | 결과 |
    |---|---|
    | 제품군 | 제목 `두 개의 장치`, 카드 2, flip-id 2, `/product/demo-app` 404 |
    | 흡수 | /experience 본문에 `설치 없이 브라우저에서 겨룬다` 확인 |
    | 무배경 | 무대 배경 `rgba(0,0,0,0)`, 보더 0, 라운드 0, 그림자 none, 캔버스 투명 |
    | 겹침 | 1440/768/320 전부 3D와 우측 텍스트 교집합 **0**(검사 텍스트 13개) |
    | Flip | 정방향 두 제품 다 카드 사각형에서 출발해 무대 사각형에 착지, 역방향도 성립 |
    | WebGL | 상세당 1, 왕복 3회 누적 0(생성 3 손실 3, 복귀 시 canvas 0) |
    | OrbitControls | `touch-action: pan-y` 유지, autoRotate 기본 회전 / 모션 감소 정지 |
    | 탭 | ArrowRight로 선택과 포커스 이동, `aria-selected` 전환, 포커스 링 2px solid red, 뷰어는 같은 캔버스 유지 |
    | 대비 | 23개 검사에 미달 1건(아래) |
    | 폭 | 320/768/1440/3840 가로 overflow 0 이하 |
    | fps | 프로덕션 프리뷰 상세 2종 60, /products 60, /experience 60, pageerror 0 |
    | clean 빌드 | 4앱 전부 격리 트리 `npm ci` 후 통과 |
  - **미달 1건은 이 세션이 만든 것이 아니다.** 아이브로우(`XR 글라스`)가 red.light 21px
    weight 600이라 **4.02:1**이다. WCAG 대형 기준은 24px 이상 또는 18.66px 이상이면서 굵기 700
    이상이라 600은 대형으로 안 쳐서 4.5 기준에 0.48 모자란다. DESIGN 2절이 red.light를
    4.0:1로 이미 기록해 두고 "대형과 UI 전용"이라 적어 둔 그 값이다.
    닫는 길은 둘. **굵기를 700으로 올리면**(21px라 대형 기준 3.0이 적용돼 통과)
    또는 아이브로우 색을 text.primary로 바꾸면 된다. 다만 REBOOT_PLAN 2.2가
    "weight 600 세미볼드 유지"를 확정해 둔 전역 결정이라 이 세션에서 임의로 뒤집지 않았다.
    아이브로우는 전 페이지 공용이라 고치면 brand 전체가 함께 바뀐다
- **brand R3 랜딩 리부트 완료(확인 대기).** 트랙 선점 해제. 커밋 `42a3e3d`
  - 만진 것은 `ScrollWarp.jsx`(신설), `App.jsx` 배선, `WorldScene.jsx` pinType,
    `HeroTrail.jsx` 표시 한 줄, `Landing.jsx` 관문, `index.css`, `theme.js` 일곱이다.
    새 라이브러리 없이 Lenis와 GSAP만 썼다
  - **단계 0 실측이 설계를 바꿨다.**
    1. **우리 Lenis는 네이티브 스크롤이다.** html에 `lenis` 클래스만 붙고 body/html
       transform은 none, 휠에 `window.scrollY`가 0 → 331 → 600으로 실제로 움직인다.
       lusion처럼 가상 이동 래퍼가 없으므로 **skew를 걸 대상을 새로 세워야 했다**(`.vx-warp`)
    2. **히어로 리본 idle은 이미 돌고 있었다.** 입력 없이 캡처한 3장이 전부 다르고
       마우스를 멈춘 1.5초 뒤에도 계속 움직인다. 손댈 것이 없었다.
       (처음에 `--disable-gpu`로 띄운 브라우저에서 "정지"로 읽혔는데 WebGL이 없어
       HeroTrail이 스스로 빠진 상태였다. 소프트웨어 GL로 다시 띄워 바로잡았다)
    3. **월드빌딩 pin은 문서 900에서 2700까지**(pin-spacer 높이 2700, 뷰포트 2화면)
  - **최대 리스크는 실제로 터졌고 실측으로 잡았다.** `main`에 skewY(3deg)를 거는 순간
    pin 중이던 섹션(당시 `position: fixed`)이 뷰포트 top 0에서 **-1836으로 날아갔다.**
    transform 조상이 fixed의 기준을 뺏어 가기 때문이다. 셋으로 나눠 풀었다.
    헤더·커서·프리로더는 래퍼의 형제로 남기고(전부 fixed),
    pin은 `pinType: 'transform'`으로 바꾸고,
    밖으로 낼 수 없는 리본 캔버스(히어로 안 absolute다)는 같은 각을 빼서 되돌렸다
  - **텍스트 선명도는 속성을 지워서 지킨다.** 0을 쓰면 합성 레이어가 남는다.
    정지 판정에서 `transform`과 `will-change`를 인라인에서 삭제한다
  - **실측.**

    | 항목 | 결과 |
    |---|---|
    | 워프 거동 | 최대 2.95도(상한 3), 저속(휠 100) 0.000도, 정지 후 인라인 `transform`/`will-change` 둘 다 빈 문자열 |
    | 텍스트 선명 | 워프 전 렌더와 워프 뒤 정지 렌더의 캡처 **바이트 동일**(흐림 잔여 0) |
    | fixed 3종 | skewY(2.73도) 순간 헤더 top 0 left 15 그대로, 커서 자기 행렬 유지, 리본 합성행렬 `[1,0,0,1]` 정확한 항등 |
    | pin 정확도 | 문서 900~2700 전 구간 top 0. 워프 중 편차 [-21, 0]이고 -21은 그 프레임 기울기(1.70도)의 전단량과 일치. 워프 무력화하면 [0, 0] |
    | 관문 호버 | 제목 51 → 59(8px), 이미지 자리 0.55 → 1.00, 커서 링 0.20 → 1.00, 뗀 뒤 전부 복귀 |
    | 관문 선 | 보더·밑줄·헤어라인 **0건** |
    | 히어로 idle | 워프 적용 후에도 입력 없이 3장 전부 다름 |
    | 모션 감소 | 워프 계산값 `none`, 제목 이동 0, 밝기만 0.55 → 1.00 |
    | 이동 후 위치 | /products 0, /duelists 0, 뒤로 가기 0, 워프 잔여 0 |
    | fps | 프로덕션 프리뷰에서 히어로 58 / 스크롤 중 59 / pin 60 / 관문 60. **워프 전 빌드와 최대 1 차이**(같은 브라우저 같은 조건 비교) |
    | 폭 | 320/768/1440/3840 정지와 최대 기울기 양쪽에서 가로 overflow 0 이하 |
    | 인계 | 스플래시 `[53,378,417]` 대 히어로 글리프 상자 `[53,378,416]`. R2와 같은 값(래퍼가 상자를 안 밀었다) |
  - **남은 것.** 워프 중 문서 높이가 전단량만큼 늘어난다(1440에서 +14, 3840에서 +98).
    Lenis는 ResizeObserver로 border box만 보므로 한계가 흔들리지 않고 실측 이상은 없었지만,
    바닥 근처에서 아주 빠르게 굴릴 때의 실기 느낌은 발표 노트북에서 한 번 확인할 값이다
  - **관문 카드에 이미지 자리를 새로 뒀다.** 호버 사양이 "이미지 자리 밝기 상승"이라
    그 자리가 있어야 했다. 제품 인덱스와 같은 문법(박스 없이 `이미지 첨부 예정` 문구,
    `.vx-card-visual`)이라 좁은 화면에서는 같이 접힌다
- **brand R2 프리로더와 스플래시 완료(확인 대기).** 트랙 선점 해제. 커밋 `fc829bd` `25dffe3`
  - 만진 것은 `Preloader.jsx`(신설), `App.jsx` 배선, `index.css`, `theme.js`, `index.html` 다섯이다.
    **히어로 내부와 다른 페이지는 안 만졌다.** 새 라이브러리 없이 GSAP만
  - 워드마크 SVG는 리포 루트 `VORTEX.svg`가 사양과 일치해(viewBox 0 0 570 108, 흰 채움 path 1개)
    `brand/public/brand/vortex-wordmark.svg`로 반입했다. clean 빌드 dist에 실리는 것 확인
  - **진행률은 진짜다.** 폰트, 워드마크 SVG, 본문 마운트, window load 넷을 센다.
    가짜 타이머가 아니라 실제 로드에 묶여 있다
  - **최소 표시를 네비게이션 기준으로 잰다(고쳐 잡은 것).** 효과 실행 시점으로 재면
    모듈 파싱 시간만큼 앞당겨져 사람이 보는 시간이 **979ms로 미달했다.**
    `performance.now()`가 곧 네비게이션 이후 시간이라 그 값을 그대로 견주고,
    마지막 구간 이징도 none으로 바꿨다(power2.out은 끝값의 99.5퍼센트에 지속의
    93퍼센트에서 닿아 카운터가 100을 일찍 보여 준다). 고친 뒤 1212ms
  - **인계 정확도가 이 세션의 핵심이었다.** h1 상자를 그대로 쓰면 line-height 여백 때문에
    스플래시가 **16px 위에 서서 워드마크가 둘로 보였다**(실측 스크린샷으로 잡았다).
    우리 SVG viewBox가 글자에 딱 붙은 캡 상자라 히어로 쪽도 canvas TextMetrics의
    `actualBoundingBox*`로 같은 기준의 상자를 구한다. 착지 오차 1px
  - **첫 페인트 흰 번쩍임도 잡았다.** 스타일시트가 붙기 전 한 프레임이 흰색이었다.
    `index.html`에 배경 한 줄을 인라인으로 뒀다(부트스트랩 예외, index.css와 같은 지위).
    슬라이스 초기 opacity도 CSS가 쥔다(GSAP set 전 한 프레임 노출 방지)
  - **실측(프로덕션 프리뷰, 헤드리스).**

    | 항목 | 결과 |
    |---|---|
    | 카운터 100 도달 | 1212ms (네비게이션 기준, 최소 1200 요구) |
    | 진입 | 기본 3.7초 / WebGL 차단 3.8초(상한 5초 안) / 모션 감소 0.58초(1초 미만 요구) |
    | 착지 | 스플래시 `[53,378,417]` 대 히어로 글리프 상자 `[53,378,416]` |
    | 이중 노출 | 크로스페이드 한복판(판 0.59)에서 워드마크가 하나로 읽힌다(픽셀 확인) |
    | 재생 1회 | 라우트 3회 이동에 재등장 0건 |
    | 종료 후 | 오버레이 잔존 0, 스크롤 정상(600 이동 확인) |
    | 폭 | 320/768/1440/3840 카운터와 워드마크 화면 안, 가로 overflow 0 |
    | fps | 로딩중 32 / 스플래시 53 / 직후 60 / 안정 60 |
    | clean 빌드 | 4앱 전부 격리 트리 `npm ci` 후 통과 |
  - **스크롤 잠금은 App이 쥔다.** 프리로더가 뜨는 최초 로드에만 Lenis를 세우고
    스플래시가 끝나면 푼다. 모듈 플래그를 훅 안에서 직접 읽어 의존성을 만들지 않는다
  - **남은 것.** 로딩 구간 fps 32는 헤드리스 소프트웨어 렌더링에서 셰이더가 컴파일되는
    구간이다. 오버레이에 가려 화면에는 안 보이지만 발표 노트북 실기 확인이 최종 게이트다
- **brand R1 전역 표피 리셋 완료(확인 대기).** 트랙 선점 해제. 커밋 `6f68dc5` `ac963e7`
  - **1. 마진 시스템.** 거터는 이미 한 변수를 쓰고 있었고 **진짜 불일치는 최대폭이었다.**
    Page/Section/WorldScene/Landing은 1680, ProductDetail/NotFound는 1280이라
    넓은 화면에서 좌측 시작선이 200px 어긋났다. 골격을 `.vx-shell` 한 클래스로 모으고
    `--page-gutter` `--page-max` `--section-gap` `--page-top` 넷이 값을 낸다.
    헤더 바도 같은 골격을 써서 워드마크가 페이지 제목과 같은 세로선에서 시작한다
  - **스크롤바 자리도 잡았다(예상 못 한 것).** 스크롤이 있는 페이지와 없는 페이지를
    오갈 때 콘텐츠가 가로로 **15px 튀었다**(3840에서는 중앙 정렬이 7.5px 어긋났다).
    `scrollbar-gutter: stable both-edges`로 양쪽을 항상 비워 둔다
  - **제품 인덱스 첫 화면 완결.** 격자가 남는 세로를 먹고 카드 안 비주얼 자리가 늘어난다.
    **320에서는 그 자리를 접는다.** 안 접으면 줄 높이가 안 남아 카드끼리 글자가 겹쳤다
    (첫 시도 스크린샷에서 실제로 겹쳤다. 고치고 재측정)
  - **2. 아이브로우 v2.** 불릿 원 제거, caption 13px에서 **21px**로(+8px), weight 600 유지.
    크기는 `shared/tokens.js`의 `typography.eyebrow`가 쥔다.
    개별 구현 셋(랜딩 관문 카드, 제품 상세 제품명, 상세 탭의 레드 점)을 컴포넌트로 통합
  - **3. 선 제거.** 히어로 검끝 실선, 헤더와 시트 헤어라인, 카드 보더, 뷰어와 정보 패널 테두리,
    유파 카드와 사진 테두리, 인용 왼쪽 선, 체험 번호 원, 상세 탭 테두리.
    **아웃라인 버튼의 테두리는 남긴다.** 장식이 아니라 버튼의 형태다
  - **4. 레드 그라데이션 제거.** 히어로 배경 radial, 월드빌딩 깊이 radial,
    월드빌딩 SVG 검끝 스트로크 그라디언트. `brandGradient`는 참조처가 사라졌고
    배경 사용 금지를 주석으로 박았다
  - **5. 커스텀 커서. react-creative-cursor(MIT) 실설치 후 미채택.**
    라이선스는 통과했고 구현이 안 맞았다. **대상 요소를 마운트 시점에 한 번만 모아서**
    라우터 SPA인 우리 구조에서는 호버가 어디에도 안 걸린다(마운트 뒤 태그한 요소는
    라우트 6회 이동 뒤에도 리스너 0건). 클릭 수축도 소스에 없다. 터치와 모션 감소 가드도 없다.
    자체 구현은 **이벤트 위임**이라 나중에 생긴 요소도 걸리고 리스너가 안 자란다.
    판정 후 패키지는 제거했다. 사유 전문은 LIBRARIES 25번
  - **실측 요약(전부 헤드리스 실제 픽셀).**

    | 항목 | 결과 |
    |---|---|
    | 좌우 여백 | 6페이지 x 4폭 전부 좌우 동일, 헤더 워드마크도 같은 세로선 |
    | 제품 인덱스 | 5개 뷰포트에서 세로 넘침 0, 카드 3장 전부 첫 화면, 겹침 0 |
    | 아이브로우 | 5페이지 전부 21px / weight 600, 컴포넌트 내부 원형 요소 0 |
    | 배경 채도 | 채도 0.05 미만이 히어로 98.5%, 나머지 95~99.6%. 0.30 이상은 0.3~2.8%(CTA와 글로우) |
    | 커서 | 비인터랙티브 0.20 / 인터랙티브 1.00 / 누르는 중 0.70 / 뗀 뒤 1.00 |
    | 커서 폴백 | 터치 기기 없음, 모션 감소 없음 |
    | overflow | 320/768/1440/3840 x 6페이지 전부 가로 0건 |
    | clean 빌드 | 4앱 전부 `git archive` 격리 트리에서 `npm ci` 후 통과, 교차 오염 0 |
  - **남은 것 둘.** (a) 헤더 현재 페이지 표시의 레드 점은 남겼다. 아이브로우 불릿과
    같은 문법이지만 이건 장식이 아니라 현재 위치 표시라 굵기와 색과 함께 상태를 낸다.
    빼려면 별도 판단이 필요하다. (b) **Vercel 배포 확인은 사용자 몫이다**(자격 없음)
- **가드 재설계 완료(확인 대기).** 트랙 선점 해제. 커밋 `1b8ebc5` `ffc35c5`
  - **확정 조작.** 이게 최종이다
    ```
    폰 세로 (단자 아래, 화면 몸 쪽)   앙가르드. 이산 이벤트 없음
    좌우로 기울임 (아무 쪽이나)        가드 (guard_on). 세로 복귀에 guard_off
    앞으로 눕히며 지름                찌르기 (가속 감지, 현행 유지)
    ```
  - **각도 규칙은 `shared/pose.js` 한 곳에 있다.** controller의 실기 판정과 arena의
    `?posetest=1` 미리보기가 같은 함수를 읽는다. 두 곳이 각자 구현하면 미리보기가
    실기와 다른 말을 하게 되고, 그러면 미리보기가 오히려 사람을 속인다
  - **축 분해는 오일러가 아니라 칼끝 벡터의 구면 좌표다.** 오일러는 순서에 따라 값이 달라지고
    짐벌락에서 두 축이 엉킨다. `roll = asin(x)`, `pitch = atan2(-z, y)`라
    **순수 좌우는 roll만, 순수 앞뒤는 pitch만** 움직인다(10/25/45/70도 단위 검증)
  - **문턱.** 가드 켜짐 좌우 25도 / 풀림 15도 / 우세 축 여유 10도.
    **정지 조건을 두지 않는다.** 가드는 반응이라 홀드 대기를 걸면 지연이 그대로 보인다
    (MOTION 1절, ThreeRenderer의 GUARD_IN_SEC 55ms와 같은 이유)
  - **찌르기 임계 18에서 8로.** 여기가 이번 트랙에서 제일 큰 발견이다.
    임계는 EMA(0.2, 시정수 약 83ms)를 지난 뒤의 값이라 **손이 낸 실제 크기가 아니다.**
    120ms 찌르기 펄스는 EMA를 지나며 **원 피크의 56%**가 된다. 즉 예전 18은
    **원 피크 37 m/s^2 = 3.8G**를 요구했다. 팔을 휘둘러야 나오는 값이고
    그것이 "체력 소모가 커서 테스트조차 힘들다"의 정체였다. 8이면 원 피크 17 = 1.73G다
  - **크기 관문을 내려도 오탐이 안 늘었다(실측).** 걷기와 고쳐잡기를 자르는 것은 크기가 아니라
    **수평비(1.25)와 최소지속(40ms)**이다. 임계 6/8/10/12 전부에서 오탐 시나리오 일곱 개
    (정지한 손, 떨림 큰 사람, 걷기, 빨리 걷기, 고쳐잡기, 가드 기울임, 손 내렸다 올리기) 0건.
    **크기 관문은 오탐을 막는 대신 체력만 쓰고 있었다**
  - **합성 센서 스트림 실측(motion.js를 실제로 돌렸다).**
    3초 중립 유지 이산 이벤트 **0건**, 좌우 양쪽 GUARD_ON/OFF 한 쌍씩,
    앞으로 90도 눕히며 지르는 동안(손목 좌우 흔들림 18도 포함) **THRUST 하나에 가드 0건**,
    문턱 위 25~28도 손떨림 600표본에서 상태 전환 **1회**(켜지고 유지)
  - **AI 밸런스 회복(4유파 x 5시드 20경기, 판정 무변경, 가드 대본만 교체).**

    | 가드 대본 | AI 득점 | 내 득점 | 내 승리 |
    |---|---|---|---|
    | 상시 가드 (예전 세로=가드) | **0** | 110 | 20/20 |
    | 자주 막음 26% | 8 | 106 | 20/20 |
    | 가끔 막음 11% | **30** | 103 | 20/20 |
    | 안 막음 0% | 47 | 90 | 16/20 |

    가드 점유율이 내려가면 AI 득점이 단조로 올라간다. **세로가 중립이 되면서 AI가 다시 위협이 된다.**
    새 조작에서 가드는 능동적 동작이라 현실적 점유율이 10~25%이고, 그 구간에서 AI가 8~30점을 낸다
  - **캘리브레이션 직후 한 틱 버그도 같이 고쳤다.** 가드가 자세를 보게 되면서
    `orientation.setBaseline`이 상대 자세를 그 자리에서 다시 계산하지 않으면
    다음 센서 이벤트까지 보정 전 각도가 남는다. 경기 시작 순간에 엉뚱한 guard on/off
    한 쌍이 판정에 들어갈 수 있었다
  - **`?posetest=1`** 좌측 패널. 앞뒤/좌우 슬라이더로 자세를 넣으면 검이 그 자세로 서고
    상태가 글자로 뜬다. 프리셋 넷(중립/가드 좌/가드 우/찌르기)은 한 번 눌러 확인.
    실측(실제 픽셀): 넷이 검 수직/좌경/우경/전방 단축으로 서고 상태 표시가 맞는다.
    앞뒤 0도에서 좌우만 쓸면 25도에 켜지고 16도까지 유지되다 15도에 풀린다.
    앞뒤 85도에서는 좌우 25도까지 올려도 가드가 아니다(우세 축)
  - **`?debug=1`에 "수평 최대"를 넣었다.** 임계와 같은 단위(평활 뒤)로 최근 최대를 2초 붙든다.
    앉아서 손목으로 한 번 지르고 그 값이 임계를 넘는지 눈으로 본다.
    좌우 roll / 앞뒤 pitch도 가드가 실제로 보는 두 축으로 바꿨다(예전 오일러 표시는
    분해가 달라서 가드가 왜 켜졌는지를 설명하지 못했다)
  - 셀프테스트 13/13, **서명 1097/c820d0f2 유지**, 4앱 빌드 통과.
    presentation과 presentation-v2 무변경
  - **커밋 분리를 하나 못 지켰다.** 손목 스케일(임계 8, 수평 최대 표시)이 별도 커밋이 아니라
    `1b8ebc5`에 함께 들어갔다. 근거는 코드 주석과 이 기록에 남겼다
- **조작 수정 셋 완료(확인 대기).** 트랙 선점 해제. 커밋 `0e5b7c3` `47ec94e` `56b8e45`
- **조작 수정 셋 완료(확인 대기).** 트랙 선점 해제. 커밋 `0e5b7c3` `47ec94e` `56b8e45`
  - **1. 검 자세 매핑을 의도한 그립에 정렬 (`0e5b7c3`)**
    - **원인은 축 대응 하나였다. 좌표계 문제가 아니다.** 캘리브레이션 그립이
      "폰 세로, 충전 단자 아래, 화면이 몸 쪽"이면 기기 +Y가 위, +Z가 몸 쪽(카메라 뒤),
      +X가 오른쪽이라 **기준 기기 좌표계가 곧 카메라 로컬 좌표계다.** 갈아끼울 것이 없다
    - 어긋난 것은 **칼날이 따라가는 축**이다. 손이 따라가는 것은 폰의 세로축
      (단자에서 상단, 곧 손잡이에서 칼끝)인데, 회전을 그대로 얹으면 칼날이 기기 -Z
      (화면 뒤쪽)를 따라갔다. **90도 어긋난 것이지 뒤집힌 것이 아니었다**
    - 실측한 수정 전 거동(그래서 "뒤틀려 보인다"였다).
      세로 → 검이 **앞**. 앞으로 90도 → 검이 **바닥**. 좌우 기울임 → **아무 일도 안 일어남**
      (칼날 자기 축 회전이라 방향이 안 변한다). 세로인 채 좌우로 돌리면 검이 옆으로 움직임
    - 수정은 고정 보정 하나다. `ThreeRenderer.js`의 `GRIP_TO_BLADE`
      (x축 +90도, `BLADE_AXIS(0,0,-1)` → 기기 세로축 `(0,1,0)`)를 폰 쿼터니언에 곱한다
    - **실측(소켓 실경로 + 실제 픽셀).** 가짜 컨트롤러가 서버를 거쳐 MSG.MOTION을 흘리고
      arena 화면을 자세마다 촬영했다. 세로 → 검 수직(세워진 가드), 앞으로 45도 → 위 앞 중간,
      앞으로 90도 → 검 전방(완전 단축), 오른쪽 30도 → 검 오른쪽, 왼쪽 30도 → 검 왼쪽.
      **좌우 반전 없음.** 세로인 채 좌우로 돌리면 칼날 방향 불변(자기 축 롤, 물리적으로 옳다)
    - **가드 문턱은 부등호가 반대였다.** `guardTiltDeg: 35`가 "기준에서 35도 이상 기울면 가드"라
      새 기준에서는 **눕힐수록 가드가 켜졌다.** 눕히는 것은 찌르기 방향이라 두 동작이
      같은 구간에서 싸웠다. `guardZoneDeg: 20`(안쪽이 가드) / `guardReleaseDeg: 30`(풀림)으로
      바꾸고 두 각을 벌려 경계 떨림을 막았다. 문턱이 하나면 쉬는 자세가 정확히 경계에 앉아
      guard on/off가 판정에 쏟아진다
    - **렌더 보간 지연은 점검했고 손댈 것이 없었다.** `POSE_BLEND_SEC 0.35`는 소스 전환용이고,
      붙은 뒤 2초면 `poseBlend`가 0.997이라 매 프레임 남은 각의 99.7%를 따라간다.
      정상 상태 잔여 지연은 각도의 **0.006%**로 사실상 0이다.
      체감 지연의 지배 항은 **30Hz 전송 스로틀(최대 33ms)**이지 렌더 보간이 아니다
    - **THRUST는 현행 가속 감지 그대로다.** 자세 매핑과 별개 채널임을 `poseSword` 주석에 재명시.
      R1 우선순위(thrust 트윈 > guard 홀드 > 쿼터니언 자세)는 위치를 트윈이, 방향을 폰이
      쥐는 기존 구조로 그대로 성립한다
    - 판정 엔진 무변경. 셀프테스트 13/13, **서명 1097/c820d0f2 유지**
  - **2. 경기 종료 시 상태 색 리셋 (`47ec94e`)**
    - **원인은 플래시 재발화 방식이었다.** App이 패턴을 넣고 10ms 뒤 null로 되돌려
      같은 패턴이 연달아 와도 이펙트가 다시 돌게 했는데, 그 null이 의존성을 바꾸면서
      **이전 실행의 정리 함수가 140ms 복귀 타이머를 지웠다.** 새 실행은 조기 반환해
      `setOn(null)`에 영영 닿지 못했다. 그래서 **첫 명중 한 번에 색이 영구히 굳었다**
    - 플래시를 `{ pattern, id }` 객체로 낸다. 매번 새 객체라 같은 패턴도 다시 돌고
      null 되돌리기가 필요 없다. `HapticFlash`는 조기 반환을 없애고 flash 하나에서
      `on`을 통째로 결정한다. null이 오면 그 자리에서 꺼진다
    - result 수신 시 플래시와 가드 표시를 함께 끈다. 마지막 명중이 경기 끝과 겹쳐도
      결과는 중립 색이다
    - **실측(폰 뷰포트 390x844, 진짜 소켓 경로로 페어링부터 결과 수신까지).**
      수정 전: 직후 `rgb(230,13,21) 0.55` / 500ms 뒤 그대로 / 3초 뒤 그대로 / **결과 화면도 그대로**.
      수정 후: 직후 `rgb(230,13,21) 0.55` / 500ms 뒤 없음 / 3초 뒤 없음 / 결과 화면 없음
  - **3. 코드 입력 시스템 키보드로 전환 (`56b8e45`)**
    - 자체 키패드(알파벳 32칸 그리드)를 걷어냈다. 칸 4개 표시는 그대로 두고
      그 위를 투명 `input`이 덮는다. 칸을 탭하면 시스템 키보드가 뜬다
    - `autocapitalize="characters"`, `maxLength 4`, `font-size 16px`
      (**16px 아래로 내리면 iOS가 초점에서 화면을 확대한다**). 혼동 문자(0/O, 1/I) 배제는
      키패드가 하던 일을 입력 필터가 이어받는다. 4자리 미만 다음 차단 유지
    - PITFALLS의 네이티브 UI 금지는 `select`와 `date`처럼 OS 위젯이 통째로 뜨는 것을 말하고
      텍스트 입력은 그 대상이 아니다. 근거를 주석에 남겼다
    - 실측: input 1개 / 키패드 버튼 0개, 칸 위 적중 요소가 input,
      `A 0 I b 7 K M X` 입력 → `AB7K`(0과 I 걸러짐, 소문자 승격, 4자리에서 멈춤).
      그 코드로 실제 페어링과 경기 진입까지 완주
  - **4. 웹캠은 코드 수정 없이 사용자 실기 확인으로 남긴다.** `?cam=1`로 arena를 열어
    추적 상태와 머리 x와 안정도 값이 실제로 변하는지 본다. 값이 움직이면 캠은 정상이고
    효과가 절제된 것뿐이다. 값이 안 변하면 그때 별도 진단
  - 4앱(arena/controller/brand/presentation-v2) 빌드 통과. presentation과 presentation-v2 무변경
- **brand Vercel 빌드 실패 수정 완료(확인 대기). 원인은 arena 파일의 `three` 해석이었다.** 트랙 선점 해제
- **brand Vercel 빌드 실패 수정 완료(확인 대기). 원인은 arena 파일의 `three` 해석이었다.** 트랙 선점 해제
  - **잘렸던 진짜 원인 줄을 재현해서 뽑았다.**

    ```
    Error: [vite]: Rolldown failed to resolve import "three"
      from ".../arena/src/game/render/three/trail.js"
    ```

  - **경로.** `brand/src/components/HeroTrail.jsx`가 `../../../arena/.../trail.js`를 직접 import한다
    (BRAND_SITE_GUIDE 8절이 지시한 재사용이다). 그 arena 파일 안의 `import * as THREE from 'three'`가
    **arena 폴더 기준으로 해석**돼 `arena/node_modules/three`를 찾는다.
    **개발 기계에는 arena를 빌드해 본 적이 있어 그게 깔려 있고 clean 체크아웃에는 없다.**
    이것이 "로컬 통과 Vercel 실패"의 정체다
  - **시점도 맞는다.** 배포가 멈춘 `83e7180`은 HeroTrail이 생기기 전 커밋이다.
    HeroTrail이 들어온 다음 커밋부터 모든 배포가 죽었다. 직전 트랙에서 확인한
    "배포가 첫 커밋에서 멈춤"과 같은 사건의 양면이다
  - **수정은 한 줄이다.** `brand/vite.config.js`에 `resolve: { dedupe: ['three'] }`.
    bare specifier를 brand 기준 한 벌로 모은다
  - **이건 이 리포가 이미 쓰던 관용구다. brand만 빠뜨렸다.** `presentation-v2/vite.config.js`에
    같은 줄이 이미 있고 주석까지 달려 있다("리본(arena)과 S3 하네스가 같은 three 인스턴스를 쓰게 한다").
    presentation-v2도 같은 arena 파일을 두 곳에서 import하는데 **격리 트리에서 정상 빌드된다**(실측)
  - **덤. `Multiple instances of Three.js` 경고가 사라졌다.** 두 폴더가 각자 한 벌씩 물던 three가
    한 벌이 됐다. 번들 1084KB에서 **926KB로 158KB 줄었다**. 런타임 경고 실측 0건
  - **의심했던 둘은 아니었다.**
    대소문자 감사 결과 상대 import **412건 전수에서 불일치 0건**이다(`git ls-files`와 바이트 대조.
    걸린 둘은 `?url` 쿼리 접미사 오탐이고 실파일이 있다).
    lockfile은 다섯 앱 전부 커밋돼 있고 vite는 lockfile과 설치본이 8.2.0으로 일치한다
  - **오염을 한 번 걸렀다.** 한 트리에서 네 앱을 이어 빌드하면 arena의 `npm ci`가
    옆 폴더에 `node_modules`를 만들어 presentation-v2가 그 덕을 본다. **앱마다 따로 푼 트리**에서
    다시 재서 진짜 상태를 확인했다
  - 검증: `git archive HEAD`로 **커밋된 것만** 푼 격리 트리에서 `npm ci` 후
    brand, arena, controller, presentation-v2 **네 앱 전부 빌드 통과**.
    격리 brand 프리뷰에서 히어로 canvas 2(리본 + 워드마크), 콘솔 에러 0
  - **presentation과 presentation-v2는 손대지 않았다**(상시 규칙). 확인만 했고 둘 다 문제없다
- **brand 구조 버그 진단 완료. 코드 버그가 아니었다. 배포본이 낡았다.** 트랙 선점 해제
  - **코드 변경 0.** 신고된 셋이 지금 리포에서 이미 의도대로다. 고칠 것을 못 찾은 게 아니라
    **고칠 것이 없다.** 없는 버그를 고치면 멀쩡한 코드가 망가진다
  - **뿌리는 하나다. 배포가 brand 첫 커밋에서 멈춰 있다.** 증상 셋이 각각 다른 버그가 아니라
    배포 하나가 안 따라온 것이다
  - **배포본 정체를 커밋으로 특정했다.** 번들에 있는 문구와 없는 문구로 구간을 좁혔다

    | 문구 | 배포본 | 들어온 커밋 | 나간 커밋 |
    |---|---|---|---|
    | `검술 도장 세계관 카피 확정 예정` | **있다** | `83e7180` 기반 세팅 | `dd1f6b9` UX 라이팅 반입 |
    | `slug: 'branding'` | **있다** | `83e7180` 기반 세팅 | `dbac9a2` 제품군 3종 축소 |
    | `체험하기` | **없다** | `dd1f6b9` UX 라이팅 반입 | 현재까지 유지 |

    **즉 배포본은 `83e7180`(brand 기반 세팅) 빌드다.** brand 커밋 17개 중 첫 번째다.
    B3 라우팅과 헤더, B4 문구, B5 3종 축소와 관문 축소, B7 유파, B8a Flip, B8b 상세 뷰어,
    스펙 문구, 햄버거, 워드마크 셰이더가 **전부 배포에 없다**
  - 크기로도 드러난다. 배포 번들 176KB 대 현재 빌드 1043KB, 배포 CSS 3875B 대 현재 6995B.
    배포 CSS에 `vx-sheet`(햄버거)도 `vx-detail`(상세 4구역)도 0건이다
  - **오진할 뻔한 함정 하나. SPA rewrite가 없는 경로까지 200을 준다.**
    `/licenses/paper-design-shaders/NOTICE`가 200이길래 배포가 최신인 줄 알았는데,
    `vercel.json`의 전량 rewrite가 index.html을 돌려준 것이었다. 아무 경로나 200이다.
    **상태 코드가 아니라 본문을 봐야 한다.** PITFALLS에 확인 절차와 함께 적었다
  - **현재 빌드 실측(로컬 dist 프리뷰).** 랜딩 관문 3개에 썸네일 0(축소 카드),
    제품군 3장 전부 `data-flip-id` 썸네일(풀 카드), 두 화면 모두 `브랜딩` 0건,
    월드빌딩 제목 `일상의 공간이 도장이 된다`와 본문 노출에 자리표시 0건,
    `/product/branding`은 `없는 주소다`, 320 / 768 / 1440 / 3840 x (랜딩, 제품군) overflow 전부 0,
    4앱 빌드, 콘솔 에러 0
  - **남은 조치는 재배포뿐이고 그건 코드 작업이 아니다.** Vercel 접근 권한이 없고
    `.vercel`은 gitignore라 이쪽에서 배포를 못 건다. **밖으로 나가는 동작이라 임의로 하지 않는다.**
    사람이 Vercel에서 이 프로젝트가 이 리포 main을 자동 배포하도록 연결돼 있는지 확인해야 한다.
    지금 상태는 첫 배포 이후 한 번도 갱신이 안 된 모양새다
  - **경계할 것.** 같은 유형이 arena, controller, presentation에도 있을 수 있다.
    각 배포 주소에 위 문자열 확인법을 한 번씩 돌려 봐야 한다. 이 트랙에서는 brand만 봤다
- **brand 워드마크 크롬 셰이더 승격 완료(확인 대기). 판정 넷 다 통과했다.** 트랙 선점 해제
  - **MetalFlow에서 막혔던 것을 대체재로 뚫었다.** `@paper-design/shaders` 0.0.78(Apache-2.0)의
    `liquidMetalFragmentShader`와 자체 런타임 `ShaderMount`를 쓴다. three도 R3F도 안 쓴다(의존성 0)
  - **게이트를 먼저 통과시켰다**(커밋 `68658b7`). `shaders`(2024-10-09)가 `liquid-logo`(2025-02-18)보다
    **넉 달 먼저다.** 라이브러리가 먼저고 PolyForm 데모가 나중이라 파생 방향이 반대다.
    게다가 저작권자가 같은 `paper-design`이라 자기 코드를 배포처마다 다르게 낼 수 있다.
    MetalFlow가 막힌 이유는 **제3자가 남의 코드를 재라이선스**했기 때문이고 여기는 그 구조가 아니다
  - **마스크 방식.** CSS로 캔버스를 텍스트에 클립할 수 없다. VORTEX를 canvas 2D로 굽고
    라이브러리의 `toProcessedLiquidMetal`로 전처리해(R = edge gradient, G = opacity) `u_image`로 넘긴다.
    **`document.fonts.ready` 뒤에 굽는다.** 먼저 구우면 폴백 글꼴 모양이 마스크에 박힌다
  - **셰이더 상수 두 줄을 바꿨다. 이게 (d)의 핵심이었다.** 원문의 밝은 스톱 `vec3(.98,.98,1.)`과
    어두운 스톱 `vec3(.1,...)`이 **유니폼이 아니라 상수**라 밖에서 못 넘긴다.
    어두운 쪽 0.1이 무대 배경 `#101010`(0.063)과 거의 붙어 글자 안 어두운 띠가 배경에 잠긴다.
    둘을 `steel.hi`와 `steel.shadow`로 치환했다. **앵커가 유일하지 않으면 치환을 포기하고 steelText로 내려앉는다**
  - **fps 판정이 단발 측정으로는 뒤집혔다. 반복 측정이 필요했다.**
    단발로는 50.3, 52.3, 42.0으로 흔들려 미달로 보였다. 픽셀 수를 1/4로 줄여도 42로 그대로였고
    1/16(103x28)에서야 54.7이 됐다. **비용이 채움률이 아니라는 뜻이라 표본을 늘렸다.**

    | 조건 | 4회 측정 | 중앙값 |
    |---|---|---|
    | 셰이더 켬 | 46, 55.8, 55.8, 56.3 | **55.8** |
    | 셰이더 끔(steelText) | 51.3, 51.8, 58.8, 60.3 | **55.3** |

    **조건 간 차이보다 조건 내 산포가 훨씬 크다.** 헤드리스 SwiftShader에서 두 조건이 구별되지 않는다.
    유체 때는 켬 23 대 끔 59로 노이즈 밖이었다. 그때 미채택하고 지금 채택하는 근거가 이 차이다
  - **판정 넷.**
    (a) 통과. 밑글자를 끄고 찍은 셰이더 단독 화면에서 크롬이 글자 안에만 있고 밖으로 안 샌다.
    (b) 통과. 합성 픽셀 실측 **평균 채도 0.092, 최대 0.256, 0.3 초과 픽셀 0개.**
    steel 팔레트 자체의 채도 상한(`steel.shadow` 0.247) 안이다. `u_shiftRed`와 `u_shiftBlue`를 0으로 박아 분산을 껐다.
    (c) 통과. 위 반복 측정.
    (d) 통과. 글자 밝기 40~253 대 배경 16, 밝은 픽셀 84퍼센트.
    **컨텍스트 랜딩 2(리본 three 1 + 워드마크 1), 제품군 0, 4회 왕복 누적 0**
  - **라이브러리 `dispose`가 컨텍스트를 안 놓는다**(설치본 `loseContext` 0건). 텍스처와 프로그램만 지운다.
    그대로 두면 왕복마다 쌓이므로 언마운트에서 `WEBGL_lose_context`를 직접 부른다
  - **생성자가 캔버스를 먼저 붙이고 나중에 던진다.** WebGL을 막았을 때 빈 캔버스가 남아서 실패 경로에서 지운다
  - 검증: WebGL 차단에서 steelText 폴백이 서고 캔버스 0에 pageerror 0,
    reduced motion에서 캔버스를 아예 안 만든다(GPU 미사용),
    sr-only VORTEX 존재하고 밑글자와 호스트와 캔버스 전부 `aria-hidden`,
    320 / 768 / 1440 / 3840 overflow 0이고 **버퍼가 글자 박스를 따라 재래스터된다**
    (155x42, 254x69, 410x111, 528x143), 4앱 빌드, 콘솔 에러 0
  - **Apache-2.0은 이 리포의 첫 배포 의무 라이브러리다.** `brand/public/licenses/paper-design-shaders/`에
    LICENSE 전문과 NOTICE와 변경 고지 `CHANGES.txt`를 두어 `dist/`로 나가는 것을 실측했다. CREDITS에 기록했다
- **brand A 히어로 vortex 유체 배경 시도 후 미채택. fps 판정 실패다.** 트랙 선점 해제
  - **코드 변경 0.** 히어로는 배경 레드 radial + 리본 + steelText 워드마크 그대로다.
    반입했던 `vendor/webgl-fluid/fluid.js`와 `components/HeroFluid.jsx`는 지웠다.
    **CREDITS 무변경**(리포에 남긴 서드파티 코드가 없다. three-particles 기각 때와 같은 규율)
  - **라이선스는 통과했다.** MIT(Copyright 2017 Pavel Dobryakov), 파생 진술 없음.
    README `References` 셋을 상류 추적한 결과는 LIBRARIES에 기록했다.
    에셋 `LDR_LLL1_0.png`는 미확인이라 안 쓰고 코드 생성 노이즈로 대체했다
  - **판정 넷 중 셋은 통과했다.**
    (a) 은은하게 흐르고 텍스트와 리본을 안 덮는다(스크린샷 확인).
    (b) 색이 레드와 무채다. 원문 `HSVtoRGB(Math.random(),1,1)` 무지개를 버리고
    팔레트를 `red.light`, `red.deep`, `steel.shadow`로 고정했다.
    (d) **컨텍스트 최대 2, 누적 0.** 랜딩 live 2(리본 three 1 + 유체 순수 WebGL 1),
    제품군 0, 4회 왕복 내내 고정. 생명주기는 완성돼 있었다
  - **(c) fps가 실패했다. 이것 하나로 미채택이다.**

    | 구성 | fps |
    |---|---|
    | 제품군(WebGL 0) | 60.0 |
    | 상세(three 1) | 58.3 |
    | **랜딩 리본만(기준선)** | **59.0** |
    | 랜딩 리본+유체 기본(SIM64 DYE256) | 23.0 |
    | 시뮬 최소(SIM32 DYE64 압력2) | 39.3 |
    | **DPR 0.35까지 낮춘 최선** | **48.3~49.7** |

  - **시뮬 해상도를 내려도 안 잡힌다.** SIM 64에서 32로, DYE 256에서 64로, 압력 반복 12에서 2로
    내려도 25.7에 머문다. **비용이 시뮬레이션이 아니라 전면 표시 패스에 있다.**
    백킹 스토어를 DPR 0.35까지 줄여야 48까지 올라가고 거기서 평평해진다(0.25로 더 내리면 43으로 되레 떨어진다)
  - **해석의 한계를 적어 둔다.** 측정은 헤드리스 Chromium이라 SwiftShader 소프트웨어 래스터다.
    실제 GPU에서는 훨씬 나을 수 있고 그것을 이 환경에서 확인할 방법이 없다.
    **그래도 미채택으로 간다.** 근거 둘이다. 첫째 판정 기준이 60fps로 명시돼 있었다.
    둘째 **상대 비용이 크다.** 의도한 화질에서 기준선의 39퍼센트(23/59)까지 떨어지는데,
    이건 일부러 잘 안 보이게 만든 배경 장식 하나가 먹는 몫이다.
    남은 프레임 예산은 히어로의 주인공인 리본이 써야 한다(DESIGN 10절)
  - **다시 하려면.** 반입 절차는 그대로 재현 가능하다. 원문 `script.js` 1646줄에서
    시뮬 본체(118~207, 283~286, 351~1166, 1176~1462, 1597~1646)만 슬라이스하고
    프로모(29~55), dat.gui(116, 208~282), 스크린샷(287~350), 애널리틱스 7곳,
    **포인터 입력(1464~1524)**, `generateColor`와 `HSVtoRGB`를 뺀다.
    부트스트랩 블록(1167~1175)을 걷으면 `lastUpdateTime`과 `colorUpdateTimer`가 같이 딸려 나가니 다시 세워야 한다.
    **먼저 실기기 GPU에서 fps를 재고 60이 나오면 그때 반입하는 순서가 맞다**
- **brand 크롬 워드마크 셰이더 시도 후 미채택. 라이선스 게이트 실패다.** 트랙 선점 해제
  - **코드 변경 0.** 히어로 워드마크는 `steelText` CSS 클립 그대로다
  - 클론은 성공했다(`Saganaki22/MetalFlow`). 파일 18개, 셰이더 전문은 `main.js` 1~8행 vertex와
    10~201행 fragment. **에셋 의존 0**(환경맵도 HDR도 없고 텍스처는 우리가 구울 마스크 하나),
    three는 r128이라 0.185로 올리려면 `CanvasTexture`와 `outputColorSpace` 두 곳만 손보면 됐다.
    즉 **기술적으로는 막힌 데가 없었다**
  - **막은 것은 README 6행이다.** "A ported fork of paper-design/liquid-logo".
    상류 `paper-design/liquid-logo`를 게이트에 걸었더니 **PolyForm Shield License 1.0.0**이고
    GitHub SPDX는 `NOASSERTION`이다. 비경쟁 조건이 붙은 소스공개 라이선스라
    MIT/Apache/ISC/Zlib/CC0 어디에도 안 든다
  - **drbaph의 MIT는 본인 기여분까지만 덮는다.** 상류가 PolyForm Shield인 코드를
    하류 포크가 MIT로 재라이선스할 수 없다. 우리가 가져오려던 프래그먼트 191줄이 정확히 그 부분이다
  - **판정 근거는 저자 본인의 진술이고 liquid-logo 저장소는 열지 않았다.** 게이트 절차인
    라이선스 조회만 했다. MetalFlow의 유니폼 이름(`u_patternScale`, `u_refraction`, `u_edge`)이
    아래 Apache 배포본의 이름(`u_repetition`, `u_softness`, `u_shiftRed`)과 전혀 달라
    구버전 liquid-logo 계열을 물려받았다는 정황과도 맞는다
  - **라이선스가 열린 대안은 있다. 채택하지 않았다.** `@paper-design/shaders` 0.0.78이
    **Apache-2.0**이고 LICENSE 전문과 NOTICE가 동봉돼 있으며 `dist/shaders/liquid-metal.js`에
    같은 계열 셰이더가 실재한다(임시 폴더 실설치로 확인, 리포에 안 남겼다).
    다만 GLSL ES 3.00이고 자체 sizing varying 체계에 얹혀 있어 vertex와 `shader-utils`까지
    같이 와야 하고 Apache-2.0 고지 의무가 붙는다. **다른 라이브러리를 새로 들이는 결정이라 임의로 안 갈아탔다**
  - **CREDITS 무변경**(미채택). LIBRARIES 판정표는 아래 오판을 정정했다
- **brand 모바일 햄버거와 헤더 반응형 수정 완료(확인 대기).** 트랙 선점 해제
  - **원인부터 없앴다.** `<nav style={{display:'flex'}}>`가 `.vx-nav`의 미디어쿼리를 이겨
    320에서 메뉴가 안 접혔다. 인라인 `display`를 전부 걷고 배치를 `index.css`로 옮겼다.
    실측: 320 / 390 / 767에서 `nav` `cta-bar` 계산 display가 `none`, 바에 남는 것은 워드마크와 햄버거 둘뿐.
    768 / 1440 / 3840은 기존 다섯 항목 그대로다
  - **같은 함정을 다시 심을 뻔했다.** CTA에 `className="vx-cta vx-cta-bar"`를 달면서
    `display: inline-flex`를 인라인에 남겨 두면 md 미만에서 바 CTA가 안 접힌다.
    **한 요소의 `display`는 정확히 한 클래스에만 둔다**는 규칙으로 정리해
    `.vx-cta-bar`와 `.vx-cta-sheet`로 갈랐다. PITFALLS에 세 번째 사례로 적었다
  - **시트는 오버레이가 아니라 헤더 드롭다운이다.** DESIGN 8절대로 오버레이 90에 두면
    헤더 50을 덮어 햄버거 재클릭이 막힌다. `<header>`가 만든 쌓임 맥락 안에서
    스크림 0, 시트 1, 바 2로 두어 바가 항상 위에 남는다. 시트는 `top: 100%`라 **바 높이를 상수로 안 적는다**
  - **바는 DOM에서 먼저 두고 z로 올린다.** 칠하는 차례는 z가 정하므로 탭 순서를
    워드마크, 햄버거, 시트 항목 순으로 사람이 누른 차례에 맞출 수 있다.
    실측 고리: PRODUCTS, DUELISTS, EXPERIENCE, 체험하기, VORTEX, 메뉴 닫기로 순환하고 **헤더 밖으로 안 샌다**
  - **md 이상으로 넓히면 닫는다.** 시트는 CSS가 `display: none`으로 지우는데 상태가 열린 채 남으면
    `<main>`의 `inert`가 안 풀려 본문이 통째로 죽는다. 실측: 1024로 넓히면 navOpen false, inert false
  - **CTA는 시트로 넣었다.** 바에 남기는 안도 실측했다. 320에서 여유가 47px라 넘치지는 않지만
    `space-between` 세 항목이라 빨간 알약이 바 한가운데에 떠서 헤더 액션으로 안 읽힌다(스크린샷).
    오른쪽으로 붙이려면 묶음 요소가 하나 더 필요하고, 시트에서는 폭을 꽉 채워 주액션으로 선다
  - **애니메이션을 CSS transition에 두어 전역 reduced-motion 규칙이 그대로 먹는다.**
    실측 추적(페이지 안에서 클릭과 표본을 붙여 돌림): 평시 opacity 0 -> 0.22 -> 0.78 -> 0.97 -> 1.00에
    translateY -12 -> -9 -> -3 -> 0으로 200ms ease-drawer 램프, reduce에서는 duration 1e-05s로 다음 프레임에 안착
  - 검증: 햄버거 터치 타깃 44x44, 시트 항목 44px, `aria-expanded` true/false 토글,
    `aria-label`이 메뉴 열기와 메뉴 닫기로 갈림, 열면 첫 항목 포커스, ESC와 바깥 탭과 재클릭과
    항목 탭(라우트 이동) 네 경로 모두에서 닫히고 **전부 햄버거로 포커스 복귀**,
    열린 동안 `<main>`이 `inert`와 `aria-hidden`, 랜딩 최상단에서 열면 바에 판이 깔림,
    320 / 768 / 1440 / 3840 overflow 0, 4앱 빌드, 콘솔 에러 0
- **brand 제품 상세 스펙과 특징 문구 반입 완료(확인 대기).** 트랙 선점 해제
  - **`brand/src/copy.js` 한 파일만 바뀌었다.** 참조부(`Overview`, `Features`)는 B8b에서
    빈 배열이면 자리표시, 아니면 목록이 뜨게 이미 갈라져 있어 컴포넌트를 손대지 않았다.
    배선 확인은 코드가 아니라 화면으로 했다
  - 3종 x 스펙 5행 + 특징 4개. OVERVIEW에 스펙표, FEATURES에 특징 목록이 실제로 뜬다(실측)
  - **`확정 예정` 값 넷은 `TODO_MARK` 상수를 쓴다.** 리터럴을 네 번 적으면 확정될 때
    고칠 자리가 넷으로 흩어진다. 미해결에 실측 필요 항목으로 남겼다
  - 검증: 3종 OVERVIEW 스펙 5행과 FEATURES 특징 4개 실측, `확정 예정` 자리 표시 확인,
    320 / 768 / 1440에서 가로 overflow 0이고 패널 자체 overflow도 0,
    금지 문자 0, 컴포넌트에 하드코딩 문자열 0, 4앱 빌드, 콘솔 에러 0
- **brand B8b 제품 상세 오버워치식 레이아웃과 플레이스홀더 360 뷰어 완료(확인 대기).** 트랙 선점 해제
  - 4구역. 왼쪽 세로 탭(OVERVIEW 스펙, FEATURES 특징), 중앙 360 뷰어, 뷰어 아래 정보 패널, 하단 CTA.
    **탭은 뷰어를 바꾸지 않는다.** 제품이 하나라 뷰어는 상시 유지된다(탭 전환 후 같은 canvas, 개수 1 실측)
  - 활성 탭은 red 점 + weight 700 + 보더. **색 단독 구분 금지라 굵기와 보더를 함께 올렸다**(DESIGN 13절).
    영문 라벨 아래 국문(스펙, 특징)을 세워 무엇이 바뀌는지 글로 읽히게 했다
  - 회전은 설치본 애드온 `three/examples/jsm/controls/OrbitControls.js`다.
    `export { OrbitControls }`를 열어 확인하고 썼다. **새 패키지 설치 0**(three는 이미 채택분).
    줌과 팬 끔, 감쇠 0.08, autoRotateSpeed 0.6(기본 2.0은 팽이처럼 돌아 형태가 안 읽힌다)
  - **모델은 플레이스홀더다.** 각진 8면체를 세로로 늘려 크롬(metalness 1, roughness 0.22)으로 띄웠다.
    반사 환경은 DESIGN 3절 크롬 스톱(hi, mid, shadow)을 세로 그라디언트로 구워 PMREM에 넣은 것이라
    **재질이 토큰에서 나온다.** 교체 지점은 `MODEL_URL`과 `createPlaceholder` 둘이고
    **GLTFLoader는 일부러 import하지 않았다**(부르지 않는 로더가 번들에 실리면 예산만 축낸다)
  - **함정 하나. OrbitControls의 `connect`가 캔버스 `touch-action`을 none으로 덮는다**(설치본 실확인).
    그대로 두면 폰에서 뷰어 위를 손가락으로 못 내려 스크롤 사이트가 막힌다.
    생성 뒤 `pan-y`로 되돌렸다. 실측(CDP 실입력): 세로 스와이프 scrollY 0 -> 205, 가로 스와이프는 회전하고 scrollY 0 유지
  - **함정 둘. 첫 셰이더 컴파일이 Flip 전환을 잡아먹었다.** 상세 진입과 뷰어 초기화가 같은 프레임 구간에
    겹쳐 800ms 동안 rAF가 2번밖에 안 돌았다(폭 411 -> 974로 한 번에 튐, 실측).
    `lib/flip.js`에 `flipSettleMs()`를 더해 **전환이 끝난 뒤에 뷰어를 세운다.**
    고친 뒤 폭 추적 411 -> 573 -> 666 -> 790 -> 872 -> 924 -> 953 -> 967 -> 974로 매 프레임 보간
  - **바뀐 게 없으면 그리지 않는다.** `controls.update(dt)`가 카메라 변화를 돌려주는 것을 설치본에서 확인하고
    그 값으로 렌더를 건다. 자동 회전이 꺼진 정지 상태의 1초 draws가 0이다(실측)
  - **핵심 리스크였던 컨텍스트 누적은 없다.** 상세 3종을 9회 왕복하는 동안 살아 있는 컨텍스트가
    상세에서 1, 제품군에서 0으로 고정이다. **프로덕션 프리뷰에서는 생성 자체가 방문당 정확히 1**이고
    dev의 2는 StrictMode 이중 마운트다(히어로와 같은 양상). 랜딩 히어로와 섞어도 1을 안 넘는다
  - 검증: 3종 4구역 렌더, 드래그와 autoRotate 회전 실측, 입력 후 자동 회전 정지 실측,
    화면 밖 1.5초 draws 0 / 화면 안 1.5초 172(약 57fps, DESIGN 9절 minFps 30 위),
    B8a Flip 정방향과 역방향 유지, WebGL 차단 시 폴백 문구와 페이지 전부 생존(pageerror 0),
    reduced motion 3초 정지에 드래그는 살아 있음, controller는 체험하기 없이 제품군으로만,
    탭 포커스 링 2px rgb(230,13,21), 터치 타깃 63px, 320 / 768 / 1440 / 3840 overflow 0,
    320에서 4구역 세로 재배치, 4앱 빌드, 전 페이지 콘솔 에러 0
  - **CREDITS에 OrbitControls 행을 더했다.** three LICENSE(MIT), package.json license 필드,
    애드온 파일에 별도 헤더 없음 셋을 확인했다. LIBRARIES 판정표는 three.js 채택 확정이 이미 있어 변경 없음
- **brand B8a 제품 카드 상세 Flip 전환 완료(확인 대기).** 전환만 했다. 트랙 선점 해제
  - **짝은 썸네일이다.** 카드 전체가 아니라 카드 썸네일과 상세 대표 비주얼이 같은
    `data-flip-id`(`product-<slug>`)를 공유한다. 카드에는 상세에 없는 문구가 있어
    통째로 모핑하면 글자가 뭉개진다. 상세 레이아웃은 손대지 않았다(기존 FEATURES 비주얼을 짝으로 썼다)
  - **상태는 모듈 변수로 넘긴다**(`lib/flip.js`). 라우트가 바뀌면 카드가 언마운트되므로
    React state로는 못 넘긴다. 떠나는 쪽이 `captureFlip`, 도착한 쪽이 `playFlip`으로 한 번 꺼낸다
  - **꺼내는 즉시 비운다.** StrictMode가 layout effect를 두 번 돌려도 두 번째가 빈손이라
    중복 재생이 없다(히어로 타임라인과 같은 함정을 다른 방법으로 막았다).
    실측: 전환 종료 후 `transform: none`, 인라인 잔여 0
  - **도착지에 후보가 여럿이면 짝을 먼저 골라야 한다.** 제품군은 카드가 셋인데 아무 카드에나
    붙였더니 Flip이 id 짝을 못 찾아 역방향 전환이 통째로 죽었다(실측). `pendingFlipId()`로
    기록된 id를 보고 그 slug의 카드에만 건다
  - **스크롤을 Flip보다 먼저 맨 위로 보낸다.** `ScrollToTop`은 useEffect라 layout effect 뒤에 도는데,
    날아가는 도중에 스크롤이 바뀌면 목표 자리가 움직인다. 양쪽 페이지 layout effect가 직접 `scrollTo(0, 0)`을 부른다
  - 지속 0.48s. **DESIGN 7절의 modal 200~500ms 구간을 근거로 잡았다.** 크로스페이드(150~200ms)가 아니라
    모달 확대에 가까운 동작이다. 슬라이드업 없음
  - 안전망 셋. reduced motion이면 기록 자체를 안 남긴다(즉시 이동, 실측),
    `getState`나 `from`이 던지면 조용히 넘어간다(실측 강제 실패에서도 상세 정상 도착),
    1200ms 지난 기록은 버린다(도착 안 한 전환이 다음 진입에 끼어들지 않게)
  - 검증: 3종 전부 전환(썸네일 411 → 상세 1206으로 보간), 역방향도 1206 → 411,
    헤더 PRODUCTS 링크로 나가면 전환 없이 정상 이동, 상세 진입 후 scrollY 0,
    없는 slug는 여전히 NotFound, 320 / 768 / 1440 / 3840 overflow 0, 4앱 빌드, 페이지 에러 0
  - **CREDITS의 GSAP 행에 Flip 용도를 더했다.** LIBRARIES 판정표의 GSAP 무료화(Flip 포함)와 일치한다.
    설치본 `gsap/dist/Flip.js`에서 export를 확인하고 썼다. **새 패키지 설치는 없다**
- **LIBRARIES 라이선스 게이트 2차 완료(확인 대기).** 문서만 만졌다. 트랙 선점 해제
  - 후보 7개 전부 라이선스 파일이 실재했다. **사용 불가 0, 열람 금지 추가분 없다**
  - **채택 가능 7.** three-story-controls(Apache-2.0), WebGL-Fluid-Simulation(MIT),
    webgl-fluid(MIT), three-js-trail(MIT), svartmc/trails(MIT),
    threejs-z-fold-gift-card(MIT), MetalFlow(MIT, 1차 판정 재확인)
  - **2차부터 R3F 의존 열을 넣었다.** 일곱 다 R3F 비의존이라 vanilla three에 그대로 얹힌다.
    셋은 `package.json`이 없는 플레인 JS 저장소이고 셋은 three를 직접 의존한다
  - **three-story-controls 불일치를 기록했다.** GitHub SPDX가 `NOASSERTION`이다.
    LICENSE가 Apache-2.0 전문이 아니라 8행짜리 짧은 고지문이라 기계 판독이 실패한다.
    본문에 "Licensed under the Apache License, Version 2.0"이 있고 `package.json` license 필드도 `Apache-2.0`이다.
    **채택 가능이되 도입 시 Apache-2.0 의무**(고지와 사본 동봉, 변경 파일 표시)를 진다고 표에 적었다.
    ogl과 segue와 ahrs와 같은 불일치 기록 규율이다
  - 로드맵 용도 한 줄씩 달았다. three-story-controls 스크롤 카메라 시네마틱,
    WebGL-Fluid vortex 유체 배경, three-js-trail 검끝 궤적 대안
  - **설치 0, 코드 변경 0, CREDITS 변경 없음.** 판정표는 게이트일 뿐 도입이 아니다
- **brand B7 유파 셀렉션 완료(확인 대기).** `/duelists` 한 페이지만 만졌다. 트랙 선점 해제
  - **메커니즘 출처.** 팀 포폴 `hyunho2378/26-portfolio-hyunho`를 clone해
    `client/src/components/work/StackCarousel.jsx` **실파일을 읽고** 가져왔다(DESIGN 15절).
    가져온 것 다섯: `circDist` 원형 최단거리, 거리 기반 dim, z 정렬 `100 - absD`,
    락 기반 한 칸 스텝(`go` + `lockRef` + 90ms 해제), 드래그 판별(6px 이동, 30px 스텝)과 `touchAction: 'pan-y'`
  - **안 가져온 것 둘.** 아치 회전(ARC_ANGLE, ARC_RADIUS, transformOrigin)과 **휠 핸들러**다.
    원본은 부채꼴이라 안무가 다르고, **brand는 일반 스크롤 사이트라 휠을 가로채면 안 된다.**
    실측으로 세로 스크롤이 그대로 산다(휠 50 → 218)
  - 안무는 제자리 포커스다. 활성 scale 1과 선명과 red 보더와 글로우, 비활성 scale 0.9와 blur 5와 밝기 0.6.
    **카드 중심 x가 [259, 720, 1181]로 전환 전후 동일하다**(좌우 위치 안 흔들림. 바운딩 박스는 축소분만큼 줄어든다)
  - 입력 셋. 클릭이 기본, 좌우 화살표와 좌우 스와이프가 보조다. 전부 실측 확인
  - **활성을 색과 blur만으로 구분하지 않는다.** `aria-selected`와 이름 굵기(700 대 600)와
    보더 색과 겨루기 버튼 노출을 함께 쓴다. reduced motion에서는 blur와 scale을 끄고
    불투명도(1.00 대 0.60)와 굵기와 보더로만 읽힌다(실측)
  - **인물 칸 높이를 뷰포트로 묶었다.** 3대4 비율로 두었더니 카드가 900px를 넘어 유파명과
    스타일과 인용이 화면 밖으로 밀렸다(실측 스크린샷). 선택 화면은 한눈에 들어와야 한다
  - **brand public에 유파 이미지가 없다.** `/images/duelist/style-1,2,3.png` 규약 경로를 참조하고
    `onError`로 다크 플레이스홀더에 내려앉는다. presentation-v2에서 복사하지 않았다(불가침).
    파일을 그 자리에 놓으면 코드 수정 0으로 뜬다
  - **three.js 컨텍스트를 만들지 않았다**(실측 WebGL 0, canvas 0). 이미지와 DOM과 CSS로만 구성했다
  - **새 라이브러리 설치 없음. CREDITS 변경 없음.** GSAP Flip은 쓰지 않았다(제자리 포커스라 필요 없다)
  - 검증: 3카드 활성 하나와 blur 둘, 클릭 전환, 화살표 양방향, 스와이프,
    세로 스크롤 생존, 배지와 문구 짝(1 공격 세이버 / 2 카운터 에페 / 3 심리 헝가리안),
    이미지 폴백, `aria-selected`와 로빙 tabindex(`true:0`, `false:-1`),
    reduced에서 blur 0, 320 / 768 / 1440 / 3840 overflow 0, 4앱 빌드, 페이지 에러 0
- **brand B5 완료(확인 대기).** 파트 셋을 순서대로 닫았다. 커밋 셋으로 나눴다. 트랙 선점 해제
  - **파트 0 제품군 3종 축소(dbac9a2).** 브랜딩을 뺐다. copy에서 카드와 상세 히어로와 TODO 상수 셋 제거.
    **상세 라우트를 있는 slug만 명시로 바꿨다.** `/product/:slug` 하나로 받으면 없는 제품도 상세 골격이
    뜨고 그 안에서 다시 안내를 내야 한다. 이제 `/product/branding`이 전역 NotFound로 그냥 떨어진다.
    그래서 ProductDetail의 not-found 분기가 도달 불가가 되어 걷었다.
    인덱스 한 줄은 네 개 전제라 `PRODUCTS_INDEX_LINE_TODO` 자리표시로 바꿨다. 카드 격자 최소폭 240에서 280으로
  - **파트 1 히어로 승격(9a2d182).** 배경 글로우 + 궤적 + 텍스트 3겹.
    낮은 알파 레드 radial 하나(중심 0.13, 62퍼센트에서 투명)로 vortex를 암시한다.
    GSAP 타임라인으로 워드마크, 아이브로우, 모티프 선, 서브, 꼬리 순서로 등장한다
  - **gsap.context와 revert가 필수였다.** `tl.kill()`로 정리했더니 StrictMode 이중 마운트에서
    첫 타임라인이 남긴 `opacity 0`을 두 번째 `from()`이 **도착 상태로 읽어** 워드마크가 영원히 안 떴다(실측).
    revert가 인라인 스타일을 되돌린다. 복귀 왕복에서 0.86에서 1로 재생되는 것을 실측했다
  - **파트 2 월드빌딩 sticky(이 커밋).** ScrollTrigger pin 2화면 + scrub 1.
    제목이 서고 배경 원이 깊이로 물러나고 검끝 선이 그려지고 본문이 뒤따른다.
    실측 진행: title 0 → 0.21 → 0.84 → 1, body 0 → 0.37 → 1, dash 874 → 0, depth scale 1.35 → 1.
    구간 내내 `section.top`이 0으로 고정된다(pin 확인)
  - **두 번째 WebGL 컨텍스트를 만들지 않았다.** 월드빌딩 선은 SVG다.
    프로덕션 preview 실측 **컨텍스트 생성 1, 살아 있는 canvas 1**.
    dev에서 2로 세어지는 것은 StrictMode 이중 마운트이고 살아 있는 canvas는 1이다.
    그 김에 언마운트에서 `forceContextLoss()`를 걸어 컨텍스트를 명시적으로 놓게 했다
  - **dash 길이를 상수로 박지 않는다.** 1200으로 적었더니 실제 path 길이와 어긋나 선이 조각으로 보였다(실측).
    `getTotalLength()`로 브라우저가 잰 값을 쓴다. 곡선 경로도 하단으로 내렸다.
    처음 경로가 화면 중앙을 지나 **본문 줄 위에 선이 얹혔다**(스크린샷으로 잡았다)
  - **GSAP 3.15.0을 brand에 설치했다.** 새 라이브러리가 아니라 이미 채택하고 CREDITS에 등재된 것의
    워크스페이스 확장이고 presentation과 같은 버전이다. **SplitType은 쓰지 않았다.** CREDITS의 GSAP 행에 brand를 더했다
  - 검증: 4앱 빌드, 페이지 에러 0, reduced motion에서 pin 없이 정적(title과 body 모두 opacity 1),
    WebGL 차단 시 canvas 0에 워드마크 정상, 히어로 밖에서 렌더 정지(드로우콜 0),
    320 / 768 / 1440 / 3840 가로 overflow 0, 금지 문자 0

- **brand B4 라이팅 반입 완료(확인 대기).** 임시 카피 여덟 곳이 확정 문구로 바뀌었다. 트랙 선점 해제
  - **copy.js를 페이지 네임스페이스로 재편했다.** `HEADER` `LANDING` `PRODUCTS` `PRODUCT_DETAIL`
    `DUELISTS` `EXPERIENCE` `NOT_FOUND`. 화면을 열면 어느 상수를 고쳐야 할지가 이름으로 보인다.
    옛 평면 상수(`HERO` `WORLD` `GATEWAYS` `PAGES` `DEMO` `DETAIL` `NAV`)는 전부 폐기했다
  - **미확정 다섯은 지어내지 않았다.** 파일 최상단에 모아 선언하고 값은 자리표시 문장이다.
    빈 문자열로 두면 자리가 접혀 레이아웃이 무너지므로 문장을 넣었다. 화면 실측으로 다섯 곳 전부 노출 확인
  - **임시 꼬리표 체계를 걷었다.** `TempMark`와 `temp` prop을 지웠다. 확정 카피에는 붙일 자리가 없고
    미확정은 자리표시 문장이 스스로 "확정 예정"이라고 말한다. 표시 방법이 둘이면 하나는 거짓말이 된다
  - 상세페이지에 OVERVIEW, FEATURES, EXPERIENCE 라벨 구조를 배선했다. 뒤로 가기는 라벨이 "제품군으로"라
    **이력이 아니라 `/products`로 간다**(navigate(-1)에서 바꿨다. 라벨과 동작을 맞췄다)
  - `DUELISTS.selection.enter`("겨루기")만 참조처가 없다. 셀렉션 버튼은 B7이고
    **동작 없는 버튼을 미리 세우지 않았다**
  - **검증(실브라우저):** 9경로(랜딩, products, duelists, experience, notFound, 상세 4종) 전부
    확정 문구로 뜬다. TODO 다섯 곳 자리표시 노출, 관문 카드 3장 높이 181px로 균일,
    320 / 768 / 1440 / 3840 **가로 overflow 0**, 페이지 에러 0, 4앱 빌드 성공
  - grep: 화면 문자열 하드코딩 0(남은 한글은 `console.warn` 진단 문구 둘뿐이고 화면 카피가 아니다),
    금지 문자(가운데점, em대시, 박스라인, 이모지) **0건**
  - **라이브러리 미설치. CREDITS 변경 없음**
- **brand B3 구조 재편 완료(확인 대기).** 라이선스 게이트, 5페이지 재편, 고정 헤더. 트랙 선점 해제
  - **라이선스 게이트(커밋 cecf1d0).** 후보 17개를 GitHub API로 열어 확인했다.
    채택 가능 12, **사용 불가 5**(LICENSE 파일이 어느 이름으로도 없다).
    채택 가능분은 파일 본문을 내려받아 첫 줄과 저작권자까지 봤다. 판정표는 `docs/LIBRARIES.md` 최상단이고
    **B5 이후 세션의 유일한 채택 근거다**. 사용 불가 5개는 열람 금지 목록에 넣었다
  - **codrops 계정이라고 통과시키지 않는다.** 같은 계정에서 `GridLayoutAnimation`은 LICENSE가 있고
    `FullscreenLayoutPageTransitions`는 없다. 저장소 단위로 확인한다
  - **5페이지 재편.** `/` 랜딩(관문), `/products`, `/product/:slug`, `/duelists`, `/experience`.
    랜딩은 히어로와 월드빌딩만 남기고 나머지는 **관문 카드 셋**으로 줄였다. 제품군과 유파와 데모 섹션의
    본문은 삭제하지 않고 각자 페이지로 옮겼다(`pages/Products.jsx`, `Duelists.jsx`, `Experience.jsx`)
  - **없는 경로를 랜딩으로 튕기지 않는다.** 전역 `NotFound`를 세웠다. 조용히 홈으로 보내면
    주소가 틀렸다는 사실이 화면에 안 남아 사람이 오타를 못 찾는다
  - B8 Flip을 위해 카드에 `data-flip-id`만 미리 달았다. 제품은 slug, 유파는 ver 기준이다(실측 확인)
  - **고정 헤더 신설.** 워드마크(steelText 축소본) + 메뉴 3 + 체험하기 CTA. z-index 50(DESIGN 8절).
    최상단에서는 투명이고 60px를 넘기면 `rgba(16,16,16,0.85)` + `blur(12px)` + 헤어라인이 켜진다.
    현재 라우트는 red 점과 `aria-current`와 **weight 700**으로 함께 표시한다(색 단독 구분 금지)
  - **press 함정을 brand에서 실제로 막았다.** `theme.js`로 토큰을 CSS 변수로 내보내고
    채움을 `.vx-cta` 클래스가 쥔다. **인라인 background를 걸지 않았다.**
    누른 상태 배경이 `rgb(184, 10, 17)`(red.press)로 실제로 바뀌는 것을 실측했다.
    같은 이유로 기존 `ArenaCta`의 인라인 background도 클래스로 옮겼다
  - **검증(실브라우저):** 5라우트 + 상세 + 깊은 오타 경로 전부 직접 URL과 링크 이동 양쪽으로 열린다.
    랜딩 섹션 hero world products(관문) 셋, **히어로 궤적 초당 122 드로우콜로 재편 뒤에도 살아 있다.**
    헤더 최상단 투명 대 스크롤 후 딤과 blur 실측, 현재 페이지 표시 정확, 헤더 CTA는 `/experience`로,
    experience의 arena CTA는 `VITE_ARENA_URL`로 향한다. 320 / 768 / 1440 / 3840 **가로 overflow 0**,
    페이지 에러 0, 4앱 빌드 성공
  - **이 세션은 라이브러리를 설치하지 않았다. CREDITS 변경 없음**
- **P-B brand 세션 2 히어로 검끝 궤적과 Lenis 완료(확인 대기).** 히어로 섹션 하나만 만졌다
  - **궤적 방식은 후보 A(arena 리본 재사용)로 확정했다. 둘 다 실제로 확보해 비교했다**
    - 후보 B `@newkrok/three-particles`를 임시 폴더에 **실설치**해 확인했다. 3.0.0, LICENSE 파일 MIT,
      `RendererType.TRAIL`과 `TrailConfig`가 실재한다. 기억이 아니라 설치본을 열어 봤다
    - **기각 사유 둘.** peer가 `three@^0.182.0`이라 리포 표준 0.185.1과 충돌한다
      (`npm install three@0.185.1`에서 ERESOLVE 경고 실측). 그리고 검끝 한 줄을 그리려고
      파티클 엔진과 전이 의존성 셋을 들인다. **설치본은 리포에 남기지 않았다**
    - 후보 A는 이미 arena에서 검증된 단일 리본이고 모티프와 1:1이다. `createTrailRibbon`을 그대로 import하고
      **arena 파일은 읽기만 했다**(diff 0줄)
  - **좌표 배율이 리본의 순간이동 가드와 직결된다.** trail.js는 프레임 간 이동이 `max(0.35, 12 x dt)`를
    넘으면 이력을 버린다. 그래서 커서 좌표를 그대로 밀지 않고 **속도 상한 9단위/초를 건 추종점**을 민다.
    이 상한은 어느 프레임레이트에서도 가드 아래다(60fps 0.15 대 0.35, 20fps 0.45 대 0.6)
  - **직선 추격을 스프링으로 바꿨다.** 남은 거리의 일정 비율만 따라가니 목표가 바뀔 때마다 경로가 꺾여
    리본이 **다각형으로 보였다(실측 스크린샷)**. 속도를 상태로 들면 방향이 관성으로 돌아 곡선이 남는다
  - **소용돌이 각속도가 곧 호의 길이다.** 리본 수명이 520ms라 화면에 남는 것은 그 사이 지나온 길뿐이다.
    1.8rad/s에서는 54도 조각이라 소용돌이로 안 읽혀 **2.6rad/s(약 78도)로 올렸다**
  - 색은 DESIGN 10절대로 **평시 스틸**이다(core steel.mid, glow steel.shadow). red.light는 CTA와 명중 맥락 전용이고
    상대 블루는 브랜드 화면에 오지 않는다. 리본 반폭 0.0028단위로 1440x900에서 약 2.5px다
  - Lenis 1.3.26 실설치. `autoRaf`로 자기 rAF를 돌린다(설치본 기본값 false를 확인하고 켰다).
    묶어야 할 외부 시계가 없어 ticker에 태우지 않았다. reduced motion은 Lenis가 `respectReducedMotion` 기본값으로 처리한다.
    **방향키 셸을 두지 않았다. 일반 스크롤이다**
  - reduced motion에서는 rAF를 아예 안 돌리고 나선을 한 번 그려 정적으로 남긴다. WebGL 생성 실패는 콘솔 경고 후 히어로가 워드마크만으로 선다
  - **검증(실브라우저 픽셀):** 커서 정지 상태에서 소용돌이가 살아 있고, 커서를 흔들면 곡선 한 줄이 따라온다.
    워드마크 중앙의 최상위 요소가 `H1`이라 궤적이 텍스트를 안 가린다.
    **드로우콜 계수로 생명주기를 실측했다. 히어로 보일 때 초당 122, 벗어나면 0, 복귀하면 122**
    (계수기가 WebGL1과 WebGL2 프로토타입을 겹쳐 잡아 프레임당 2로 세므로 실제는 약 61fps다).
    Lenis 휠 한 번에 scrollY가 67 → 231 → ... → 674로 이어지고 다른 섹션과 충돌 0. 페이지 에러 0, 4앱 빌드 성공
  - **헤드리스 fps로 통과 판정하지 않는다**(arena fps 항목과 같은 원칙). 발표 노트북에서 육안 확인이 남는다
  - 이 세션은 버튼을 만들지 않아 press 인라인 함정은 해당 사항이 없었다. 다음에 brand에 버튼을 만들면 클래스와 `:active`로 간다
- **shared 토큰 red 역할 개정 완료(확인 대기).** 화면의 레드가 `#E60D15` 하나가 됐다. 트랙 선점 해제
  - **CTA 채움 `#80070C` → `#E60D15`.** 이전 채움은 배경 `#101010` 대비 **1.77:1**이라
    버튼이 무대에 잠겨 형태가 안 읽혔다. 지금 4.02:1로 뜬다
  - **텍스트는 흰색을 유지했다. 검정 반전은 실측으로 기각됐다.**
    같은 레드 위에서 검정 4.02:1 대 흰색 4.66:1로 **검정이 오히려 낮고**,
    press(`#B80A11`)에서는 검정이 2.80:1이라 UI 최소 3:1도 못 넘는다. 흰색은 press에서 6.68:1로 오른다
  - **buttonRed 탐색 결과 darken 비율은 0이다.** 흰 텍스트 대비가 4.5:1을 넘는 최소 darken을
    0.01 단위로 훑었더니 **r=0에서 이미 4.657:1로 통과**한다. 어둡게 할수록 흰 대비는 오르므로
    브랜드 레드 그대로가 하한이자 정답이다. 그래서 `red.fill`은 별도 값이 아니라 `red.light`와 같은 값이다
  - `red.deep` 신설. `#80070C`는 채움에서 빠지고 **브랜드 그라디언트 스톱과 깊이용 액센트로만** 남는다
  - `press: darken(RED, 0.2)` = **`#B80A11`**. 파생 기준이 딥 레드에서 브랜드 레드로 옮겨졌다
  - `text.onFill`은 이미 흰색이라 새 토큰을 만들지 않았다. 검정 반전이 기각되면서 필요가 없어졌다
  - **영향 범위는 UI 채움뿐이다.** `red.fill` 참조처가 arena 버튼과 온보딩, controller 버튼과
    코치마크와 결과 바와 햅틱 플래시, brand CTA, presentation 버튼이고 **게임 렌더는 `red.light`와
    `trail`을 쓰므로 판정 화면의 궤적과 램프는 값이 안 바뀐다**
  - 검증: 실브라우저 3앱 CTA 실측이 전부 `rgb(230, 13, 21)` 채움에 `rgb(253, 253, 253)` 글자다.
    light / fill / press / deep 네 색을 나란히 띄워 fill이 light와 같은 색이고 press가 한 단 어두운 것을 확인했다.
    4앱 빌드, 셀프테스트 13/13, **서명 1097 / c820d0f2 유지**
  - DESIGN 2절 레드 절을 다시 썼다. 빛과 면의 이분법을 걷고 채움 표준과 검정 반전 기각 근거를 실측값으로 적었다.
    13절 대비 목록도 갱신했다(fill 4.66, press 6.68, deep 10.6)
  - **press 인라인 버그는 이 세션에서도 안 고쳤다. 아래 미해결 유지.**
    다만 arena에서 인라인 background를 걷으면 `rgb(184, 10, 17)`로 정확히 바뀌는 것은 다시 확인했다
- **shared 토큰 press 파생 완료(확인 대기).** `red.press` 리터럴을 `red.fill` 명도 파생으로 바꿨다.
  코드 변경은 `shared/tokens.js` 한 파일이고 트랙 선점은 해제한다
  - `darken(hex, ratio)` 신설. `withAlpha` 옆에 두고 같은 임무를 진다.
    **어둡기 파생값을 손으로 적지 않기 위한 유일한 통로다.** ratio 0.2면 각 채널에 0.8을 곱한다
  - **알파로 눌러 어둡게 하지 않는다.** 그러면 뒤 배경이 비쳐 같은 버튼이 카드 위와 배경 위에서
    다른 색이 된다. press는 채움 전용이라 불투명해야 한다. 그래서 rgba가 아니라 HEX를 돌려준다
  - `press: darken(RED_DEEP, 0.2)` = **`#66060A`**(실측 로그). 채널 비율 0.797 / 0.857 / 0.833으로
    반올림 오차 안에서 목표 0.8이다. DESIGN 2절 "fill을 명도 방향으로 약 20퍼센트 낮춤"과 일치한다
  - 폐기한 `#610509`와의 차이는 채널당 5, 1, 1이라 눈에 띄는 변화가 아니다.
    바뀐 것은 값이 아니라 **값이 나오는 방식**이다. fill이 바뀌면 press가 따라온다
  - press 위 흰 텍스트 대비 **12.94:1**(fill의 10.6:1보다 오른다). bg.base 대비 1.45:1로 blackout이 아니다.
    fill과 press를 나란히 띄운 픽셀 캡처로 어두워진 것을 눈으로 확인했다
  - 검증: 4앱 빌드 성공, 셀프테스트 13/13, **서명 1097 / c820d0f2 유지**(토큰은 판정에 닿지 않는다)
  - **부수 발견 하나. press 색이 어느 앱에서도 화면에 안 뜬다. 아래 미해결에 적었다**
- **P-B brand 세션 1 기반 세팅 완료(확인 대기).** 라우팅, 토큰, copy, 랜딩 5섹션 뼈대, 상세 4종, 체험하기 연결.
  **비주얼 라이브러리 0개 설치.** 궤적, 셰이더, 유리, morph 전환은 세션 2 이후다
  - **패키지는 이미 서 있었다.** SETUP이 만든 React 18 + Vite + react-router-dom 6.30.4 + vercel.json SPA rewrite +
    `.env.example`(VITE_ARENA_URL)이 그대로 요건을 만족해 다시 세우지 않았다. 세운 것은 그 위의 화면 계층뿐이다
  - **토큰은 재수출로 간다.** shared의 4색이 이미 VORTEX_DESIGN_SYSTEM 2.1 확정값(#101010 #FDFDFD #E60D15 #80070C,
    redGlow rgba(230,13,21,0.45))과 **같은 값**이라 brand에 두 벌로 적지 않았다. shared는 수정 금지 지시가 있어
    브랜드 그라디언트와 `displayFamily` 둘만 `brand/src/tokens.js`에 추가했다. 제목 폰트가 정해지면 그 키 한 줄만 바꾼다
  - **워드마크는 메탈릭이다.** 처음에 브랜드 그라디언트를 글자에 클립했더니 마지막 스톱이 #101010이라
    **X가 배경에 묻혔다(실측 스크린샷).** shared의 `steelText`로 바꾸고 그라디언트는 워드마크 아래 검끝 모티프 선으로 옮겼다
  - **copy.js에 출처를 적었다.** 유파 3종과 ENTER THE VORTEX와 히어로 한 줄은 VORTEX_DESIGN_SYSTEM 3.11 / 3.14 / 0절 **원문**이다.
    제품군 4종 한 줄과 제품군 헤드라인은 확정 원문이 없어 임시로 썼고 `temp` 플래그로 화면에 `임시` 꼬리표가 뜬다.
    **월드빌딩은 주제만 있고 문장이 없어 지어내지 않고** "검술 도장 세계관 카피 확정 예정"만 세워 뒀다
  - 상세는 `/product/:slug` 하나가 4종을 받는다. 넷이 같은 골격이라 라우트를 쪼개면 같은 뼈대를 네 번 고치게 된다.
    없는 slug는 빈 화면 대신 안내와 뒤로 가기를 낸다
  - **ScrollRestoration을 쓰지 않았다.** 실설치본에 export는 있으나 데이터 라우터 전용이라 BrowserRouter에서는 던진다.
    직접 만든 `ScrollToTop`이 대신하고 **뒤로 가기(POP)에서는 일부러 손대지 않는다**(브라우저 복원 위치를 지우지 않기 위해서다)
  - 체험하기 CTA는 `import.meta.env.VITE_ARENA_URL`만 읽는다. 없으면 콘솔 경고 1회와 비활성 버튼과 사유 문구를 내고
    화면은 그대로 산다. 눌러도 아무 일이 없는 버튼을 남기지 않는다
  - **검증(실브라우저 스크린샷과 DOM 실측):** 섹션 5개가 순서대로 0 / 900 / 1800 / 2700 / 3600에 선다.
    `body` 배경 rgb(16,16,16), 아이브로우 원 rgb(230,13,21)로 토큰이 먹는다.
    카드 4장이 각 상세로 이동하고 h1이 제품명과 일치하며 진입 scrollY 0, 뒤로 가기는 랜딩 scrollY 1800으로 복귀한다.
    CTA는 주소 없음에서 비활성 + 경고 1회, 주소 있음에서 `target=_blank` 링크(실측 136x44).
    320 / 390 / 1440 / 3840 **가로 overflow 0**, 페이지 에러 0, 4앱 빌드 성공
  - 라이브러리 미설치라 **CREDITS 추가분 없다.** 세션 2부터 설치할 때마다 같은 커밋에서 기록한다
  - 잔여는 아래 미해결 참조. 다음은 세션 2 히어로 궤적이다
- **controller VORTEX 앱. 트랙 선점.** controller 폴더를 VORTEX 폰 앱으로 확장한다(별도 앱 안 만듦, 하나의 주소).
  화면 흐름 HOME → CONNECT → SELECT → PERMISSION → CALIBRATION → PLAY → RESULT.
  강릉페이 리뉴얼 디자인 시스템 이식(구조·규율만, 색은 VORTEX 다크). 반입 파일은 `docs/reference/gnpay/`.
  **센서(C2)·소켓(C3) 로직 무변경, 화면 계층만 확장. arena machine.js 무변경, 최신 서명 유지가 통과 조건**
  (이 트랙은 게임플레이 변경 없음). 폴더 소유: controller/arena/shared/server/docs만. presentation-v2 불가침.
  B0~B4 단계별 커밋과 즉시 push. 커밋 접두 `[controller]`/`[arena]`.
  - **B0 앱 셸과 HOME 완료(커밋 beefc62, push됨).** 강릉페이 프레임/스테이터스바/버튼/바텀시트를
    `docs/reference/gnpay` 실파일에서 이식(색만 VORTEX 다크). 화면 머신 HOME→CONNECT→SELECT→
    PERMISSION→CALIBRATION→PLAY→RESULT + 각 화면 뒤로가기. HOME(메탈릭 워드마크+게스트 시작+로그인 스텁 바텀시트).
    `shared/tokens`에 HIG 스케일 `ig`, 시맨틱 radius, `red.tonal`, `steelText` 추가. usePlatform은 sessionStorage 제거.
    데스크톱 프레임 미리보기 살림(가로 안내는 터치 기기만, ?debug=0으로 dev 패널 끔). **센서·소켓 로직 무변경.**
    실브라우저 확인: 폰 프레임+iOS 상태바+워드마크+로그인 시트+게스트→CONNECT 전환.
  - **B1 페어링 화면과 arena 로비 완료(커밋 4a20e81, push됨).** controller CONNECT: paired 게이팅(paired 수신 →
    App이 SELECT로), ?room= 스킵, 연결 중 스피너 + 상태 문구(N1), 상한 8s 뒤 평문 에러 + 재시도/코드 다시 입력(N9).
    ui.jsx Spinner(reduced 정지). arena 로비(PairingScreen 강화): VORTEX 워드마크(ChromeText) + FUI 프레임(GlassFrame) +
    "키보드로 진행 (유파 선택)" → IDLE 1/2/3/4 카드(EV.RESET). 코드/QR은 C3 QRPanel 재사용.
    실브라우저 확인: ?room=ABCD 자동 스킵→스피너→8s 에러→재시도, arena 로비 코드/QR/키보드 진행.
  - **B2 대전자 선택과 유파 전달 완료(커밋 45895ab, push됨).** controller SelectScreen(세로 스크롤 4카드,
    **VORTEX 3.11 원문 카피** copy.js SELECT, 인용문 포함, FENCING_RULES 근거 한 줄, "이 상대와 대전").
    사진은 라이선스 미확인이라 그라디언트 플레이스홀더(제출 전 교체 필수, CREDITS 기재 금지 — 아래 미해결).
    MSG.SELECT/RESULT 신설, server RELAY_TYPES에 추가. arena socket select 수신 → **engine.setSchool**
    (A3 스케줄러 갈아끼움, 엔진 재생성 없이 machine 보존, 경기 전이라 결정성 무관) → 로비 "선택된 유파 OO".
    **셀프테스트 13/13, 서명 1097/c820d0f2 유지.** 로컬 3자 페어링 검증: 선택 세이버 → arena 로비 반영 → PERMISSION.
    **카운트다운(5-4-3-2-1)은 B3 온보딩으로 이관**(첫 판 플래그 공유).
  - **B3 온보딩 코치마크와 PLAY 리스킨 완료(커밋 2954782, push됨).** arena 온보딩(ArenaOnboarding 신설,
    첫 판 한정): 코치마크 4종(간합 게이지 / 상대 예고 / 램프 / 피스트) 순차 → 카운트다운 5-4-3-2-1 → 시작.
    스포트라이트(box-shadow 딤) + FUI 팔각 박스(clip-path, steel/red) + 다음/건너뛰기. **게임 시계 timeScale 0**
    (GameCanvas frozen prop, hitstop 경로라 판정·서명 무관). 첫 EN_GARDE ref로 1회, reduced 정적.
    폰 PLAY 코치마크 2종(CoachMarkOverlay 이식, S7, 마지막 "시작"). PLAY 탭 버튼 ig.title2 리스킨(로직 무변경).
    로컬 3자 검증: arena 온보딩 4스텝+카운트다운 게임 정지, 폰 PLAY 코치마크 2종 통과. **셀프테스트 13/13, 서명 유지.**
  - **B4 경기 결과 통계 완료(커밋 9769d90, push됨).** arena: 경기 중 결과 통계를 모은다(engine `matchStats`:
    찌르기/명중/리포스트/피스트아웃/경기시간, 표시 전용 순수 카운터라 판정·결정성 무관). MATCH_END에 `link.sendResult`로
    폰에 전송. 부위 분포는 렌더 몫이라 App이 fx(owner ME 명중)에서 모아 합친다. 폰: 경기 중 센서 통계 축적(메모리만,
    손떨림 근사=연속 쿼터니언 회전각 평균, 찌르기 수/파워, 가드 수) → `buildMatchResult`로 arena 결과와 합성 → RESULT.
    ResultScreen 신설(승패+스코어, 경기/부위/폰 통계 카드, div 바, "저장 안 됨 + 근사 지표" Footnote).
    **검증: arena getMatchStats 노드 실측** — 정상 5-3(명중률 38%)·완전 수동 0-5(14초) 모두 정합. 셀프테스트 13/13, 서명 유지.
    **실브라우저 2탭 완주 스크린샷 미확보** — 백그라운드 탭이 rAF를 멈춰(arena 게임 루프 정지) 한 창에서 arena 게임과
    폰을 동시에 못 돌린다(환경 한계, 코드 아님). 전달은 B2 select 중계와 동일 경로. **실기 육안 확인 필요(아래 잔여).**
  - **트랙 완료(확인 대기). VORTEX 폰 앱 = controller 확장, 화면 7종 전부.** 강릉페이 시스템 이식(프레임/상태바/버튼/
    바텀시트/코치마크/HIG 타이포/8pt/radius/휴리스틱 주석 규율). 색은 VORTEX 다크. **센서·소켓·machine.js 무변경.**
    - **통합 검증:** 4앱 빌드 성공. 셀프테스트 **13/13**, 기준 서명 **1097/c820d0f2 유지**(게임플레이 무변경 증명).
      grep 전수 0건: localStorage/sessionStorage 실사용 0, 100vh 0(주석만), 이모지 0, HEX(컴포넌트) 0, 서버주소 하드코딩 0
      (arena faceTracker의 MediaPipe CDN은 기존 웹캠 기능, 게임 서버 주소 아님). 휴리스틱 주석 9개 컴포넌트에 인용.
      실브라우저: HOME/로그인시트, CONNECT 스피너·에러·재시도, arena 로비 코드·QR·키보드폴백, SELECT 4카드·선택 반영,
      온보딩(arena 4스텝+카운트다운, 폰 2스텝), PLAY 탭 모드 전부 육안 확인. 3자 페어링 완주.
    - **잔여(제출 전/실기):** (1) **SELECT 유파 사진** 플레이스홀더 → 자체 제작 교체 필수(미해결에 기록, CREDITS 기재 금지).
      (2) **RESULT 실기 육안** — 실폰+실경기에서 통계 화면 확인(손떨림 등급 임계는 근사, 실기 튜닝 여지).
      (3) V4e 시간 팽창 시각 세트, 생성 이미지 setPoses 교체, PHASE2(배포 상호 연결, 리허설 3컷, 발표 노트북 실기 fps).
- **arena IDLE에서 PAIRING 진입 버튼 연결(B 트랙 후속, 확인 대기).**
  - **전이는 원래 있었다.** `machine.js`의 `IDLE + EV.START -> PAIRING`이 그대로 있고 소켓과
    `PairingScreen`도 완성돼 있었다. **없던 것은 입구의 라벨이다.** `[경기 시작]` 하나가
    `link.connect()` 성공 여부로 갈려서 **`VITE_SERVER_URL`이 없는 배포에서는 폰 연결로 가는 길이
    아예 없었고**(조용히 키보드로 샜다) 주소가 있으면 반대로 키보드로 갈 버튼이 없었다
  - `[폰으로 연결]` 신설(red 강조, 화면당 강조 1개라 `[경기 시작]`은 ghost로 내렸다).
    이 버튼만 `EV.START`를 보내고 `[경기 시작]`과 F9는 언제나 `EV.START_KEYBOARD`다.
    **숨은 분기를 없앤 것이 이 변경의 알맹이다.** 버튼 아래 한 줄로 두 경로의 차이를 적었다
  - **주소가 없어도 PAIRING으로 간다.** 조용히 키보드로 새면 왜 안 되는지가 화면에 안 남는다.
    `socket.js`에서 주소 없음을 `LINK.ERROR`가 아니라 `LINK.IDLE`로 바꿔 로비가
    "서버 주소가 없다"와 "서버에 닿지 않는다"를 **다른 문구로** 낸다(고칠 곳이 환경 변수인지
    서버인지가 갈린다). 칩 라벨은 둘 다 `없음`이라 HUD 표시는 그대로다
  - 유파 카드는 **폰 없는 경로 전용**임을 주석에 박았다. 폰이 붙으면 폰 SELECT가 `engine.setSchool`로 정한다
  - `machine.js` 무변경(전이표에 손댈 것이 없었다). `judge.js` `opponents.js`도 무변경
  - 검증(실브라우저 3상태): **서버 있음** 코드 `H2DJ` + QR 렌더 + 키보드 폴백으로 IDLE 복귀,
    **서버 꺼짐** "서버를 깨우는 중이다" 후 8초에 "서버에 닿지 않는다",
    **주소 없음**(`.env` 치우고 재기동) "서버 주소가 없다". 세 경우 모두 `키보드로 진행` 상시 노출.
    셀프테스트 13/13, **서명 1097 / c820d0f2 유지**, 4앱 빌드 성공
  - **사용자 실기:** arena 배포 URL에서 `폰으로 연결` → 코드 확인 → 폰에서 그 코드 입력 → 연결.
    배포 arena에 `VITE_SERVER_URL`과 `VITE_CONTROLLER_URL`이 실제로 들어가 있는지 함께 본다
    (QR은 controller 주소가 있어야 그려지고 없으면 코드만 크게 뜬다)
- **P-C AI 유파 완성(확인 대기).** VORTEX 유파 3종 + 통합 모드로 게임을 맞췄다. **유파 4종 완성.**
  - **A1 헝가리안 신설(심리전형).** `family 'sabre'`(라다엘리 사브르 계승). FEINT 최고(0.6),
    거리 흔들기 최고(0.55), 템포 브레이크(느린 2000~2800 / 빠른 700~1150 밴드를 매 공격 교체 shiftEvery 1),
    콤보 최저(0.08, 단발 교란), 리포스트 압박 중간(0.4), 선호 간합 중간~원거리(43).
    **실측(시드 4개 각 60초, 플레이어 없는 순수 케이던스):** FEINT 세이버 26~34% / 에페 45~62% /
    헝가리안 65~77%(3유파 중 최고). 간격 세이버 mean ~1640ms / 에페 ~3000ms / 헝가리안 ~2280ms [1100~3533]로
    폭이 가장 넓다. 세이버(몰아침)와도 에페(재고 되받음)와도 다르다 — 페인트가 압도적이고 리듬이 매 합 뒤집힌다
  - **락아웃 계열 매핑.** 유파마다 베끼던 `lockoutMs`/`doubleTouch`를 `FAMILY` 한곳으로 정리.
    sabre 170ms 시뮬타네 / epee 40ms 더블. 헝가리안은 sabre 계열이라 규칙 공유(값 무변경).
    `ThreeRenderer` 기술 표도 `school.key` → `school.family`로 바꿔 헝가리안이 세이버 베기 호/플런지를 재사용
  - **A2 통합 모드 MIXED.** `createSchoolScheduler` 하나로 네 모드 처리. 득점(라운드)마다 직전과 다른
    유파로 전환(시드 난수), 각 프로필에 난이도 계수(간격 x0.9, 페인트 x1.12 상한 0.72)를 곱한다(강화 +@).
    **실측:** seed 20260802 세이버→에페→헝가리안→세이버→에페→세이버, 같은 시드 2회 전환 순서 동일, 연속 동일 0회.
    HUD가 "MIXED 세이버 스타일"로 라운드마다 갱신, 전환 순간 라벨 블루 글로우(FUI, reduced에서 끔)
  - **A3 진입점 + 서명.** `createEngine`의 `school` 인자가 단일 창구다(`sabre`|`epee`|`hungarian`|`mixed`,
    미지정=시드). 키보드 1/2/3/4가 `attachKeyboard`의 `onSelectSchool`로 이 창구에 접힌다.
    `shared/protocol.js`에 `SCHOOL` 상수 4종. `IdleScreen`에 유파 4카드(VORTEX 3.11 카피 표기 일치)
  - **기준 서명 갱신(이 트랙 1회).** `runDeterminismCheck`를 기본 경로 한 판에서 **네 모드
    (sabre/epee/hungarian/mixed) + 기본 경로를 한 서명에 접붙이게** 바꿨다. R3 서명이 헝가리안을 통째로
    놓치던 이유(시드 20260802가 유파 2종에서도 3종에서도 세이버를 뽑음)를 이걸로 닫았다.
    **새 서명: 길이 1097 / 해시 c820d0f2 / 꼬리 `GARDE:4-0:45.250#5-0#ME`.** BASELINE 갱신. R3(205/ef4b27d5) 이관
  - **검증:** 셀프테스트 **13종 통과**(헝가리안 계열/고FEINT, MIXED 전환 결정성, 유파 선택 진입점 3종 추가),
    같은 시드 2회 + 교차 프로세스 동일, 4앱 빌드(presentation/brand/arena/controller),
    유파 4종+기본 각 1경기 완주(세이버 5-3, 에페 5-1, 헝가리안 5-0, MIXED 5-0),
    실브라우저 2D 스팟(?renderer=2d): 헝가리안/MIXED 카드 선택 → 경기 진입, HUD 유파명, 호환 렌더 정상.
    grep HEX 0 이모지 0(추가 라인 기준)
  - **machine.js 무변경**(경기 상태 머신 불가침 유지). presentation 폴더 무변경. A1/A2/A3 커밋 분리
- **P-C VORTEX 네이밍 통일 완료(확인 대기).** 화면 표시 제품명이 전부 영문 VORTEX다
  - 탭 제목 3개: `VORTEX Arena` / `VORTEX Controller` / `VORTEX Brand`.
    arena IDLE 워드마크와 brand 히어로는 `{BRAND}`를 읽는다(실측 스크린샷 확인)
  - **제품명을 컴포넌트에서 뺐다.** `arena/src/copy.js`와 `brand/src/copy.js` 신설.
    `presentation-v2/src/copy.js`와 같은 규약이다. 이름이 또 바뀌면 앱마다 이 파일 하나와
    정적 HTML의 `<title>` 한 줄만 고친다(정적 HTML은 상수를 못 읽으므로 주석에 명시했다)
  - **간합을 제품명 자리와 게임 용어 자리로 갈랐다.** 워드마크는 VORTEX가 됐고,
    거리를 뜻하던 두 곳은 **거리**로 옮겼다(arena HUD 게이지 라벨, IDLE 조작 안내 문장).
    거리 게이지에 VORTEX를 넣으면 계기 라벨이 제품명이 되어 뜻이 사라진다.
    실측 확인: HUD가 `거리 52`로 뜬다
  - **controller 5화면에는 제품명이 원래 없다.** JOIN PERMISSION CALIBRATION PLAY를 실제로 밟아
    화면 텍스트를 떠 봤고 넷 다 제품명 문자열이 0이다(탭 제목만 VORTEX Controller).
    END는 arena가 `MSG.STATE`를 안 보내 도달하지 못하는 기존 이슈라 소스로만 확인했고 역시 0이다.
    **워드마크를 새로 넣지는 않았다.** 그것은 교체가 아니라 신설이고 VORTEX 앱 신설 트랙의 HOME 화면 몫이다
  - server는 표시 문자열 자체가 없다. brand는 SETUP 골격이라 워드마크 한 줄이 전부다
  - `<meta>`는 **네 앱 어디에도 없다.** 불가침인 presentation-v2도 title 하나뿐이라 그것이 집 규칙이고
    교체할 대상이 없었다. description과 og를 새로 세우려면 별도 판단이 필요하다(OG 이미지는 절대 URL 필수)
  - shared의 헤더 주석 두 줄(`tokens.js`, `protocol.js`)도 제품명이라 VORTEX로 옮겼다.
    나머지 주석의 간합은 전부 **거리를 뜻하는 경기 용어**라 그대로 뒀다(판정 3파일 포함)
  - **presentation 폴더는 규칙대로 안 건드렸다.** 그래서 탭 제목이 `간합 프레젠테이션`으로 남아 있다.
    구버전 동결본이고 발표는 presentation-v2가 쓰지만, 그 폴더를 여는 트랙이 한 줄만 바꾸면 된다
  - 검증: 셀프테스트 10/10, **R3 서명 유지**(205 / ef4b27d5 / `JUDGE:6-1:51.833#6-1#ME`),
    judge machine opponents diff 0줄, presentation과 presentation-v2 diff 0줄,
    화면 표시 코드의 간합 間合 GANHAP 잔존 0, 4앱 빌드 성공, 추가분 이모지 em대시 가운데점 0
- **controller C3 소켓 S0~S3 전체 완료. 사용자 실기 확인 대기.**
  - 데모 코어 루프(폰이 검이 된다)가 성립했다. 로컬 통합 검증 11/11
  - 남은 것은 **실기 하나**다. 폰으로 QR을 찍고 검이 따라오는지, 지연이 체감되는지,
    검 자세가 자연스러운지를 사람이 봐야 한다. 헤드리스로는 센서를 만들 수 없다
  - 배포는 외부 공개 액션이라 사용자가 수행한다. 절차는 `docs/SERVER.md` 배포 절차 절에 있다
- **P-C VORTEX 리스킨 V0~V2 전체 완료. 사용자 실기 확인 대기.**
  - **갈래 B 확정.** 색 값은 VORTEX로 통일하되 궤적과 램프의 소유 관계는 arena 기존 그대로 간다
    (내 검 레드, 상대 블루). VORTEX 2.1의 "내 검 실버 시안, 상대 레드"는 채택하지 않았다.
    사유는 DESIGN.md 2절 "소유 관계 결정"에 있다. arena D0의 "레드는 사건의 색" 체계를 지키기 위해서다
  - **judge.js machine.js opponents.js 무변경.** 세 단계 내내 R3 서명
    205 / ef4b27d5 / JUDGE:6-1:51.833#6-1#ME이 그대로다
  - `presentation-v2/src/tokens.js`와 기존 `presentation/` 폴더는 안 건드렸다
  - **사용자 실기 확인 요청 셋.** 헤드리스로는 판정할 수 없는 것들이다
    1. **새 색 육안.** 발표 노트북에서 arena를 열어 평시 화면과 득점과 피격을 본다.
       특히 **득점 플레어의 붉기**를 본다. 계산상 emissive 휘도가 25퍼센트 낮아졌고
       블룸 번짐은 그만큼 줄었다. 약하면 `swordModel.js`의 `FLARE_PEAK`만 올린다
    2. **`?cam=1`로 웹캠 추적 확인.** 주소 뒤에 붙이고 아무 키나 눌러 권한을 준 뒤
       좌하단 패널이 `추적 중`이 되는지, 머리 x와 안정도가 움직이는지 본다.
       `거부됨`이나 `얼굴 없음`이면 캠 문제이고 게임은 그대로 돈다
    3. **고개 기울임 체감.** 위 패널이 `추적 중`인 상태에서 고개를 좌우로 움직이며
       패럴랙스 mm 값과 화면 이동을 함께 본다. **순수 옆이동이 회전보다 훨씬 크게 움직인다.**
       약하면 `PARALLAX.yaw`를 내리고, 어지러우면 셋을 함께 내린다(ARENA_SCENE 12절에 근거를 적어 뒀다)
  - 잔여(이 트랙 밖): controller C3 소켓, V4e 시간 팽창 시각, 생성 이미지 setPoses 교체
- **controller C2 (코드 완료, 실기 검증 대기). 트랙 선점.**
  데스크톱 검증까지 끝났고 **남은 것은 실기 폰 측정 하나**다. Vercel 연결은 사용자가 맡는다
  - 실기 절차: 안드로이드로 프리뷰 URL + `?debug=1` → 캘리브레이션 →
    찌르기 20회(오탐/미탐), 걷기 30초(오탐 0), 가드 10회, 스와이프 → debug 수치 보고
  - 임계 조정이 필요하면 debug 화면의 수평/수직/임계 수치를 근거로 보고
- **P-C 룰 심화 R0~R5 전체 완료(확인 대기).**
  - controller C2 코드 완료. 다음은 **C3 소켓 연동**이다
    (쿼터니언 → arena `setSwordPose` 직결, arena의 키보드 임시 착탄 변주 `AIM_JITTER` 제거)
  - 잔여: V4e 시간 팽창 시각 세트(R5 웹캠 트리거와 연동 확인 필요), 생성 이미지 setPoses 교체 단계마다 커밋을 남기고 승인 대기 없이 이어간다
  - **R3은 서명이 바뀌는 유일한 단계다.** 갱신 절차(변경 전 그린 → 변경 → 같은 시드 2회 동일
    → 새 서명 채택)를 밟고 selftest BASELINE과 이 문서를 함께 고친다. R4 R5는 새 서명 유지가 조건
  - **절대 정지 조건: R3에서 같은 시드 2회 결과가 갈리면 그 자리에서 멈춘다.**
    시드 안 거친 난수가 판정에 샌 것이므로 R4로 넘어가지 않는다
  - R0 문서 배치 / R1 가드 홀드 전용화 / R2 E3 히트 증폭 / R3 실룰 심화(서명 갱신)
    / R4 기술 다양화 시각 / R5 E5 웹캠
  - 사실 근거는 `docs/FENCING_RULES.md`이고 **채택 판정표가 본문보다 우선**한다
  - **서명 관리**: R1 R2는 렌더 전용이라 D3 서명 유지가 통과 조건.
    R3은 의도적 게임플레이 변경이라 갱신 절차를 밟는다. R4 R5는 R3의 새 서명 유지가 통과 조건
  - **부위는 표시 전용, 점수는 균일(1투셰 1점).** 착탄을 판정에 넣으면 결정성이 무너지고
    실룰에도 부위 점수 차등이 없다(FENCING_RULES 판정표 1행). 초록 램프 미도입도 같은 표 2행
  - **단계마다 검증 보고와 커밋 후 대기.** 확인 전 다음 단계 착수 금지
  - fps 실기 통과(평균 47, 최저 31)
- **P-A 개편 (P0). 트랙 선점.** presentation 탈PPT 전면 개편(P0~P7). 인터랙티브 스크롤 경험으로 재편.
  - 보존 자산: Lenis+ScrollTrigger 동기 루프, 8섹션 골격과 확정 카피, ScrollTrail, motionMode reduced 분기, cover SplitText 1회, interactions pin. 갈아엎지 않고 위에 얹거나 재배치
  - 스택 판정(조사보다 우선): R3F/r3f-scroll-rig 미도입, 영상 스크럽은 GSAP imageSequence 로직 포팅(canvas 2D), 배경 셰이더는 three.js 최소 도입(P5, R3F 없이)
  - 단계마다 검증 보고와 커밋 후 대기. 확인 전 다음 단계 착수 금지
  - **P0 완료(7390f75).** compass A LIBRARIES 배치(sha256 동일, 무수정) + 스택 판정표. 강화 패스 LIBRARIES/CREDITS 소급분 이미 반영 확인. CREDITS 무변경(신규 도입 0)
  - **P1 완료. 보강 없음, 검증만 통과.** scroll.js에 정석 3요소가 이미 전부 있고 단일 루프라 새 코드 0줄
    - (1) grep 확정: `gsap.ticker.add`·`new Lenis`·`lenis.on('scroll',ScrollTrigger.update)`·`lagSmoothing(0)` 전부 scroll.js 1곳 1회, dispose에서 정리. 경쟁 rAF 0(App 해시점프·Preloader 그리기는 1회성 비스크롤), 중복 scroll 리스너 0
    - (2) 실측(앱과 동일 모듈 임포트해 실제 ScrollTrigger 구동): ScrollTrail body 스크럽 트리거 `.progress`가 스크롤 비율과 정확 일치(0/0.5/0.9/1), interactions pin 동반 진행(0→0.312→1), 8개 트리거 전부 단일 `ST.update()` 경로로 반응. 숨겨진 pane의 rAF 스로틀로 scrub:1 easing만 정지했으나 gsap.ticker 수동 펌프 시 trailOffset 단조 감소 확인 — 앱 버그 아님
    - (3) reduced: `if(isReduced()) return ()=>{}`로 Lenis 미기동, 네이티브 스크롤 분기 생존(정적 확정)
    - 4앱 빌드 성공, A1~A3 무결(8섹션·레일 8점·ScrollTrail 존재, top offset 1000 미도색 시작)
  - **P2 완료.** `docs/SCRUB_ASSET_SPEC.md`(실물 영상 요건 5~10초·어두운/단색 배경·1080p·24fps, scroll-scrub-starter build.sh 추출, cwebp 변환, leading-zero frame_%04d, 240 상한·2프레임 스킵, 투명 시 clearRect). 임시 프레임 72장 생성(`presentation/scripts/gen_temp_frames.py`, Pillow) → `presentation/public/frames/hero/frame_0001~0072.webp` + `manifest.json`(placeholder:true). 1280×720 webp 680KB. 검끝 곡선 모티프 진행(크롬→red 검끝), 우하단 TEMP 라벨은 스크럽 검증 보조. 실물 교체는 폴더 덮어쓰기 + manifest.count만. scroll-scrub-starter/Pillow는 빌드타임 도구라 CREDITS 미기재
  - **P3 완료. 표지 히어로 영상 스크럽.** CoverSection이 Section 골격 대신 자체 `<section id=cover>`를 소유(200vh). 안쪽 `position:sticky` 100dvh 레이어에 펜싱 프레임 canvas(배경) + 스크림 + 콘텐츠. App cover 분기를 `<CoverSection/>` 직접 렌더로 변경
    - 스크럽: ScrollTrigger onUpdate로 progress→프레임 인덱스 반올림 + curFrame 비교 redraw 방지(imageSequence 로직 포팅, scrub 없이 1:1). 새 라이브러리 0(GSAP ScrollTrigger 기존, 로직 포팅이라 CREDITS 무변경)
    - 프리로드: manifest 읽고 frame0 decode() 후 즉시 그림(빈 canvas 금지) → boot.markAssetsReady로 프리로더에 알림. 나머지는 fetch만, 디코드는 draw 온디맨드(ponytail: 240장 1080p에서 메모리 오르면 ±10 윈도잉). DPR 캡 min(dpr,2). 투명 프레임 clearRect 분기
    - 프리로더 결합: Preloader가 폰트+frame0(whenAssetsReady) 함께 대기, 2.5초 상한 유지
    - reduced: 200vh·스크럽 없이 대표 프레임(마지막) 한 장 정지, 섹션 100dvh
    - 캔버스 사이징은 ResizeObserver(window resize보다 견고, 컨테이너 뒤늦은 사이징/dvh 변화까지 잡아 빈 canvas 방지)
    - 워드마크 SplitText 1회 보존(스크럽 위 콘텐츠 레이어, top 167 정위치 확인)
    - 검증(실측): 스크럽 프레임 1:1(frame0 검끝 시작 → frame36 곡선 중앙 → frame71 red 검끝, 픽셀 프로브 확인), coverProgress 스크롤과 0/0.25/0.5/0.75/1 일치, 역스크롤 대칭(onUpdate 반올림), sticky 핀 유지(표지 top 스크롤 무관 134 고정), DPR 캡 동작(2530=1265×2, 640=320×2), **ScrollTrail 앵커 200vh 후 재계산 정상**(body progress 1:1, 단조 감소, 가로 오버플로 0), 320·3840 무결(ResizeObserver 자동 사이징, 4K 워드마크 136px), 4앱 빌드 성공
    - 관찰: 헤드리스 pane이 winH=0으로 붕괴하는 순간 InteractionsSection 기존 `end:+=innerHeight*4`가 0이 되어 pin이 throw(콘솔 에러). winH 720 안정 시 pin progress 0→1 정상, 실브라우저/프로덕션에선 미재현. P3 스코프 밖이라 방어코드 미추가
  - **P4 완료. 편집 레이아웃 개편 + 유리 카드. 카피는 원문 그대로, 배치만 변경.**
    - background/insight: 신규 `MediaTextSection`으로 미디어 60/텍스트 40 비대칭 병치, 좌우 섹션마다 교차(background 미디어 우, insight 미디어 좌). 제목을 대형 크롬 인용(ChromeText title)으로 위계 상승. 텍스트 Reveal 단락 등장. 미디어 슬롯은 기존 자체 SVG 도식 재사용
    - concept: 신규 `ConceptSection`으로 전폭 대형 타이포. 월드빌딩 lead를 문장 단위 4행(title 크기)으로, 행마다 독립 Reveal 등장, 여백 공격적. 제목은 heading 프레이밍(kicker)
    - interactions: pin 스텝 유지, 그리드 40/60(텍스트/비주얼)로 미디어 우위, 비주얼 슬롯 minHeight 상향
    - ai-workflow: 행 borderTop 제거(표 느낌 삭제), 행간 spacing.unit*6, 2열 병기 내용 규칙 유지
    - outputs: 유리 카드 3장. backdrop-filter blur(16)+saturate + rgba(242,246,255,0.05) 반투명 + line.strong 보더 + glow.steel. GSAP from(yPercent:100, stagger 0.18)+ScrollTrigger once 부상. reduced-transparency 불투명(bg.raised) 폴백, reduced-motion 페이드만. drei/three 유리 미도입
    - App: cover/background/insight/concept를 자체 섹션 분기로, BODY는 interactions/ai-workflow/outputs/demo만. Reveal import orphan 제거
    - 카피 무변경 검증: content/sections.js diff 0, concept 4행 재결합===원문(글자 동일, 줄바꿈만)
    - 구조 검증(getBoundingClientRect): background 텍스트428/미디어642, insight 교차, concept 4행 54.4px, ai-workflow borderTop 0, outputs backdrop-filter blur(16)+line.strong, 레일 8점·ScrollTrail 유지, 가로 오버플로 0. 320에서 background/insight 단일 컬럼 적층, 요소 우측 최대 304<320
    - ScrollTrail 앵커 P4 후 refresh 재계산 정상(단조 감소, body progress 스크롤 1:1). 4앱 빌드 성공
    - 참고: 헤드리스 pane이 스크롤과 시각 desync/winH 붕괴로 스크린샷 검증 불가 → getBoundingClientRect 구조 측정으로 대체. interactions 40/60은 pane이 ≥1024 안정 뷰포트를 못 줘 코드 확인(pinned 모드에서 적용)
  - **P5 완료. 배경 셰이더(은은).** 신규 `StageShader.jsx`. 콘텐츠 뒤 fixed 풀스크린 캔버스에 snoise 기반 일렁임
    - **판정 변경(ponytail): three.js 대신 raw WebGL 채택.** 풀스크린 삼각형 1개 + snoise 프래그먼트 하나에 three.js(~150KB)는 과함. 의존성 0, 번들 +5KB(three.js였다면 +150KB), 데모 견고성↑, 출력 동일, R3F 배제 판정과 일관. snoise는 Ashima/stegu webgl-noise(MIT) GLSL 이식 → CREDITS 기록
    - 위계: zIndex -1로 전 콘텐츠 뒤(가독성 z-순서로 보장), pointer-events none, alpha≤0.075 극미세, 커서 근처만 약하게 밝아지고 정지 시 u_active 감쇠(relax)
    - 저하: requestIdleCallback 이후 로드(LCP 미차단), WebGL 실패/모바일(pointer coarse)/reduced-motion에서 미로드 → StageBackground 정적 그라디언트가 그대로 폴백. DPR 캡 1.5, WEBGL_lose_context로 정리
    - 검증: WebGL 초기화·사이징 정상(1920×1080=1280×1.5), **GL 에러 0**, GLSL 컴파일/링크 에러 0(콘솔 클린), 텍스트 가독성 유지(스크린샷 확인), 빌드 성공(318KB)
  - **P6 완료. Flip 상세와 클립패스 리빌.**
    - outputs 유리 카드 → 상세 오버레이 모핑: GSAP Flip(무료, gsap/Flip 지연 로드). 카드 클릭 시 Flip.getState → position:fixed 확대 → Flip.from 모핑, 닫기 역모핑. absolute 옵션 미사용(열린 카드가 이미 fixed out-of-flow라 불필요하고, 켜면 전환 후 position:absolute가 남아 뷰포트 기준이 깨짐 — 실측으로 잡아 제거)
    - 상세: role=dialog + aria-modal, 캡처 슬롯 확대 + name/role(카피 원문). 딤 스크림(zIndex overlay). ESC·바깥 클릭(scrim)·닫기 버튼 3경로 닫기. 포커스 닫기 버튼으로 이동 + Tab 트랩(첫↔끝 순환). 스크롤 잠금 미적용(전환 중 잠금 최소화). 슬라이드업 아님(모핑). reduced-motion에서 Flip 없이 즉시 전환
    - 클립패스 리빌: 신규 `ClipReveal`. inset(0 100% 0 0)→inset(0) 와이프를 ScrollTrigger scrub로. insight 미디어 **한 곳만 절제 적용**(clipReveal prop). reduced-motion 완전 노출. gl-transitions는 LICENSE 확인 불가라 미도입(clip-path로 충분)
    - 검증(DOM): 카드 클릭→dialog(aria-modal) 열림+scrim(zIndex 90)+닫기버튼 포커스, ESC 닫힘, 바깥클릭 닫힘, position fixed 안착, insight clip-path 적용. 4앱 빌드 성공. Flip 24KB 지연 청크
    - 참고: 헤드리스 winH=0 붕괴 시 Flip tween(rAF)이 못 돌아 morph transform 잔여가 관찰됨(실브라우저는 사용자가 보는 카드를 클릭→작은 delta로 부드럽게 완료·clearProps). 육안 확인 권장
  - **P7 완료. 감사와 마감.**
    - grep 감사(presentation/src 전수): HEX 하드코딩 0, 이모지 0(주석의 → 화살표는 이모지 아님), 바레 100vh 0(cover는 200dvh로 다듬음), ease-in 0, GSAP `.in` 이즈 0, hover scale 0(주석만), lorem 0, 무라이선스 3종(olivierlarose/cortiz2894/kalebkloppe/mouse-effects/scroll-image-sequence) 코드 흔적 0
    - reduced-motion 전 분기 확인(코드): StageShader 미로드, cover 스크럽 없이 대표 프레임 정지, OutputsSection Flip skip+페이드만, ClipReveal 완전 노출, Reveal fade, Magnetic off. 파티클류 없음
    - prefers-reduced-transparency + **prefers-contrast: more** → 유리 카드/상세/스크림 불투명(bg.raised) 폴백(OutputsSection에 prefers-contrast 추가). 훅은 무조건 호출 후 값 OR(조건부 호출 금지)
    - magnetic CTA(demo): 이미 정석 이관됨. try/catch+동적 import 폴백(실패 시 평범 버튼), reduced off, 한 곳만. CREDITS 기재
    - cover 200vh→**200dvh**(모바일 주소창에서 sticky 100dvh와 스크롤 길이 일관)
    - CREDITS 최종 대조: GSAP 행에 Flip 추가, snoise(P5) 기재됨. 빌드타임 도구(Pillow, scroll-scrub-starter)는 앱 반입 0이라 미기재 유지. 미도입 기재 0
    - 모바일 프레임 축소: 임시 72장(680KB)은 이미 경량이라 스킵 미적용. 실물 240장 도착 시 SCRUB_ASSET_SPEC의 2프레임 스킵 적용(문서화됨)
    - LCP 보호: StageShader는 requestIdleCallback 이후 로드, 히어로 frame0 디코드 게이트로 빈 canvas 방지. 무거운 에셋이 초기 페인트를 막지 않음
    - 4앱 빌드 성공. 320·desktop·3840 가로 오버플로 0(안정 뷰포트 실측, P3/P4)
    - **남은 사람 단계: Vercel 프리뷰 배포 실 URL 육안 검증, Lighthouse LCP/CLS/INP 실측 전후 비교(헤드리스 pane 불안정으로 자동 측정 불가), 실브라우저에서 P3 스크럽·P6 Flip 모핑 육안 확인.** 배포는 외부 공개 액션이라 권한 확인 후 사람이 수행
  - **presentation 탈PPT 개편 P0~P7 전체 완료.** 실물 펜싱 프레임 도착 시 `presentation/public/frames/hero/` 폴더 덮어쓰기 + manifest.count만 바꾸면 반영(코드 수정 0)
- (착수 전 여기에 트랙 선점 선언. P-A presentation / P-B brand / P-C arena+controller)
- **brand 레이아웃 강화 세로정렬과 제품 가독 완료(확인 대기). 트랙 선점 해제.** 레이아웃만, 색/카피 무변경.
  커밋 둘로 나눴다(섹션 상단 정렬 전역 / 제품·관문 카드 가독).
  - **파트 A 섹션 상단 정렬.** `theme.js` `--page-top`을 `섹션간격+헤더`(최대 228px)에서
    `헤더 + clamp(24~48px)`(약 112px)로 낮춰 하위 페이지 빈 상단 3분의 1을 제거했다.
    `Section.jsx`에서 `minHeight:100dvh`와 `justifyContent:center`를 걷어 콘텐츠 높이에 맞춰 자연 흐름 상단 정렬.
    히어로는 100dvh를 유지(궤적)하되 `justifyContent: center → flex-start`로 워드마크를 상단으로 올렸다.
    실측: About/products pageTop 112px, 히어로 아이브로우 157px(중앙 대비 축소), 관문 콘텐츠 상단 정렬(중앙 아님).
  - **파트 B 제품 인덱스 가독.** `Page.jsx`에 `compact` 모드 신설(제목 68→40px, 간격 축소).
    Products가 `fit compact`. 카드에 `.vx-card` 판(`--card-bg` 미세 상승 + 라운드 16 + 호버 밝기, 선 없음),
    안쪽 패딩, 이미지 슬롯 상단(자리표시 가운데). `data-flip-id` 유지(B8a). **실측: 제품 2종이 첫 화면에
    스크롤 없이 다 보인다(320/768/1440/3840 전부 allInView).** 카드가 판으로 또렷이 읽힌다(스크린샷).
  - **파트 C 관문 위계.** 관문 제목 heading(28px) → title 계열(clamp 최대 42px, 실측 41.6px)로 확대,
    `.vx-card` 판+호버 적용(파트 B 문법), 상단 정렬(파트 A). 셋 균형.
  - `index.css`에 `.vx-card` 신설, `theme.js`에 `--card-bg`/`--card-bg-hover`/`--card-radius`.
  - **검증.** 워프(R3)·Flip(B8a)·커서(R1)는 레이아웃 변경 대상 밖이라 무변경. 320/768/1440/3840 가로 오버플로 ≤ 0.
    build:all 4앱 통과. REBOOT_PLAN 2.3에 세로 정렬 규칙과 카드 가독 문법 추가.
  - **참고: getComputedStyle이 트리 요소의 var 기반 background를 투명으로 오보고**하는 프리뷰 페인 아티팩트가 있어
    카드 판독은 스크린샷(시각)으로 확정했다(헤더 CTA 레드도 같은 오보고, 실제로는 렌더됨).
- **GUARD_TWIST(길 B) 가드를 비틀기로 완료(실기 확인 대기).** 커밋 셋으로 나눠 각 직후 push했다.
  가드 제스처를 옆으로 기울이기(roll, 길 A)에서 사용자가 실제로 하는 "잡은 채 화면이 몸을 보게 비틀기"
  (전완 회외, 장축 둘레 회전)로 옮겼다. diff는 입력/렌더/미리보기 7파일뿐, 판정 파일 무변경.
  - **비틀림 채널(orientation.js, shared/pose.js).** 자이로 각속도를 칼날 장축(기기 Y, BLADE_REST=(0,1,0))에
    정사영(gamma)한 성분을 누수 적분(`twist += rate*dt; twist *= exp(-dt/TWIST_TAU)`). 빠른 비틀기는 쌓여
    문턱을 넘고 느린 드리프트는 새어 0으로 돌아온다. **절대 트위스트각을 안 쓴다**(자기계 없어 드리프트).
    비틀기는 swing에서 구조적으로 잘려 있어(길 A) swing/tiltFromQuaternion/cb.pose는 무변경. setBaseline에서 0 리셋.
  - **가드 소스 교체(motion.js, pipeline.js).** detectGuard가 nextGuardTwist(크기 히스테리시스)로 낸다.
    nextGuard(roll)는 라이브에서 끊고 길 A 후퇴선으로 pose.js에 남겼다. thrust(detectThrust, 임계 8, 수평비,
    최소지속, 오탐 7종) 무변경. 비틀기는 자이로 각속도라 thrust의 가속 채널과 축·소스가 직교.
  - **렌더 패리 스냅(ThreeRenderer.js).** 비틀림은 swing에 없어 폰 회전이 검을 안 눕힌다. 폰 쿼터니언 슬러프 뒤
    guardBlend만큼 프리셋 대각 패리(SWORD_POSES.guard, D4 방어선)로 되돌린다. 찌르기 중엔 미적용(thrust>guard).
  - **미리보기 정합(?posetest).** 비틀림 슬라이더 신설, 실기와 같은 nextGuardTwist로 가드 표시. roll은 가드 안 켬.
    실측: roll 50→가드 없음 / twist 20→OFF, 30→ON, 15(ON 뒤)→ON(히스테리시스), 8→OFF.
    실브라우저 육안: 키보드 경기 + posetest 수직 쿼터니언에서 가드 잡으면 검이 대각 패리로 스냅, 놓으면 복귀.
  - **튜닝 레버(전부 ?debug 실측 확정).** TWIST_TAU_SEC=2.5(orientation.js), TWIST.onDeg=25 / offDeg=12(pose.js).
    onDeg는 100~200ms 스냅이 넘길 값, offDeg는 정지 자이로 바이어스 정상상태(bias x TAU) 위. ?debug에 비틀림 값 표시.
  - **불변식 확인.** 세로 중립 이산 이벤트 0(적분 0 근처), 드리프트 면역(각속도 누수 적분), 세로 고정 보장(swing 무변경).
    **서명 1097/c820d0f2 유지**(13/13, 결정성, 판정 파일 무변경). build:all 4앱 + 서명 그린.
  - **실기(사람 단계) 확인 필요.** 실제 폰 비틀기로 가드 ON/OFF, gamma 부호, 드리프트 수 분 방치, 찌르기 오탐 0,
    TWIST_TAU와 ON/OFF 손맛 튜닝. 안 잡히면 길 A 후퇴선(nextGuard(roll) 복구)으로 배포본 동작 복귀.
- **presentation-v2 P1 아이브로우 v2와 유파 문구 통일 완료(확인 대기). 트랙 선점 해제.** presentation-v2만 만졌다
  (brand/arena/controller 무변경). 커밋 하나.
  - **파트 A 아이브로우 v2.** `tokens.js`에 `typography.eyebrow`(size 1.3125rem=21px, weight 700, tracking 0.06em,
    leading 1.25) 신설. `components/Bits.jsx`의 공용 `Eyebrow`에서 레드 불릿 원을 걷고 영문·국문을 같은
    21px 700으로 세웠다. 개별 구현은 없었다(전 섹션이 이 컴포넌트 하나를 쓴다). 색은 pres-v2 기존 유지
    (영문 red, 국문 primary). **반응형 clamp를 안 쓴다**: brand와 같은 근거로, 작은 폭에서 18.66px 아래로
    떨어지면 레드 아이브로우가 대형 대비 기준을 잃는다. 21px 고정이라 전 폭에서 대형(700) 3.0 통과.
  - **파트 B 유파 문구 통일(기준 brand copy.js DUELISTS 확정본).** `copy.js`에서
    `COVER.sub`(몰입형 펜싱 XR → 훈련), `DUELIST.headline`('로 구성' 제거),
    `DUELIST.sub`(인격이다/진화한다 2줄), `DUELIST_STYLES` quote(유발한다/불리해진다/승리다, 인용부호 제거).
    styles 세 줄은 이미 brand와 일치했다. `VORTEX_DESIGN_SYSTEM.md` 3.11과 `DESIGN.md` 4절도 개정.
  - **실측(presentation-v2).** 레드 아이브로우 12곳 전부 21px / weight 700, 레드 불릿 0개.
    유파 headline/sub/cover-sub 신규 문구 렌더 확인, 옛 문구('로 구성', '코치이자 상대', '펜싱 XR') 소멸.
    방향키 셸 진행 정상(01→02), 320/768/1440/3840 가로 오버플로 0(최장 아이브로우 267px<320).
    build:all 4앱 + presentation-v2 빌드 통과.
  - **미검증(환경 한계).** 프리뷰 rAF 스로틀로 유파 카드 포커스 시 인용구의 실시간 필름은 못 잡았다.
    인용 문구는 copy.js 데이터로 확정됐고(빌드 통과) headline/sub가 같은 원천에서 정상 렌더됨을 확인했다.
- **brand R6 하단 CTA와 뉴스레터 리빌과 푸터 완료(확인 대기). 트랙 선점 해제.** 랜딩 하단만 손댔다.
  새 라이브러리 없이 GSAP ScrollTrigger와 Lenis만. 커밋 하나(세 블록이 한 흐름이라).
  - **구현.** `copy.js`에 `LANDING.outro`(cta/newsletter/footer)와 `SECTION.CTA/NEWSLETTER` 신설.
    `components/Newsletter.jsx` 신설(폼 + scrub 리빌). `Landing.jsx`에 `CtaSection`(→/experience 레드 채움)과
    `Footer`(소형 크롬 워드마크 + HEADER.nav 재사용 메뉴 + 크레딧 + 팀 + 저작권)를 인라인으로 두고
    관문 다음에 CtaSection → Newsletter → Footer 순으로 렌더.
  - **리빌은 ScrollTrigger scrub 하나.** transform(y 48→0)과 opacity(0→1)만. **fixed pin 없음**(월드빌딩 pin
    함정 회피). pin이 없어 워프 skew가 걸려도 리빌 진행은 scrollY로만 계산돼 어긋나지 않는다.
    reduced motion에서는 트리거를 아예 안 걸어 블록이 정적으로 선다. willChange는 리빌 요소에만.
  - **뉴스레터 전송 백엔드 미연결.** 입력과 버튼은 세우되 제출은 자리표시 동작이다. onSubmit이
    preventDefault 후 콘솔 로그 한 줄과 "구독 기능 준비 중" 안내(role=status, aria-live=polite)만 띄운다.
    실제 구독 API가 생기면 Newsletter.jsx의 onSubmit만 바꾼다. label↔input 연결, type=email required,
    키보드 제출, focus-visible red 2px(전역). **입력 필드 border는 기능이라 R1 선 제거 예외(최소).**
  - **실측(1440).** 섹션 순서 hero→gates→cta→newsletter + footer. 리빌 요소 화면 밖 초기 상태
    opacity 0 / translateY 48px(scrub from-state 적용 확인). CTA href /experience. 폼 제출 크래시 없음,
    제출 뒤 "구독 기능 준비 중" 렌더. 푸터에 워드마크·메뉴(ABOUT/PRODUCTS/DUELISTS/EXPERIENCE)·
    크레딧·팀·저작권 전부. outro 영역 장식 선 0(입력 필드와 CTA 버튼 채움만), 레드 배경 그라디언트 0
    (크롬 워드마크의 text-clip 그라디언트만, 허용). 커스텀 커서 하단에서도 data-cursor-on.
    320/768/1440/3840 가로 오버플로 ≤ 0. build:all 4앱 통과.
  - **미검증(환경 한계).** 프리뷰 페인이 rAF를 강하게 스로틀해 리빌 애니메이션의 실시간 필름은 못 잡았다.
    from-state가 라이브임은 확인했고(스크럽이 요소를 쥔 증거) gsap.from 의미상 progress 1에서 자연 상태(보임)로
    수렴한다. **실기(사람 단계)에서 스크롤 리빌 육안 확인 권장.**
- **brand 헤더 About 순서 이동 완료(확인 대기). 트랙 선점 해제.** 헤더 메뉴에서 ABOUT을 맨 앞으로
  (ABOUT, PRODUCTS, DUELISTS, EXPERIENCE). **copy.js `HEADER.nav` 배열 하나만 재정렬했다.**
  데스크톱 바와 모바일 시트가 이 배열을 같은 순서로 렌더하고, 탭 순서(DOM 순서)와 현재 표시
  (aria-current, 레드 점)도 전부 여기서 파생돼 다른 파일은 손대지 않았다. 워드마크는 맨 왼쪽 유지.
  - 실측: 데스크톱(1440) 탭 순서 VORTEX→ABOUT→PRODUCTS→DUELISTS→EXPERIENCE→체험하기(시각 순서와 일치),
    모바일 시트도 ABOUT 첫 항목 + 레드 점. /about에서 ABOUT aria-current=page, /products로 가면
    PRODUCTS로 옮겨간다(현재 표시 정상). 320/768/1440/3840 가로 오버플로 ≤ 0. build:all 4앱 통과.
  - 커밋: [brand] 헤더 ABOUT 메뉴 맨 앞으로. **푸시는 사용자가 나중에 일괄 수행.**
- **brand R5 월드빌딩 제거와 About 신설 완료(확인 대기). 트랙 선점 해제.** 커밋 둘로 나눴다.
  - **파트 A: 월드빌딩 섹션과 pin 제거.** `WorldScene.jsx`(ScrollTrigger sticky pin 소유)를 git rm으로 삭제하고
    Landing에서 `WorldSection`과 import, `world` 구조분해를 걷어냈다. copy.js에서 `LANDING.world`와
    `SECTION.WORLD`를 제거했다. ScrollWarp 주석의 삭제된 WorldScene 참조와 "이제 없는 pin" 서술을 정정했다.
    - **실측(1440x720): pin-spacer 0, ScrollTrigger 인스턴스 0**(전역 노출 안 됨이지만 pin-spacer로 확인).
      문서 높이 1440 = 뷰포트 720의 정확히 2배라 히어로+관문 둘뿐이고 죽은 스크롤 공간 0.
      히어로 bottom 720 = 관문 top 720으로 flush, 워프 래퍼가 둘 다 감싼다. 320에서도 docH=2×vh.
    - **워프 정상.** 워프는 `window.scrollY` 프레임 차분만 읽고 pin에 의존하지 않아 제거 영향 없다.
      래퍼 안에 남는 fixed가 이제 없다(pin이 유일했다).
  - **파트 B: About 페이지 신설과 헤더 메뉴.** `/about` 라우트(App), 헤더 nav에 ABOUT 추가(copy),
    `ABOUT` 카피 네임스페이스, `pages/About.jsx`(무배경/무선 3섹션). 골격은 `.vx-shell .vx-page` 재사용.
    - **실측: /about이 헤더 링크와 직접 URL 양쪽으로 열린다.** 헤더 ABOUT에 aria-current="page" + 레드 점 + weight 700.
      아이브로우 영문 computed weight 700(대비 요건). 커스텀 커서 data-cursor-on=true, ring+dot 2개 동작.
      3섹션(네이밍 h1 "왜 VORTEX인가" / 원칙 h2 "우리가 지키는 것" + 명사형 셋 / 팀 h2 "만든 사람들" +
      dim "팀 이미지 첨부 예정") 전부 무배경·무선. 진입 애니메이션 없음이라 reduced motion 자동 만족.
    - **overflow 0: /about과 / 둘 다 320/768/1440/3840 전부 가로 오버플로 ≤ 0**(scrollbar-gutter 예약분 -15 포함).
      3840은 page-max 1680px 중앙 정렬.
  - 검증: **build:all 4앱(presentation/brand/arena/controller) 전부 성공.** 판정 파일과 arena/controller 무변경.
  - **남은 사람 단계: 푸시 후 Vercel 프로덕션 프리뷰 초록불과 /about 실 URL 육안 확인**(배포는 외부 공개 액션).
  - **보존: 제거한 LANDING.world 문구(재사용 대비).**
    - eyebrow: { en: 'WORLDBUILDING', ko: '월드빌딩' }
    - title: '일상의 공간이 도장이 된다'
    - body: '검과 검 사이, 찌르기가 성립하는 찰나의 거리를 몸이 먼저 읽는다. VORTEX는 그 순간의 감각을 훈련으로 옮긴다.'

## 결정 기록

- **arena를 three.js 1인칭으로 승격한다 (V0에서 확정).**
  근거 둘: 컨트롤러가 보내는 쿼터니언이 1인칭 검 자세에 1대1로 꽂혀 C3 연동이 자연스러워진다.
  궤적 리본과 후처리가 GPU로 넘어가 CPU 예산에 여유가 생긴다.
  제약: 게임 로직(machine, judge, opponents, loop, 입력 큐, HUD, 셀프테스트)은 재활용하고 수정하지 않는다.
  루프 주도권은 loop.js가 쥔다(react-three-fiber 미도입). canvas 2D 렌더러는 삭제하지 않고 폴백으로 유지한다.
  **타임박스 3세션.** 초과 시 즉시 중단하고 그 시점 상태를 커밋한 뒤 2D 폴백으로 데모를 확정한다.

## 다음 작업
- ~~AI 유파 완성(프롬프트 ARENA_AI_SCHOOLS.md)~~ → **완료(확인 대기).** 유파 4종(세이버/에페/헝가리안/MIXED).
  서명 갱신 1회 수행(205/ef4b27d5 → **1097/c820d0f2**). 상세는 위 진행중 항목
- ~~VORTEX 앱 신설(프롬프트 VORTEX_APP_BUILD.md)~~ → **완료(확인 대기, B0~B4).** controller 확장, 화면 7종,
  강릉페이 시스템 이식, machine.js 무변경, 서명 1097/c820d0f2 유지. 상세는 위 진행중 "controller VORTEX 앱" 항목.
  잔여: SELECT 유파 사진 교체, RESULT 실기 육안, PHASE2 리허설·배포.
- **E4 확인 후 E3(히트 이펙트 게이밍 증폭), 그 다음 E5(웹캠 시간 팽창 트리거 + 머리 패럴랙스).**
  순서 고정이고 확인 전 착수 금지다. E5는 아래 V4e 항목과 겹치므로 착수 시 함께 정리한다
- **C3에서 지울 것 하나.** `ThreeRenderer.js`의 `AIM_JITTER_X` / `AIM_JITTER_Y`와
  `poseSword`의 `jitterX/jitterY` 적용 두 줄이 키보드 임시 조준이다.
  폰 쿼터니언이 검 그룹 회전을 쥐면 이 변주만 지우면 되고, 착탄 투영식은 그대로 진짜 조준이 된다
- **게임필 개편 D0~D5 전체 완료.** 잔여는 셋이고 다음 방향은 사용자 판단이다.
  1. **V4e 시간 팽창 시각** — AfterimagePass damp 상승, 채도 하강, 비네트 강화, 300ms 복귀.
     후처리 뼈대가 이미 서 있어 붙이기만 하면 된다
  2. **생성 이미지 도착 시 setPoses 교체** — `docs/OPPONENT_IMAGE_SPEC.md`대로 5장.
     교체 후 SOLE_V와 HEAD_V 실측 재조정과 밝기(선형 0.42 미만) 확인이 필요하다
  3. **controller C2 센서 + C3 소켓** — 폰을 검으로 쓰는 경로. `setSwordPose` 쿼터니언 슬롯은 이미 파여 있다
- (구) 상대 검 blue emissive는 D2에서 완료했다(finish 'antique').
- **상대 검 blue emissive 적용.** 로더와 emissive 함수는 색 인자로 재사용 가능하게 짜 두었다.
  `attachSwordModel(parent, { emissive: colors.trail.ai, ... })` 한 줄이면 되지만
  상대는 빌보드(평면)라 3D 검을 어디에 붙일지가 먼저 정해져야 한다
- **환경(도장 배경과 조명) 작업**
- **승격 V4 타임박스 3세션 소진(V4a / V4b / V4c). 완료 서브단계 V0 V1 V2 V3 V4a V4b V4c.**
  V4d(hitstop, ShockWave, 색수차, 셰이크)와 V4e(시간 팽창 시각 세트)가 잔여다.
  **controller C2로 갈지 승격 잔여를 먼저 할지는 사용자 판단이다.** 임의 착수하지 않는다
- P-C C2 controller 센서 (C1 확인 후 착수). C1 판정 규칙 보강분 승인 여부를 먼저 확인할 것
- P-A presentation / P-B brand는 계정별 선점 후 병렬 착수 가능
- 배경 이미지 재생성: 네이비 톤 도장 이미지 폐기, 블랙 기조로 생성
- 시각디자이너 워드마크 SVG 슬롯 전달(크롬 레터링)

## 미해결 이슈
- **AI가 아직 약하다. 가드 왜곡은 걷혔으니 이제 진짜 난이도 조정이 가능하다.**
  - 직전까지는 "세로 = 가드"가 가드를 상시로 켜서 AI 득점이 0이었다. 그건 이번 트랙에서
    해소됐다(가드 재설계 항목의 표 참조). **남은 것은 순수한 밸런스 문제다**
  - **실측.** 사용자가 가드를 아예 안 써도(0%) 20경기에서 내가 **16번 이긴다**.
    AI 득점 47점 대 내 득점 90점이다. 가드를 정상적으로 쓰면(11~26%) AI가 8~30점으로 더 내려간다
  - 조정 축은 판정이 아니라 상대 쪽이다. 거리 압박, 예고 길이, 페인트 비율, 피스트 아웃 유도.
    **판정 엔진을 건드리면 서명이 바뀐다.** 의도적 변경이면 절차대로
    (변경 전 그린 → 같은 시드 2회 동일 → 새 서명 채택 + 기록) 진행한다
  - 시작점은 `arena/src/game/opponents.js`와 유파별 계수다
- **제품 상세 3D가 아직 플레이스홀더다. 실제 GLB 반입과 톤 조정이 다음 세션이다.**
  - 지금 도는 것은 각진 8면체이고 제품 형태가 아니다. 화면에 `제품 3D 모델 확정 예정`이 뜬다.
    **제출 전 교체 필수**(BRAND_SITE_GUIDE 7절 6항 이미지 규율과 같은 지위)
  - 교체 지점은 `brand/src/components/ProductViewer.jsx`의 `MODEL_URL`(지금 셋 다 null)과
    `createPlaceholder` 둘이다. 로더는 설치본 `three/examples/jsm/loaders/GLTFLoader.js`에 있고
    **일부러 import하지 않았다.** 모델이 정해지는 세션에서 그때 건다
  - 모델이 들어오면 톤을 다시 잡아야 한다. 지금 크롬은 반사 환경 하나(DESIGN 3절 스톱 그라디언트)와
    방향광 둘로만 서 있어서 실제 제품 지오메트리에서 같은 값이 맞을 이유가 없다
- **제품 스펙 실측값 4곳 확정 필요(XR 시야각 디스플레이 무게, 컨트롤러 배터리).**
  - 스펙표와 특징 목록은 반입 완료다. 남은 것은 **실제 하드웨어를 재야 나오는 값 넷**이고
    항목 이름은 확정이라 행은 세워 두고 값 자리에만 `확정 예정`이 뜬다
  - 자리는 `brand/src/copy.js`의 `PRODUCT_DETAIL.spec` 안 `value: TODO_MARK` 넷이다.
    **리터럴로 적지 않고 상수를 쓴다.** 확정되면 그 자리만 실측값으로 바꾼다
  - 화면에 남은 다른 자리표시. `TODO_XR_GLASS_DETAIL`(xr-glass OVERVIEW 설명 문단),
    `TODO_EXPERIENCE_NOTICE`, `PRODUCTS_INDEX_LINE_TODO`, `TODO_PRODUCT_MODEL`(뷰어)
- **`red.press`가 어느 앱에서도 화면에 뜨지 않는다. 토큰 값이 아니라 적용 경로의 문제다.**
  - **arena 원인 규명 완료.** `index.css`에 `.ganhap-btn-primary:active { background: var(--red-press); }`가
    있고 `--red-press`도 `#66060A`로 정상 주입된다. 그런데 `Button.jsx`가 `style` 속성으로
    `background: colors.red.fill`을 **인라인**으로 걸어서 인라인이 스타일시트 규칙을 이긴다.
    실측: 누른 상태의 계산 배경이 `rgb(128, 7, 12)`(fill)로 그대로다.
    **인라인 background만 걷어내면 그 자리에서 `rgb(102, 6, 10)`으로 바뀐다**(브라우저에서 확인)
  - **이 버그는 이번 변경이 만든 것이 아니다.** 옛 리터럴 `#610509`도 똑같이 죽어 있었다.
    press 토큰이 생긴 이래로 한 번도 그려진 적이 없다
  - **controller와 brand는 press 색 규칙 자체가 없다.** controller `ButtonPrimary`는 `scale(0.97)`만 걸고
    brand CTA는 `:active`가 없다. 둘 다 누르면 크기만 줄고 색은 그대로다(실측)
  - 고치는 범위는 앱 코드 셋이라 이 트랙(shared 한 파일)에서 손대지 않았다.
    arena는 인라인 background 제거 한 줄이고, controller와 brand는 press 규칙 신설이다.
    **DESIGN 7절 press 규약을 세 앱에 실제로 적용할지가 먼저 정해져야 한다**
  - **red 역할 개정 세션에서도 유지.** press 값만 `#66060A`에서 `#B80A11`로 바뀌었고
    안 뜨는 것은 그대로다. arena에서 인라인 background를 걷으면 `rgb(184, 10, 17)`로 바뀌는 것은 재확인했다
- **brand 비주얼 라이브러리 전부 미설치. 세션 2 이후 몫이다.**
  - 히어로 궤적(NewKrok/three-particles TRAIL 또는 arena `trail.js` 재사용), GSAP Flip 카드 morph,
    유리 카드, 갤러리와 마우스 인터랙션, Lenis 부드러운 스크롤이 전부 남았다.
    지금 랜딩은 네이티브 스크롤에 정적 골격이다
  - **BRAND_SITE_GUIDE 3절 코드 출처 계약**을 세션마다 지킨다. 실설치 또는 실클론 후 파일을 읽고 쓰고,
    실패하면 그 자리에서 멈추고 보고한다. 설치한 것은 **같은 커밋에서 CREDITS에 기록**한다
  - arena `trail.js`를 재사용할 경우 그 파일은 **읽기만** 한다(BRAND_SITE_GUIDE 7절 4항)
- **brand 이미지 전부 미확보. 지금은 다크 플레이스홀더다.**
  - 제품 상세 4종의 대표 비주얼과 랜딩 카드 썸네일 자리가 비어 있다. 히어로와 월드빌딩도 비주얼이 없다
  - 반입되는 이미지는 **전부 미확인 시안으로 간주**한다(BRAND_SITE_GUIDE 7절 6항).
    팀 자체 제작 확정분만 CREDITS 채택이고 나머지는 **제출 전 교체 필수**다
- **brand 미확정 문구 셋. 상수명 그대로다. 화면에는 자리표시 문장이 뜬다.**
  - `TODO_XR_GLASS_DETAIL` XR 글라스 상세(OVERVIEW 본문)
  - `TODO_EXPERIENCE_NOTICE` 체험 페이지 주의 사항
  - `PRODUCTS_INDEX_LINE_TODO` 제품군 인덱스 한 줄. **B5에서 신설했다.**
    옛 문장 "각 장치가 거리, 자세, 타이밍, 대결을 맡는다"가 네 개 전제라 3종과 안 맞는다
  - 셋 다 `brand/src/copy.js` 최상단에 모여 있다. **값만 바꾸면 되고 참조처는 손대지 않는다**
- **브랜딩을 제품군에서 제외했다(B5).** 제품군은 XR 글라스, 모의 검 컨트롤러, 인터랙티브 데모 앱 셋이다.
  브랜딩은 **컬러 시스템과 네이밍으로 흡수**한다. 별도 제품 카드와 상세를 두지 않는다.
  `/product/branding`은 이제 NotFound다
- **brand 시네마틱 승격 대기.** 월드빌딩은 sticky pin과 텍스트 시퀀스와 깊이 페이드까지다.
  3D 카메라 궤도 같은 화려한 연출은 **채택 가능한 시네마틱 소스를 더 확보한 뒤** 별도 세션으로 올린다.
  현재 LIBRARIES 판정표에서 시네마틱 계열은 codrops/GridLayoutAnimation과 daniel-cotton/segue가 채택 가능이고
  JosephASG cinematic-scroll과 codrops FullscreenLayoutPageTransitions는 **사용 불가**다
- ~~**brand 유파 문구가 VORTEX_DESIGN_SYSTEM 3.11 원문과 갈렸다.**~~ **해소(P1).**
  - brand 확정본을 기준으로 삼아 presentation-v2 copy와 VORTEX_DESIGN_SYSTEM 3.11을 그 확정본으로 통일했다.
    유파 문구(유발한다/불리해진다/승리다, 인용부호 제거), 서브(진화한다), 히어로 서브(몰입형 펜싱 훈련) 일치.
    상세는 아래 "presentation-v2 P1" 완료 항목.
- **유파 셀렉션의 "겨루기"가 대전 진입에 미연결이다(B7).**
  - 버튼은 활성 카드에만 서고 **비활성 모양에 사유 문구("대전 진입 준비 중")를 옆에 단다.**
    동작 없는 버튼을 활성처럼 보이게 두지 않았다(ArenaCta와 같은 규율)
  - 연결처가 정해지면 `DUELISTS.selection.pending`을 지우고 핸들러를 붙인다.
    arena로 유파를 넘기는 경로는 이미 있다(소켓 `MSG.SELECT`, controller SELECT 화면)
- **brand 유파 이미지 3장 미확보.** `brand/public/images/duelist/style-1,2,3.png` 규약 경로가 비어 있다.
  현재는 다크 플레이스홀더로 내려앉는다. **전부 미확인 시안이므로 CREDITS 채택 금지, 제출 전 자체 제작 교체 필수**
- **brand 페이지 전환이 즉시 이동이다. 연출은 B8이다.**
  - 카드에 `data-flip-id`는 이미 달려 있다(제품 slug, 유파 ver). Flip이 이 속성으로 짝을 찾는다
- **라이선스 사용 불가 판정 5개. 열람도 하지 않는다.**
  - wass08/r3f-ultimate-character-configurator, Cuberto/bglines, amandaghassaei/VortexShedding,
    codrops/FullscreenLayoutPageTransitions, JosephASG/codrops-cinematic-scroll-animations
  - 전부 루트에 `LICENSE` `LICENCE` `COPYING` `LEGAL` 어느 이름의 파일도 없다(2026-08-06 실확인).
    셋은 `package.json`의 license 필드도 없다. 근거와 채택 가능 12개는 `docs/LIBRARIES.md` 최상단 판정표에 있다
- **brand 제목(디스플레이) 폰트 미정.** `brand/src/tokens.js`의 `displayFamily`가 지금 본문과 같은 Pretendard다.
  참조처는 히어로 워드마크와 ENTER THE VORTEX 둘뿐이라 폰트가 정해지면 그 키 한 줄만 바꾼다
  (presentation-v2의 같은 이름 키와는 별개 파일이다)
- **controller SELECT 카드의 유파 사진이 없다(그라디언트 플레이스홀더).** B2에서 유파 사진(강릉페이 style-1~3
  대응 + MIXED)을 라이선스 미확인이라 반입하지 않았다. `SelectScreen`의 카드 상단 그라디언트 자리에 다크 처리한
  사진을 얹으면 되고 코드 수정은 최소다. **제출 전 자체 제작 이미지로 교체 필수, CREDITS 기재 금지(미확인).**
- **arena 소켓이 서버에 못 붙으면 브라우저 콘솔에 재시도 로그가 계속 쌓인다.**
  - `ERR_CONNECTION_REFUSED`가 socket.io 재시도마다 한 줄씩 남는다(실측 14건/30초).
    **앱 예외가 아니라 브라우저 네트워크 로그이고 경기는 그대로 돈다.**
  - 일부러 끊지 않았다. Render 무료 플랜은 콜드 스타트가 수십 초라 계속 재시도해야
    늦게 깨어난 서버에 자동으로 붙는다. 재시도를 멈추면 그 자동 복구가 사라진다
- **controller END phase가 도달하지 않는다.**
  - arena가 `MSG.STATE`를 안 보내므로 폰이 경기 종료를 모른다. C3 범위는 이산과 연속과
    햅틱까지였고 상태 브로드캐스트는 넣지 않았다. 폰 화면은 PLAY에 머문다
  - 넣는다면 arena의 phase 전이에서 `state`를 쏘고 controller가 MATCH_END에 END로 가면 된다
- **`presentation/src/index.css`의 첫 페인트 색이 구 팔레트(`#0B0B0E` / `#F2F6FF`)로 남아 있다.**
  - VORTEX 리스킨 트랙이 "기존 presentation 폴더는 건드리지 않는다"를 받아 손대지 않았다.
    arena controller brand 셋은 `#101010` / `#FDFDFD`로 옮겼다
  - 증상은 첫 페인트 한 프레임의 색이 앱 렌더와 다른 것뿐이고 컴포넌트는 전부 토큰을 읽는다.
    그 폴더를 여는 트랙이 한 줄만 바꾸면 된다
- **`presentation/index.html`의 탭 제목이 `간합 프레젠테이션`으로 남아 있다.**
  - VORTEX 네이밍 통일이 "presentation 폴더는 건드리지 않는다"를 받아 손대지 않았다.
    arena controller brand 셋은 `VORTEX Arena` / `VORTEX Controller` / `VORTEX Brand`로 옮겼다
  - 구버전 동결본이라 발표 경로에는 안 뜨지만 그 폴더를 여는 트랙이 한 줄만 바꾸면 된다.
    위의 첫 페인트 색 한 줄과 같이 처리하면 된다
- **`presentation/src`에 토큰을 안 거치는 rgba 파생값 4곳이 남아 있다.**
  - `CoverSection.jsx:221`의 `rgba(5,5,6,0.4)`, `StageBackground.jsx:17`의 `rgba(216,226,240,0.05)`,
    `OutputsSection.jsx:94,96`의 `rgba(242,246,255,0.05)`와 `rgba(21,21,26,0.72)`
  - 전부 구 팔레트 값이라 VORTEX 색을 안 따라온다. arena 쪽 같은 유형 7곳은 `withAlpha`로 고쳤고
    같은 방법을 그대로 쓰면 된다. **이 트랙 범위 밖이라 손대지 않았다**
- **`judge.selftest.js:318`의 콘솔 출력에 em대시가 남아 있다.**
  - 화면 카피가 아니라 dev 콘솔 문자열이고 판정 파일이라 이 트랙이 안 건드렸다.
    `controller/src/sensors/motion.js`와 `orientation.js` 주석의 박스라인도 같다
- **표지 이미지는 Gargarella 사진작가 워터마크 저작물의 임시 시안. 제출 전 생성형 자체 제작 이미지로 필수 교체. 현 상태로 제출 불가.**
  - 워터마크가 남으므로 **크롭이나 편집도 하지 않았다**(2차 저작 위험). 임시 배치만 했고 CREDITS에 채택 기록 없음
  - 위치 `presentation-v2/public/images/cover/fencer.png`. 원본 `KakaoTalk_20260804_192012608 1.png`은 리포 루트에 남겨 뒀다
- **WHY 컨셉 유파 이미지 6장 배치 완료. 전부 라이선스 미확인 시안(표지 사진 포함). 제출 전 자체 제작 이미지로 필수 교체.**
  - 규약 경로 7장 전부 채워졌다: `cover/fencer.png`, `why/asis.png`, `why/tobe.png`, `concept/duel.png`,
    `duelist/style-1.png` `style-2.png` `style-3.png`. 플레이스홀더로 뜨는 자리는 이제 없다(실측 404 0건)
  - **CREDITS에 채택 기록 없음.** 6장 전부 출처와 라이선스를 확인하지 않았다
  - 교체는 해당 경로에 파일을 덮어쓰기만 하면 되고 코드 수정 0
- **브랜드/컨셉/유파 임베드 사진 전부 제출 전 자체 제작 교체 필수.**
  - 원본 슬라이드 SVG에 base64로 박혀 있던 사진은 코드로 임베드하지 않고 별도 파일로 반입했다.
    **반입한 5장(`keyword/dynamic|precision|immersion.png`, `naming/photo-1|2.png`)도
    라이선스 미확인 시안이다.** CREDITS에 채택 기록 없음
  - 교체는 같은 경로에 덮어쓰기만 하면 되고 코드 수정 0
  - **팀 자체 제작분이 있으면 이 목록에서 빼야 한다.** 5장의 출처를 확인하지 못해 전부 미확인으로 뒀다
  - **인사이트(PAIN POINT) 배경 사진도 같다.** `1.svg`에 base64로 박혀 있던 흐린 펜싱 사진을
    임베드하지 않고 `/images/painpoint/bg.png` 경로로 뺐다. 파일이 없어 지금은 다크 플레이스홀더이고
    쓰려면 그 경로에 파일을 놓으면 된다(코드 수정 0). **CREDITS 채택 기록 없음**
- **presentation-v2 데모 CTA의 `VITE_ARENA_URL`이 미설정이다.**
  - 지금은 버튼을 눌러도 이동하지 않고 콘솔 경고만 남는다(실측 확인).
  - `presentation-v2/.env`에 `VITE_ARENA_URL=<arena 배포 URL>`을 넣으면 새 탭으로 열린다
- **`Slide 16_9 - 5.svg`(TARGET)는 수정본이 아니다.**
  - 지시서는 "수정된 원본 SVG 스크린샷 기준"이라고 했으나 리포의 파일은 4345바이트로 이전과 동일하고
    여전히 `fill="white" fill-opacity="0.07"` 채움이 들어 있다. 렌더해서 확인했다.
  - 그래서 원 디자인은 지시서의 명시 스펙(채움 없음 + 글래스 림)을 따랐고 크기와 배치만 SVG 좌표를 유지했다.
    **수정본 SVG가 따로 있다면 받아서 대조해야 한다**
- ~~유파 인물 3종의 소스 비율이 어긋난다~~ → **해소.** 원본 `Slide 16_9 - 7.svg`에서
  인물 폭이 609.4 / 290.1 / 196.7로 **제각각인 것이 설계 의도임을 확인했다.**
  런지 자세가 넓고 서 있는 둘은 좁다. 높이를 맞추고 폭은 자연 비율대로 두는 것으로 정리했다
- **유파 상세 미디어 슬롯이 비어 있다.** 상태 1/3/5의 우측 16:9 자리는 지금 다크 플레이스홀더에
  "상세 영상 예정" 문구만 있다. 유파별 시연 영상이나 캡처가 나오면 그 자리를 채운다
- **S2 단계 0에서 라벨과 헤드라인이 배경에 묻힌다(코드 아님, 대비 문제).**
  - AS-IS 사진이 풀블리드인데 그 자리 바닥이 밝다. 실측 배경 평균 휘도
    `WHY` 주변 165.5/255, `문제` 주변 175.8/255(최대 247), 헤드라인 주변 178.1/255
  - 흰 텍스트와 red 라벨이 그 위에서 대비를 잃는다. 스크림이나 그라디언트 한 겹이 필요하다.
    **S2Why.jsx 소유 세션의 영역이라 이 세션은 손대지 않았다**
- **presentation-v2 S1 라인 60fps는 헤드리스에서 판정 불가. 실브라우저 확인이 필요하다.**
  - 헤드리스 실측 **라인 있음 18~22fps, 라인 캔버스 숨김 기준선 60.3fps**
  - **원인을 특정했다. 소프트웨어 래스터라이저의 픽셀 채우기 비용이지 내 코드 비용이 아니다.**
    드로잉버퍼만 줄이며 잰 fps가 픽셀 수에 정비례한다.
    1512x900(1,360,800px) 21.7 / 756x450(340,200px) 46.2 / 378x225(85,050px) 51.2 / 150x100(15,000px) 60.3.
    점 갱신 루프의 프레임당 JS 비용은 **0.0011ms**(16.7ms 예산의 0.007퍼센트)
  - 헤드리스는 SwiftShader라 GPU가 없다. 실기에서는 풀스크린 클리어 1회 + 드로우콜 1회이므로
    문제가 없을 것으로 보지만 **그 수치로 통과 판정을 하지 않는다**(arena fps 항목과 같은 원칙).
    실브라우저에서 S1을 열고 커서를 흔들며 확인할 것
  - MSAA는 껐다. 프래그먼트가 자체 안티에일리어싱을 하므로 중복이다. **fps 개선은 없었다**(20.1 → 18.3, 오차 범위)
- presentation-v2 제목(디스플레이) 폰트 미정. `tokens.typography.displayFamily`가 지금 본문과 같은 Pretendard다.
  폰트가 정해지면 이 키 한 줄만 바꾼다. 참조처는 S1 워드마크와 S3 컨셉명 둘뿐
- presentation-v2 S2 TO-BE 사진의 `filter: brightness` 애니메이션은 **transform/opacity 규칙의 명시적 예외 1곳**이다.
  실기에서 저프레임이 관측되면 opacity 페이드로 내린다(reduced motion 분기는 이미 opacity만 쓴다)
- dev 포트 고정: arena 5173, controller 5174, presentation 5175, brand 5176
- 루트 package.json "type": "module" 유지 필요(shared ESM 로드)
- hello의 방 코드 필드: SERVER.md는 room, 기존 골격은 code. 서버가 room ?? code로 둘 다 받는다.
  클라이언트 구현 시 room으로 통일하고 code 폴백을 제거할 것
- arena favicon.ico 404. 네 앱 공통으로 favicon이 없다. PHASE 2 배포 정리에서 함께 처리
- **arena fps 실기 확인.** V4b 시점 확인은 통과했다(발표 노트북 거의 60, 한 판 후 59 복귀).
  헤드리스 SwiftShader의 10fps는 환경 한계이고 그 수치로 통과 판정을 하지 않는다.
  다만 V4c에서 후처리 패스가 2개(잔상 EffectPass + SavePass) 늘고 빌보드 평면 2장이 붙었으므로
  **주소 뒤에 ?fps=1을 붙여 1분 교전 후 평균과 최저를 다시 확인해야 한다. 30 이상이면 통과다**
- **TODO 카피: ai-workflow 4행의 "AI가 한 것"과 "사람이 판단한 것".** 팀만 아는 내용이라 비워 두었고
  화면에는 "확정 예정"으로 정직하게 드러난다. content/sections.js의 WORKFLOW 배열을 채우면 된다
- outputs 3종 카드의 캡처 이미지 미확보. 지금은 "캡처 예정" 플레이스홀더다.
  arena와 brand 화면이 나오면 OutputsSection의 캡처 슬롯을 교체한다
- presentation/src/content/rules.js는 arena judge.js RULES의 사본이다(간합 유효 범위 35~55).
  앱 독립 배포 원칙상 arena를 import하지 않아 생긴 중복이다. shared/tokens.js로 승격하면 사라진다.
  judge.js를 고치면 이 파일도 같이 고쳐야 한다
- **저장소 밖 Web/ 폴더에 별도 git 저장소가 있다.** 커밋 1개("1번째 세팅"), 원격 없음,
  ganhap을 gitlink(서브모듈, 모드 160000)로 974e2be에 고정해 두었다. 그 커밋은 3분할 때 폐기되어
  이제 ganhap 이력에 없다. 즉 끊어진 참조다. Web/CREDITS.md도 빈 파일로 남아 있다.
  진짜 저장소는 ganhap이고 원격도 거기에만 붙어 있다. Web/.git 정리 여부는 사용자 판단 사항
- 좁은 폭(320, 390)에서 뷰포트보다 긴 섹션의 본문이 하단 고정 레일 아래를 지나간다.
  고정 내비의 정상 동작이라 레일 배경을 불투명 bg.raised로 두어 가린다. 레이아웃 버그 아님
- presentation ProgressRail 구조 결정: md(768) 미만은 우측 세로 레일이 본문 제목을 덮어(320, 390 실측)
  하단 가로 행 4개씩 2줄로 전환한다. 320에 44px 타깃 8개를 한 줄로 넣으면 352px라 넘치기 때문
- presentation 레일 라벨은 hover와 focus에서만 뜬다. 상시 노출하면 768~1440에서 본문을 덮는다(실측).
  현재 섹션 표시는 red.light 점과 aria-current가 맡는다
- arena 상대 실루엣과 배경은 코드로 그린 임시 텍스처와 그라디언트다. 1인칭이라 필요한 것은
  **상대 1인의 5포즈**이고(내 검은 3D 지오메트리라 스틸이 필요 없다), 블랙 기조 도장 이미지가 오면
  renderer의 setPoses와 setBackgroundImage로 교체한다(인터페이스 준비 완료, 키는 idle/advance/telegraph/lunge/hit)
