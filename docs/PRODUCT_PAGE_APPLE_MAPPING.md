# VORTEX 제품 페이지 = Apple Vision Pro 3탭 구조 매핑

Apple Vision Pro 페이지의 구조와 레이아웃을 그대로 따르고, 콘텐츠만 VORTEX로 바꾼다. Apple 텍스트와 이미지는 복제하지 않는다. 구조 레이아웃 스크롤 문법 타이포 위계만 재현하고, 각 자리에 우리 내용을 넣는다.

제품은 마스크와 컨트롤러 둘. 각각 이 구조를 갖는다(탭으로 전환하거나 각 제품 페이지).

---

## 0. 사이트 구조 (헤더 통합 세션에서 확정)

**다섯 탭이 각자 독립 라우트다(개정).** 예전에는 한 페이지 안의 앵커였고 탭을 누르면 스크롤이 움직였다. **Apple의 로컬 내비도 앵커가 아니라 라우트다**(실측 href: `/apple-vision-pro/`, `/apple-vision-pro/specs/`, `/os/visionos/`).

| 탭 | 주소 | 내용 |
|---|---|---|
| OVERVIEW | `/` | 히어로 + 와이드 둘 + 원칙 |
| MASK | `/mask` | 대표 미디어 + 딥다이브 + 사양 + In the Box |
| CONTROLLER | `/controller` | 같은 골격 |
| VISION | `/vision` | 와이드 + 유파 딥다이브 |
| EXPERIENCE | `/experience` | 영상 풀블리드 + 시연 연결 |

**오버뷰가 루트다.** Apple도 제품의 대표 주소가 오버뷰이고 스펙과 OS가 그 아래 다른 주소로 갈린다. `/products` 같은 상위 마디를 두지 않았다. 사이트 전체가 이 제품이라 그 마디가 아무것도 안 가른다.

랜딩, `/about`, `/duelists`, 제품 상세는 걷었다. 옛 주소는 리다이렉트로 살렸고 앵커 시절 주소(`/#mask`)는 `HashRoute`가 받아 제 라우트로 넘긴다(ROUTES.md).

**헤더도 하나다.** 예전에는 전역 헤더(fixed, 68px)가 위에 상주하고 그 아래 서브내비가 붙었다. 바 둘이 위아래로 겹쳐 자리를 다퉜고, 그래서 전역 헤더를 걷어 **서브내비가 그대로 사이트의 유일한 헤더**가 되었다. Apple에서 글로벌내비가 스크롤아웃된 뒤 서브내비가 top 0을 차지하는 자리와 같다.

| | 헤더 둘이던 때 | 지금 |
|---|---|---|
| 전역 헤더 | `fixed`, top 0, 높이 68 | 없음 |
| 제품 내비 | `sticky`, top **68** | `sticky`, top **0** |
| 앵커 여백 | `68 + 52 + 16` | `var(--pnav-h) + 16` |
| 좌측 | VORTEX(전역 헤더) | VORTEX(제품 내비가 물려받음) |

**앵커 여백에 숫자를 안 적는다.** 좁은 화면에서 바가 두 줄로 접혀 52가 아니라 **147**이 된다(320에서 실측). ProductNav가 자기 높이를 재서 `--pnav-h`로 내보내고 CSS와 현재 섹션 판정이 그것을 읽는다.

---

## 0.1 Apple 섹션 문법 (12섹션 실측, 재현 대상)

**미디어 중심이다. 카드 나열이 아니다.**

| 요소 | Apple 실측 | 우리 |
|---|---|---|
| 미디어 | 대부분 **1425x704**. 화면 폭을 꽉 채우는 가로 와이드가 각 섹션의 주인공 | 같은 비율(2.024:1)로 전폭. 1440에서 **1425x704 일치** |
| 텍스트 | 소제목 **24px** + 짧은 문장. 미디어 위나 옆에 절제되게 | 소제목 **21.8px**(`heading`), 본문 16.5px(`body`) |
| 스테이트먼트 | 전환 지점에 **56px** 큰 문장 하나짜리 섹션 | **52.5px / 800**(`display`). 미디어도 부제도 없다 |
| 딥다이브 | 핵심 구간 **7000~10000px**. 그 동안 미디어가 고정되고 텍스트가 순차로 바뀐다 | 단계마다 100dvh. 5단계 **4500px**, 3단계 2700px |
| 브리지 | 짧은 섹션(**756px**)이 딥다이브 사이를 끊는다 | 마스크와 컨트롤러 사이 한 문장. 고정 미디어 없음 |
| 영상 | 한 섹션은 영상 풀블리드 | EXPERIENCE가 그 자리. `영상 첨부 예정` 슬롯 |

**카드 나열 셋을 이 문법의 자리로 옮겼다.**

| 걷어낸 나열 | 어디로 |
|---|---|
| 안내 카드 셋 | **전환 스테이트먼트 셋.** 문장은 그대로. 부제와 링크 라벨은 한 문장 섹션이라 갈 곳이 없다 |
| 비트 스택(제품당 4) | **딥다이브의 순차 텍스트.** 한 번에 하나만 서고 스크롤이 다음으로 넘긴다 |
| 유파 3카드 | **유파 딥다이브.** 고정 미디어 옆을 셋이 차례로 지나간다. 유파와 성향과 스타일과 인용은 그대로 |
| 사양 표(제품당 5행) | **화면에서 뺐다.** `copy.js`에 남아 있고 사양 면이 생기면 그 자리를 쓴다 |
| 특징 목록(제품당 4) | **걷었다.** 딥다이브 비트가 같은 사실을 이미 문장으로 편다 |
| 3단계 목록 | 한 줄 흐름 문장으로 |
| 팀 블록 | 걷었다. 푸터가 같은 크레딧을 이미 이고 있었다 |

## 0.2 각 라우트의 순서

```
공통      내비(sticky top 0, 유일한 헤더) ... 푸터

/           히어로 와이드 + 워드마크 + 한 줄 + 팀 표기
            와이드 + 두 장치가 하나로 움직인다
            **moti 이미지** + 왜 VORTEX인가
            원칙 한 덩어리(목록 아님)

/mask       대표 미디어 747x432 + 헤드라인 + 리드
/controller 딥다이브 4단계(고정 3D + 순차 텍스트)
            사양(좌 라벨 216 / 우 값 735)
            In the Box 벤토

/vision     와이드 + 시야가 판정을 말한다
            유파 딥다이브 3단계

/experience 영상 풀블리드 + 짧은 글 + 흐름 한 줄 + 시연 연결
```

**딥다이브 고정은 sticky다. GSAP pin이 아니다.** pin은 `fixed`를 만들고, transform이 걸린 조상(R3 워프) 안에서 fixed는 뷰포트 기준을 잃는다(PITFALLS).

**글이 3D 위에 얹히지 않는다.** 뷰어는 상시 autoRotate라 밝은 크롬 면이 글자 뒤를 지나가면 대비가 그 순간에만 무너진다. 미디어가 위, 글이 아래로 자리를 갈라 **배치 자체가 보증**이 된다.

**Apple의 이미지와 영상과 문구는 복제하지 않는다.** 구조와 레이아웃과 스크롤 문법과 타이포 위계만 재현한다.

**여전히 대기.** moti 하나를 뺀 이미지와 영상 에셋 전량, 제품 3D 모델(GLB).

## 0.3 색과 얼굴

| | 값 | 근거 |
|---|---|---|
| 프라이머리 | 네이비 **#263E5F** | presentation-v2의 브랜드 네이비와 같은 값. 최악 배경(#C1C1C1)에서 **5.97:1**, 채움 위 흰 글자 **10.74:1** |
| 강조가 서는 자리 | 아이브로우 영문, 현재 탭 글자와 밑줄, 체험 예약 아웃라인, 구매 채움, 포커스 링 | 브랜드 색이 페이지마다 한 번씩 나온다 |
| 얼굴 | **SUIT Variable** | presentation-v2와 같은 CDN과 같은 버전. 축 100~900이 실제라 굵기를 합성하지 않는다 |

**레드는 없다.** 라이트 배경에서 어느 값을 써도 미달이라(#E60D15가 2.63:1) 걷었고 그 자리를 네이비가 대신한다.

---

## 상단 내비 (Apple의 Overview / Tech Specs / visionOS)

페이지 상단에 고정 내비. 좌측 워드마크(VORTEX), 우측에 탭과 CTA.

- 탭: **5탭이다(개정).** OVERVIEW, MASK, CONTROLLER, VISION, EXPERIENCE.
  **Apple의 Tech Specs 한 자리를 마스크와 컨트롤러 둘로 쪼갰다.** 제품이 둘뿐이라
  각각 자기 탭을 가지는 편이 스펙을 깊게 편다. VISION은 Apple의 visionOS 자리다.
- **다섯 다 루트(`/`) 한 페이지 안의 앵커 섹션이다.** 라우트를 나누지 않는다.
  `/products`는 `/`로, `/product/mask`와 `/product/controller`는 `/#mask`와 `/#controller`로
  보낸다(밖에 나간 링크를 살리고 화면은 한 벌만 둔다).
- **히어로는 와이드 미디어가 주인공이다.** 1425x704 슬롯이 화면 폭을 다 쓰고 워드마크와 한 줄이 그 안 좌측에 작게 앉는다(0.1절).
- CTA: Book a demo, Buy 두 버튼. **레드를 걷어 잉크 채움이다.** Book a demo는 `VITE_ARENA_URL`로 나가고 Buy는 비활성 자리표시다.
- 스크롤 시 이 서브내비 고정(Apple처럼).

> **PD-1 완료. Apple을 직접 열어 잰 값과 우리 확정값이다.**
>
> | | Apple 실측(1440x900) | 우리 |
> |---|---|---|
> | 요소 | `nav`, **sticky, top 0**, z 9997, **높이 52px**, 전폭 | `nav`, **sticky, top 0**, z 50, **52px**, 전폭 |
> | 좌측 | 제품명 **21px / 600** | 제품명 **21.8px / 700**(`heading` 토큰) |
> | 우측 탭 | Overview · Tech Specs · visionOS, **12px / 400** | OVERVIEW · TECH SPECS · EXPERIENCE, **12px**(`hud` 토큰) |
> | 현재 탭 | **밑줄**. aria-current 없음 | **밑줄 + 굵기 700 대 500 + 잉크 대 dim**(색 단독 구분 금지) |
> | Book a demo | 알약 아웃라인, radius 980px | 알약 아웃라인, radius 999px |
> | Buy | 알약 채움, 파란 배경 + 흰 글자 | 알약 채움, **잉크 `#101010` + 흰 글자**(대비 18.71) |
> | 테두리 | `border-bottom` 없음 | 없음. 배경만 옅게 깔아 글자가 안 뚫린다 |
>
> **top이 0인 이유(헤더 통합 세션에서 개정).** Apple은 글로벌내비가 스크롤아웃되면 서브내비가
> 그 자리를 이어받아 top 0에 붙는다(실측: scrollY 0에서 top 62 → 400에서 -52 → 1200부터 0).
> PD-1에서는 우리 전역 헤더가 늘 `fixed`로 상주해서 그 아래(68px)를 같은 자리로 봤는데,
> **그 헤더를 걷어냈으므로 이제 문자 그대로 top 0이다.**
>
> **고정은 sticky다. fixed가 아니다.** fixed는 transform이 걸린 조상(R3 워프)에 잡혀
> 뷰포트가 아니라 그 조상 기준이 된다(PITFALLS). 실측으로 워프와의 공존을 확인했다.
>
> **탭은 앵커 섹션이다.** 라우트를 새로 파지 않았다. 다섯 섹션(`#overview`, `#mask`,
> `#controller`, `#vision`, `#experience`)이 한 페이지에 흐르고 기준선을 지난 마지막
> 섹션이 현재로 표시된다.
>
> **Book a demo는 `VITE_ARENA_URL`로 나간다**(`arenaUrl()` 재사용). 주소가 없으면
> 비활성으로 두고 사유를 남긴다. **Buy는 갈 곳이 없어 비활성 자리표시다.**
>
> **기존 내부 탭(OVERVIEW/FEATURES)을 걷었다.** 서브내비 탭과 개념이 겹쳐 같은 이름이
> 두 벌로 읽혔다.
> **BV2-4 딥다이브는 살아 있다.** 헤더 통합 세션이 걷었던 것을 복구 세션이 되살렸고,
> Apple 문법 세션에서 **비트 스택을 순차 전환으로 바꿨다**(0.1절). 마스크와 컨트롤러가
> 각자 딥다이브 한 편씩 갖는다.

---

## 1. OVERVIEW 페이지

### 1.1 히어로 (Apple 히어로 문법)
- 메인 사진이 화면 전체(풀블리드). 그 위에 VORTEX 로고(또는 제품명), 아래 한 줄 설명.
- 우측 하단에 Book a demo와 Buy 버튼.
- 사진 자리는 이미지 첨부 예정 슬롯(풀블리드). 이미지 오면 cover.
- 제품 사이트 히어로. 제품이 주인공.

### 1.2 큰 텍스트 스테이트먼트 (Apple 이미지2 문법)
- 스크롤 내리면 큰 텍스트가 순차로 뜬다. 짧은 문장 3개가 스크롤에 따라 나타남. 우리 제품의 핵심 가치를 큰 타이포로.
- 뒤에 은은한 배경(이미지 첨부 예정 또는 블러 비주얼).

### 1.3 영상 풀블리드
- 더 내리면 영상이 화면 전체로. 제품 사용 영상(영상 첨부 예정 슬롯). 재생 컨트롤.

### 1.4 착용샷과 인터랙션 (Apple 이미지3 문법)
- 착용샷과 디테일 컷이 이미지로. 스크롤에 따라 나중에 인터랙션(고정 미디어 + 텍스트 순차). 좌우로 디테일 컷이 흐르는 가로 스크롤 또는 고정 미디어.
- 각 컷에 짧은 설명(우리 제품 특징). Apple의 Fit Dial, Light Seal 자리에 우리 부품 설명.

### 1.5 섹션 하나 더 + 푸터
- 마무리 섹션(CTA 또는 브랜드 메시지) 후 푸터.

---

## 2. 스펙 면 (MASK / CONTROLLER. 구현 완료)

**Apple `/apple-vision-pro/specs/`를 직접 열어 잰 값이다(1440).**

| | Apple 실측 | 우리 |
|---|---|---|
| 대표 이미지 | **747x432**(1.729:1), top 314. **풀블리드가 아니다** | **747x432**, 비율 1.729 일치 |
| 스펙 행 | `.techspecs-row` 17개 | 마스크 9행, 컨트롤러 8행 |
| 좌 라벨 | x 223, 폭 **216**, 24px / 600 | 폭 **216**, 21.8px / 700 |
| 우 값 | x 467, 폭 **735**, 17px | 폭 **735**, 16.5px |
| 갭 | **28px** | **28px** |
| 문서 높이 | 10066 | 6428 / 6321 |

**구조만 옮긴다. Apple의 스펙 문구와 수치는 한 글자도 가져오지 않았다.** 칩 이름, 저장 용량, 픽셀 수 같은 실제 값은 복제 금지다(DESIGN 15절).

**카테고리는 우리 제품의 것이다.**

- 마스크: 디스플레이, 시야각, 트래킹, 시간 처리, 오디오, 배터리, 무게, 크기, 연결
- 컨트롤러: 입력, 감지 축, 변환, 햅틱, 배터리, 무게, 크기, 연결

**값도 우리 것이고 실제 하드웨어 수치는 지어낼 수 없어 자리표시로 둔다.** 확정되면 `copy.js`의 `PRODUCT_DETAIL.spec` 배열만 고치고 참조처는 안 건드린다.

### 2.3 In the Box (벤토 그리드)

**Apple Vision Pro 스펙 면에는 이 섹션이 없다**(직접 열어 확인했다). 우리 기획이라 베낄 대상 자체가 없고 구성품도 우리 것이다. 첫 칸이 두 배 폭을 먹어 리듬을 만든다. 각 칸은 구성품 이미지 자리와 이름을 갖고 이미지는 첨부 예정이다.

**좁은 화면에서는 라벨이 값 위로 올라간다.** 216px 열을 320에서 쥐면 값이 한 글자씩 꺾인다.

### 2.4 제품 컨셉과 뷰 (MASK / CONTROLLER. 구현 완료)

**우리 자체 제품 콘텐츠**(presentation-v2 마스크/컨트롤러 슬라이드와 같은 문구). Apple Tech Specs 문법(좌 라벨 / 우 값)으로 눕힌다.
`copy.js`의 `PRODUCT_CONCEPT[slug]`, `ProductPage`의 "제품 컨셉" 섹션.

| 행 | 좌 라벨 | 우 값 |
|---|---|---|
| 컨셉 | `컨셉` | 마스크 "펜싱 경험을 극대화 시킨, 몰입형 XR" / 컨트롤러 "펜싱의 상징적인 곡선을 재해석한 미래형 인터페이스 컨트롤러" |
| 제품 컨셉 | `제품 컨셉` | 마스크 "기존 메쉬 시야 구조를 넓은 XR 글라스로 재해석…가볍고 역동적인 착용 경험을 제공." / 컨트롤러 "손을 자연스럽게 감싸는 인체공학적 그립으로…마스크와 통일된 CMF 아이덴티티 구현." |
| 뷰 | `뷰` | **Side / Front / Top View 이미지 슬롯 3개**(HoverMedia 호버 재생 + WaveReveal 웨이브 등장). 이미지는 첨부 예정 |

- **상단 표기(2026 KDM+ AI Workshop, Team 1, XR Fencing Glass VORTEX)는 웹사이트엔 안 넣는다**(발표 표지용).
- 뷰 슬롯은 `HoverMedia`(BRAND_MEDIA_PLAN B)라 영상이 오면 `src`만 채운다. 확보된 렌더(front1/pro2 등)를 넣을 자리.
- 대표 미디어는 `MediaSequence`(enter 1회 blur→sharp, BRAND_MEDIA_PLAN A)로 교체했다(정적 SpecMedia에서).

---

## 3. EXPERIENCE 페이지 (Apple visionOS 자리)

- Apple이 visionOS로 OS 경험을 보여주듯, 우리는 실제 훈련 경험을 영상과 인터랙션으로.
- 영상 중심. 실제로 어떤 경험을 하는지(거리 판정, 페인트 판독, 시간 팽창 등 컨셉의 인터랙션 4종).
- 이 페이지에서 Book a demo가 시연 사이트(arena)로.

---

## 4. Book a demo 연결

- Book a demo 버튼이 우리 시연 사이트(`VITE_ARENA_URL`)로 연결. `/experience` 페이지는 사라졌다.
- 실제 겨루기 데모로 넘어가는 관문.

---

## 5. 두 제품 (마스크, 컨트롤러)

- **마스크 탭과 컨트롤러 탭이다.** `/product/mask`와 `/product/controller` 별도 페이지는 걷었다.
  제품이 둘뿐이라 페이지를 나누면 같은 골격이 두 벌로 서고, 앵커 섹션이면 한 벌로 끝난다.
- 두 탭이 각각 스펙과 3D를 가진다. 2개뿐이라 각각 깊게.

---

## 6. 저작권

- Apple 이미지 영상 문구 복제 금지. 구조 레이아웃 스크롤 문법 타이포 위계만 재현.
- 스펙 항목은 카테고리 구조만 참고, 값은 우리 제품 실제 스펙(미정은 확정 예정).

---

## 7. 세션 분할

| 세션 | 범위 |
|---|---|
| PD-1 | 서브내비 3탭 골격(OVERVIEW/TECH SPECS/EXPERIENCE) + Book a demo/Buy CTA, 라우팅 |
| PD-2 | OVERVIEW: 히어로 풀블리드 + 큰 텍스트 스테이트먼트 + 영상 슬롯 |
| PD-3 | OVERVIEW: 착용샷 인터랙션 + 마무리 섹션 + 푸터 |
| PD-4 | TECH SPECS: 대표 사진 + 스펙 리스트 + In the Box 벤토 그리드 |
| PD-5 | EXPERIENCE: 영상 경험 페이지 + Book a demo 시연 연결 |

각 세션: Apple 직접 실측, 저작권 준수(구조만), 실제 픽셀 검증, npm ci clean 빌드, 애매한 회색 금지, 라이트 팔레트.

## 8. 대기
- 이미지 영상 에셋 전 페이지(첨부 예정 슬롯).
- 하드웨어 실제 스펙값(시야각, 무게, 배터리, 해상도 등) 팀 확정.
- 시연 사이트(arena) 연결.