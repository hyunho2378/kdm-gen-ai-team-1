// 책임: 캔버스 렌더 루프. 궤적, 실루엣, 파티클을 그린다.
// tokens.js의 motion.budget 상한을 반드시 참조한다.
// trailMaxSegments, particleMax, afterimageMax를 넘기지 않는다.
// 캔버스는 transform opacity 규칙의 예외 영역이다.
// SETUP 단계에서는 구현하지 않는다.
