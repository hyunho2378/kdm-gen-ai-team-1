// arena 화면 제품명 단일 원천. **컴포넌트에 제품명 문자열 하드코딩 금지.**
// presentation-v2/src/copy.js와 같은 규약이다. 이름이 또 바뀌면 이 파일 하나만 고친다.
//
// 화면 표기는 영문 VORTEX만 쓴다. 저장소명 ganhap과 코드 식별자(패키지명, CSS 클래스,
// SVG 그라디언트 id)는 표시용이 아니므로 바꾸지 않는다.
//
// index.html의 <title>은 정적 HTML이라 이 상수를 못 읽는다. 값이 바뀌면 거기도 함께 고친다.

export const BRAND = 'VORTEX';
export const APP_NAME = 'VORTEX Arena';
